# Dependencies

Read-only dependencies:

- the default work-unit and Git/worktree autonomy contracts;
- `classify-change-closeout-residue.sh`;
- `classify-proposal-worktree-hygiene.sh` when proposal scope applies; and
- `validate-closeout-worktree-wrapper.sh`.

`closeout-change`, `closeout-pr`, `repo-hygiene-cleanup`, publishers, archive
routes, and mutation helpers are later owners, not callable dependencies during
RP-00.
