# Source To Migration Packet

Run the `source-to-migration-packet` bundle.

Use this when the desired output is a migration proposal packet with rollout,
cutover, rollback, and compatibility posture.
