# Docs Documentation Area Template

Example template placeholder for a docs-focused extension pack.

This example captures the former `docs` scoped-template role as extension-pack
template content, while the former ARE workflow family is represented by the
companion `docs-are-improvement-loop` skill.
