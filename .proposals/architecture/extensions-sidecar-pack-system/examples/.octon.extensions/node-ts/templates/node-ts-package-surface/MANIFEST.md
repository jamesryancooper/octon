# Node Ts Package Surface Template

Example template placeholder for a Node.js and TypeScript extension pack.

This example captures the former `node-ts` scoped-template role as pack-local
template content inside the `.octon.extensions` proposal model.
