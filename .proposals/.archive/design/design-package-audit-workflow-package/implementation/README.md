# Implementation

This directory translates the design package into implementer-facing execution
guidance for the `audit-design-package` workflow and its assurance surfaces.

Current package status: `archived` with disposition `implemented`.

Use these files in order:

1. `minimal-implementation-blueprint.md`
2. `workflow-alignment-delta.md`
3. `first-implementation-plan.md`

These documents remain temporary historical guidance recording how the durable
`/.octon/` workflow, registry, runner, and assurance surfaces were aligned.
