# Gaps and Unknowns

- Gate letters A-E are not explicitly defined in upstream methodology docs; this audit treats them as remove-vs-remediate evidence gates and keeps remove-candidate false unless all gates are satisfied.
- Some relocation destination paths are recommendations and may require creation during implementation planning.
- This audit is read-only and does not validate runtime behavior of CI scripts; evidence is document-contract based.
