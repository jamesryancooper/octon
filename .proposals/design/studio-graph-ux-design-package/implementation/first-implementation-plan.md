# First Implementation Plan

## Workstreams

- Build the shared shell and layout primitives in the Studio runtime.
- Implement the graph canvas, node states, and selection model.
- Implement the synchronized inspector and dock states.
- Update the runtime launcher and command/catalog docs for the promoted UI.

## Acceptance

- The Studio runtime reflects the layout and state model defined in this
  proposal.
- Promotion targets no longer depend on `/.proposals/` paths after landing.
