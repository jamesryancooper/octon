# Packet - Create

Run the `create-packet` bundle.

Preserve source context under packet `resources/**`, create the packet at the
canonical active proposal path, and validate it with the base and applicable
subtype validators.
