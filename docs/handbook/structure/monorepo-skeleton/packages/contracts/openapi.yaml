openapi: 3.1.0
info:
  title: COE API
  version: 0.1.0
paths:
  /hello:
    get:
      summary: hello endpoint
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
