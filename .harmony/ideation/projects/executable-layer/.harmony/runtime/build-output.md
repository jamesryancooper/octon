# Build output

This environment does not have a Rust toolchain installed (`cargo` is unavailable), so the required commands cannot be executed here.

## `cargo check --workspace` (from `.harmony/runtime/crates/`)

```
$ cd .harmony/runtime/crates
$ cargo check --workspace
bash: line 1: cargo: command not found
```

## `cargo component check` (from `.harmony/capabilities/services/agent-platform/kv/rust/`)

```
$ cd .harmony/capabilities/services/agent-platform/kv/rust
$ cargo component check
bash: line 1: cargo: command not found
```

