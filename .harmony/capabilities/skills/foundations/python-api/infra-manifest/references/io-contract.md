# I/O Contract

This skill scaffolds local infrastructure manifests and migration setup into the target project and records a run log.

- Input parameter: `project_name` (text, required) and selected infrastructure services
- Output: compose/migration scaffolding plus execution logs
