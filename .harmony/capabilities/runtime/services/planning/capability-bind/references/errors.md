# Capability Bind Errors

- `InputValidationError`: invalid input payload.
- `UnsupportedCommandError`: unknown command mode.
- `MissingPlanError`: no inline plan and no `planPath`.
- `CapabilityBindingError`: required capabilities are unsupported in `validate` mode.
