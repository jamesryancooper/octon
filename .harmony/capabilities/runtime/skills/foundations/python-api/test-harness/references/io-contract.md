# I/O Contract

This skill scaffolds pytest harness files into the target project and records a run log.

- Input parameter: `project_name` (text, required) and context from existing package/contracts
- Output: test scaffolding files plus execution logs
