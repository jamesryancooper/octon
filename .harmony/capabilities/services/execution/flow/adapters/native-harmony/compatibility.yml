version: "1.0.0"
compatibility:
  flow_contract: ">=1.0.0 <2.0.0"
  runtime_manifest: "service-manifest-v1"
  notes:
    - Native adapter is deterministic and does not require external runtime services.
