schema_version: "design-package-v1"
package_id: "{{PACKAGE_ID}}"
title: "{{PACKAGE_TITLE}}"
summary: "{{PACKAGE_SUMMARY}}"
package_class: "{{PACKAGE_CLASS}}"
selected_modules:
{{SELECTED_MODULES_YAML}}
implementation_targets:
{{IMPLEMENTATION_TARGETS_YAML}}
status: "draft"
lifecycle:
  temporary: true
  exit_expectation: "{{EXIT_EXPECTATION}}"
validation:
  default_audit_mode: "{{DEFAULT_AUDIT_MODE}}"
  package_validator_path: {{PACKAGE_VALIDATOR_PATH}}
  conformance_validator_path: {{CONFORMANCE_VALIDATOR_PATH}}
