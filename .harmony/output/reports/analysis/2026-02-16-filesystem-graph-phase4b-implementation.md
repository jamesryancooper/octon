# Filesystem Graph Phase 4b Implementation Report

Date: 2026-02-16
Owner: Codex (architect)

## Scope
- Package `interfaces/filesystem-graph` as a real runtime wasm service (`service.json` + `service.wasm`).
- Register runtime-tier discovery and policy.
- Migrate shell scripts (`filesystem-graph.sh`, `snapshot-build.sh`, `snapshot-diff.sh`) to runtime wrappers over wasm logic.

## Delivered

### 1) Rust/WASM service implementation
Added:
- `.harmony/capabilities/services/interfaces/filesystem-graph/rust/Cargo.toml`
- `.harmony/capabilities/services/interfaces/filesystem-graph/rust/Cargo.lock`
- `.harmony/capabilities/services/interfaces/filesystem-graph/rust/.cargo/config.toml`
- `.harmony/capabilities/services/interfaces/filesystem-graph/rust/wit/world.wit`
- `.harmony/capabilities/services/interfaces/filesystem-graph/rust/src/lib.rs`

Implemented in wasm:
- Filesystem ops: `fs.list`, `fs.read`, `fs.stat`, `fs.search`
- Snapshot ops: `snapshot.build`, `snapshot.diff`, `snapshot.get-current`
- Graph ops: `kg.get-node`, `kg.neighbors`, `kg.traverse`, `kg.resolve-to-file`
- Discovery ops: `discover.start`, `discover.expand`, `discover.explain`, `discover.resolve`

Behavioral notes:
- Deterministic snapshot artifact production (`files.jsonl`, `nodes.jsonl`, `edges.jsonl`, `manifest.json`, `current`).
- Root (`.`) handling and symlink-safe traversal adapted for runtime host FS constraints.
- Pure-Rust hashing and timestamp formatting to keep build offline-compatible with existing lock state.

### 2) Runtime service packaging
Added:
- `.harmony/capabilities/services/interfaces/filesystem-graph/service.json`
- `.harmony/capabilities/services/interfaces/filesystem-graph/service.wasm`

`service.json` includes runtime integrity:
- `integrity.wasm_sha256` generated by `.harmony/runtime/run service build interfaces/filesystem-graph`

### 3) Runtime-tier registration and policy
Updated:
- `.harmony/capabilities/services/manifest.runtime.yml`
- `.harmony/capabilities/services/registry.runtime.yml`
- `.harmony/runtime/config/policy.yml`

Registered service id:
- `interfaces/filesystem-graph`

Allowed capabilities:
- `clock.read`
- `fs.read`
- `fs.write`
- `log.write`

### 4) Shell script migration to runtime wrappers
Updated:
- `.harmony/capabilities/services/interfaces/filesystem-graph/impl/filesystem-graph.sh`
- `.harmony/capabilities/services/interfaces/filesystem-graph/impl/snapshot-build.sh`
- `.harmony/capabilities/services/interfaces/filesystem-graph/impl/snapshot-diff.sh`

Wrapper behavior:
- Existing script interfaces preserved.
- Calls are routed to `.harmony/runtime/run tool interfaces/filesystem-graph ...`.
- Compatibility envelopes preserved for `filesystem-graph.sh`.
- Absolute in-repo paths are translated to repo-relative inputs for wasm contract compatibility.

## Verification

Executed successfully:
- `.harmony/runtime/run service build interfaces/filesystem-graph`
- `.harmony/runtime/run validate`
- `.harmony/runtime/run services list`
- `bash .harmony/capabilities/services/interfaces/filesystem-graph/impl/snapshot-build.sh --root . --set-current true`
- `bash .harmony/capabilities/services/interfaces/filesystem-graph/impl/snapshot-diff.sh --base <snap> --head <snap>`
- `printf '{...}' | bash .harmony/capabilities/services/interfaces/filesystem-graph/impl/filesystem-graph.sh`
- `bash .harmony/capabilities/services/_ops/scripts/validate-filesystem-graph.sh`

Result:
- Runtime discovery tiers validate.
- Service is listed and invokable.
- Snapshot build/diff wrappers and dispatcher wrapper pass smoke execution.
- Existing filesystem-graph validator passes.
