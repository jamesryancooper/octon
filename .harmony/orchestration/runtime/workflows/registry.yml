# Workflow Registry — Tier 2 Extended Metadata
#
# Extended metadata for workflow routing: versions, commands, parameters, I/O.
# Token budget: ~50 tokens per workflow entry.
#
# Load AFTER matching a workflow in manifest.yml.
# Then load workflow.yml as the authoritative contract.
# Load README.md only when human-readable staged guidance is needed.
#
# Composition note:
# - Workflows are procedural orchestrations.
# - Composite Skills are reusable capability bundles.
# - `depends_on.skill` may reference atomic or composite skills.
# - Optional per-workflow execution controls may be declared as:
#     execution_controls:
#       cancel_safe: true|false
#   Omitted `execution_controls.cancel_safe` is treated as `false`.

routing:
  explicit_command_required: false
  ambiguity_resolution: "ask"
governance:
  capability_map_ref: "../../governance/capability-map-v1.yml"
  capability_map_schema_ref: "../../governance/capability-map-v1.schema.json"
  classification_source: "workflow-capability-map"
  autonomy_policy:
    autonomous_requires_classification: "agent-ready"
    fallback_modes: ["agent-augmented", "human-only"]
workflows:
  # --- Fully specified (spec-compliant) ---
  audit-orchestration:
    version: "2.0.0"
    commands: ["/audit-orchestration"]
    parameters:
      - name: manifest
        type: file
        required: true
        description: "Migration manifest (inline YAML or file path)"
      - name: scope
        type: folder
        required: false
        default: "."
        description: "Root directory to partition and audit"
      - name: docs
        type: folder
        required: false
        description: "Companion documentation directory for optional global coherence checks"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: strategy
        type: text
        required: false
        default: "by-directory"
        description: "Partition strategy: by-directory, by-type, by-concern, auto"
      - name: concern_map
        type: text
        required: false
        description: "Manual concern-to-glob mapping (required for by-concern strategy)"
      - name: partition_count
        type: text
        required: false
        description: "Number of partitions (for auto strategy)"
      - name: run_cross_subsystem
        type: boolean
        required: false
        default: true
        description: "Run cross-subsystem coherence audit stage"
      - name: run_freshness
        type: boolean
        required: false
        default: true
        description: "Run freshness and supersession audit stage"
      - name: max_age_days
        type: text
        required: false
        default: "30"
        description: "Freshness threshold in days for stale artifact detection"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification after remediation"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for multi-pass consistency checks"
    access: human
    depends_on:
      - skill: audit-migration
        condition: "active in skill manifest"
      - skill: audit-cross-subsystem-coherence
        condition: "active in skill manifest (required when run_cross_subsystem parameter is true)"
      - skill: audit-freshness-and-supersession
        condition: "active in skill manifest (required when run_freshness parameter is true)"
    io:
      inputs:
        - name: migration_manifest
          path: "{{manifest}}"
          kind: file
          format: yaml
          required: true
          description: "Migration manifest with old/new mappings and scope"
      outputs:
        - name: consolidated_report
          path: "../../output/reports/analysis/{{date}}-migration-audit-consolidated.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Consolidated report with partitioned migration findings and optional global-stage outcomes"
        - name: partition_reports
          path: "../../output/reports/analysis/{{date}}-migration-audit-{{partition}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Individual partition audit reports"
        - name: cross_subsystem_audit_report
          path: "../../output/reports/analysis/{{date}}-cross-subsystem-coherence-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Individual cross-subsystem coherence audit report (produced if enabled)"
        - name: freshness_audit_report
          path: "../../output/reports/analysis/{{date}}-freshness-and-supersession-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Individual freshness and supersession audit report (produced if enabled)"
        - name: bounded_audit_bundle
          path: "../../output/reports/audits/{{date}}-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit evidence bundle with findings, coverage, and convergence artifacts"
  audit-pre-release:
    version: "2.2.0"
    commands: ["/audit-pre-release"]
    parameters:
      - name: subsystem
        type: folder
        required: true
        description: "Root directory of the subsystem to audit"
      - name: manifest
        type: file
        required: false
        description: "Migration manifest (inline YAML or file path); omit for health-only audit"
      - name: docs
        type: folder
        required: false
        description: "Companion documentation directory for doc-to-source alignment"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: run_cross_subsystem
        type: boolean
        required: false
        default: true
        description: "Run cross-subsystem coherence audit stage"
      - name: run_freshness
        type: boolean
        required: false
        default: true
        description: "Run freshness and supersession audit stage"
      - name: max_age_days
        type: text
        required: false
        default: "30"
        description: "Freshness threshold in days for stale artifact detection"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate enforcement for remediation verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for multi-run consistency checks"
    access: human
    depends_on:
      - skill: audit-subsystem-health
        condition: "active in skill manifest"
      - workflow: audit-orchestration
        condition: "active in workflow manifest (required only when manifest parameter is provided)"
      - skill: audit-cross-subsystem-coherence
        condition: "active in skill manifest (required when run_cross_subsystem parameter is true)"
      - skill: audit-freshness-and-supersession
        condition: "active in skill manifest (required when run_freshness parameter is true)"
    io:
      inputs: []
      outputs:
        - name: pre_release_report
          path: "../../output/reports/analysis/{{date}}-audit-pre-release.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Consolidated pre-release audit report with go/no-go recommendation"
        - name: health_audit_report
          path: "../../output/reports/analysis/{{date}}-subsystem-health-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Individual health audit report (produced by audit-subsystem-health)"
        - name: migration_audit_report
          path: "../../output/reports/analysis/{{date}}-migration-audit-consolidated.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Consolidated migration audit report (produced by audit-orchestration in migration-only mode, if manifest provided)"
        - name: cross_subsystem_audit_report
          path: "../../output/reports/analysis/{{date}}-cross-subsystem-coherence-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Individual cross-subsystem coherence audit report (produced by audit-cross-subsystem-coherence, if enabled)"
        - name: freshness_audit_report
          path: "../../output/reports/analysis/{{date}}-freshness-and-supersession-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Individual freshness and supersession audit report (produced by audit-freshness-and-supersession, if enabled)"
        - name: pre_release_audit_bundle
          path: "../../output/reports/audits/{{date}}-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle for release recommendation, context-governance evidence, and done-gate verification"
  audit-change-risk:
    version: "1.0.0"
    commands: ["/audit-change-risk"]
    parameters:
      - name: subsystem
        type: folder
        required: true
        description: "Root directory of the changed subsystem to audit"
      - name: manifest
        type: file
        required: false
        description: "Migration manifest (inline YAML or file path) used for migration-impact checks"
      - name: docs
        type: folder
        required: false
        description: "Companion documentation directory for optional coherence checks"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: run_migration
        type: boolean
        required: false
        default: true
        description: "Run migration-impact stage when manifest is available"
      - name: run_api_contract
        type: boolean
        required: false
        default: true
        description: "Run API-contract risk stage"
      - name: run_test_quality
        type: boolean
        required: false
        default: true
        description: "Run test-quality containment-risk stage"
      - name: run_operational
        type: boolean
        required: false
        default: true
        description: "Run operational-readiness risk stage"
      - name: run_cross_subsystem
        type: boolean
        required: false
        default: true
        description: "Run cross-subsystem coherence contagion-risk stage"
      - name: run_freshness
        type: boolean
        required: false
        default: true
        description: "Run freshness and supersession stale-context risk stage"
      - name: max_age_days
        type: text
        required: false
        default: "30"
        description: "Freshness threshold in days when run_freshness is enabled"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate enforcement for remediation verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for multi-run consistency checks"
    access: human
    depends_on:
      - skill: audit-subsystem-health
        condition: "active in skill manifest"
      - skill: audit-migration
        condition: "active in skill manifest (required when run_migration parameter is true and manifest is provided)"
      - skill: audit-api-contract
        condition: "active in skill manifest (required when run_api_contract parameter is true)"
      - skill: audit-test-quality
        condition: "active in skill manifest (required when run_test_quality parameter is true)"
      - skill: audit-operational-readiness
        condition: "active in skill manifest (required when run_operational parameter is true)"
      - skill: audit-cross-subsystem-coherence
        condition: "active in skill manifest (required when run_cross_subsystem parameter is true)"
      - skill: audit-freshness-and-supersession
        condition: "active in skill manifest (required when run_freshness parameter is true)"
    io:
      inputs: []
      outputs:
        - name: change_risk_report
          path: "../../output/reports/analysis/{{date}}-audit-change-risk.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Consolidated change-risk report with risk tier and recommendation"
        - name: subsystem_health_audit_report
          path: "../../output/reports/analysis/{{date}}-subsystem-health-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Subsystem-health stage report (mandatory)"
        - name: migration_audit_report
          path: "../../output/reports/analysis/{{date}}-migration-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Migration-impact stage report (produced if enabled and manifest is provided)"
        - name: api_contract_audit_report
          path: "../../output/reports/analysis/{{date}}-api-contract-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "API-contract stage report (produced if enabled)"
        - name: test_quality_audit_report
          path: "../../output/reports/analysis/{{date}}-test-quality-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Test-quality stage report (produced if enabled)"
        - name: operational_readiness_audit_report
          path: "../../output/reports/analysis/{{date}}-operational-readiness-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Operational-readiness stage report (produced if enabled)"
        - name: cross_subsystem_audit_report
          path: "../../output/reports/analysis/{{date}}-cross-subsystem-coherence-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Cross-subsystem stage report (produced if enabled)"
        - name: freshness_audit_report
          path: "../../output/reports/analysis/{{date}}-freshness-and-supersession-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Freshness stage report (produced if enabled)"
        - name: change_risk_audit_bundle
          path: "../../output/reports/audits/{{date}}-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle for change-risk recommendation and done-gate evidence"
  audit-continuous:
    version: "1.0.0"
    commands: ["/audit-continuous"]
    parameters:
      - name: subsystem
        type: folder
        required: true
        description: "Root directory of the subsystem to continuously audit"
      - name: docs
        type: folder
        required: false
        description: "Companion documentation directory for optional coherence checks"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: cadence
        type: text
        required: false
        default: "weekly"
        description: "Continuous execution cadence metadata: daily or weekly"
      - name: lookback_days
        type: text
        required: false
        default: "7"
        description: "Historical lookback window in days used for trend context"
      - name: run_operational
        type: boolean
        required: false
        default: true
        description: "Run operational-readiness layer"
      - name: run_api_contract
        type: boolean
        required: false
        default: true
        description: "Run API-contract layer"
      - name: run_test_quality
        type: boolean
        required: false
        default: true
        description: "Run test-quality layer"
      - name: run_security
        type: boolean
        required: false
        default: true
        description: "Run security/compliance layer"
      - name: run_data_governance
        type: boolean
        required: false
        default: true
        description: "Run data-governance layer"
      - name: run_cross_subsystem
        type: boolean
        required: false
        default: true
        description: "Run cross-subsystem coherence layer"
      - name: run_freshness
        type: boolean
        required: false
        default: true
        description: "Run freshness and supersession layer"
      - name: max_age_days
        type: text
        required: false
        default: "30"
        description: "Freshness threshold in days when run_freshness is enabled"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate enforcement for remediation verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for multi-run consistency checks"
    access: human
    depends_on:
      - skill: audit-subsystem-health
        condition: "active in skill manifest"
      - skill: audit-observability-coverage
        condition: "active in skill manifest"
      - skill: audit-operational-readiness
        condition: "active in skill manifest (required when run_operational parameter is true)"
      - skill: audit-api-contract
        condition: "active in skill manifest (required when run_api_contract parameter is true)"
      - skill: audit-test-quality
        condition: "active in skill manifest (required when run_test_quality parameter is true)"
      - skill: audit-security-compliance
        condition: "active in skill manifest (required when run_security parameter is true)"
      - skill: audit-data-governance
        condition: "active in skill manifest (required when run_data_governance parameter is true)"
      - skill: audit-cross-subsystem-coherence
        condition: "active in skill manifest (required when run_cross_subsystem parameter is true)"
      - skill: audit-freshness-and-supersession
        condition: "active in skill manifest (required when run_freshness parameter is true)"
    io:
      inputs: []
      outputs:
        - name: continuous_audit_report
          path: "../../output/reports/analysis/{{date}}-audit-continuous.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Consolidated audit-continuous report with ongoing risk recommendation"
        - name: subsystem_health_audit_report
          path: "../../output/reports/analysis/{{date}}-subsystem-health-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Subsystem-health stage report (mandatory)"
        - name: observability_coverage_audit_report
          path: "../../output/reports/analysis/{{date}}-observability-coverage-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Observability-coverage stage report (mandatory)"
        - name: operational_readiness_audit_report
          path: "../../output/reports/analysis/{{date}}-operational-readiness-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Operational-readiness stage report (produced if enabled)"
        - name: api_contract_audit_report
          path: "../../output/reports/analysis/{{date}}-api-contract-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "API-contract stage report (produced if enabled)"
        - name: test_quality_audit_report
          path: "../../output/reports/analysis/{{date}}-test-quality-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Test-quality stage report (produced if enabled)"
        - name: security_compliance_audit_report
          path: "../../output/reports/analysis/{{date}}-security-compliance-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Security-compliance stage report (produced if enabled)"
        - name: data_governance_audit_report
          path: "../../output/reports/analysis/{{date}}-data-governance-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Data-governance stage report (produced if enabled)"
        - name: cross_subsystem_audit_report
          path: "../../output/reports/analysis/{{date}}-cross-subsystem-coherence-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Cross-subsystem stage report (produced if enabled)"
        - name: freshness_audit_report
          path: "../../output/reports/analysis/{{date}}-freshness-and-supersession-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Freshness stage report (produced if enabled)"
        - name: continuous_audit_bundle
          path: "../../output/reports/audits/{{date}}-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle for continuous risk recommendation and done-gate evidence"
  audit-post-incident:
    version: "1.0.0"
    commands: ["/audit-post-incident"]
    parameters:
      - name: incident_id
        type: text
        required: true
        description: "Unique incident identifier used for traceability (for example INC-2026-02-23-001)"
      - name: subsystem
        type: folder
        required: true
        description: "Root directory of the incident-affected subsystem to audit"
      - name: incident_report
        type: file
        required: false
        description: "Incident report or RCA reference used for contextual grounding"
      - name: docs
        type: folder
        required: false
        description: "Companion documentation directory for optional coherence checks"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: run_security
        type: boolean
        required: false
        default: true
        description: "Run security/compliance post-incident stage"
      - name: run_data_governance
        type: boolean
        required: false
        default: true
        description: "Run data-governance post-incident stage"
      - name: run_api_contract
        type: boolean
        required: false
        default: true
        description: "Run API-contract post-incident stage"
      - name: run_test_quality
        type: boolean
        required: false
        default: true
        description: "Run test-quality post-incident stage"
      - name: run_cross_subsystem
        type: boolean
        required: false
        default: true
        description: "Run cross-subsystem coherence post-incident stage"
      - name: run_freshness
        type: boolean
        required: false
        default: true
        description: "Run freshness/supersession post-incident stage"
      - name: max_age_days
        type: text
        required: false
        default: "30"
        description: "Freshness threshold in days when run_freshness is enabled"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate enforcement for remediation verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for multi-run consistency checks"
    access: human
    depends_on:
      - skill: audit-operational-readiness
        condition: "active in skill manifest"
      - skill: audit-observability-coverage
        condition: "active in skill manifest"
      - skill: audit-security-compliance
        condition: "active in skill manifest (required when run_security parameter is true)"
      - skill: audit-data-governance
        condition: "active in skill manifest (required when run_data_governance parameter is true)"
      - skill: audit-api-contract
        condition: "active in skill manifest (required when run_api_contract parameter is true)"
      - skill: audit-test-quality
        condition: "active in skill manifest (required when run_test_quality parameter is true)"
      - skill: audit-cross-subsystem-coherence
        condition: "active in skill manifest (required when run_cross_subsystem parameter is true)"
      - skill: audit-freshness-and-supersession
        condition: "active in skill manifest (required when run_freshness parameter is true)"
    io:
      inputs: []
      outputs:
        - name: post_incident_report
          path: "../../output/reports/analysis/{{date}}-audit-post-incident.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Consolidated post-incident closure report with residual risk recommendation"
        - name: operational_readiness_audit_report
          path: "../../output/reports/analysis/{{date}}-operational-readiness-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Operational-readiness stage report (mandatory)"
        - name: observability_coverage_audit_report
          path: "../../output/reports/analysis/{{date}}-observability-coverage-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Observability-coverage stage report (mandatory)"
        - name: security_compliance_audit_report
          path: "../../output/reports/analysis/{{date}}-security-compliance-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Security-compliance stage report (produced if enabled)"
        - name: data_governance_audit_report
          path: "../../output/reports/analysis/{{date}}-data-governance-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Data-governance stage report (produced if enabled)"
        - name: api_contract_audit_report
          path: "../../output/reports/analysis/{{date}}-api-contract-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "API-contract stage report (produced if enabled)"
        - name: test_quality_audit_report
          path: "../../output/reports/analysis/{{date}}-test-quality-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Test-quality stage report (produced if enabled)"
        - name: cross_subsystem_audit_report
          path: "../../output/reports/analysis/{{date}}-cross-subsystem-coherence-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Cross-subsystem stage report (produced if enabled)"
        - name: freshness_audit_report
          path: "../../output/reports/analysis/{{date}}-freshness-and-supersession-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Freshness stage report (produced if enabled)"
        - name: post_incident_audit_bundle
          path: "../../output/reports/audits/{{date}}-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle for post-incident closure recommendation and done-gate evidence"
  audit-release-readiness:
    version: "1.0.0"
    commands: ["/audit-release-readiness"]
    parameters:
      - name: scope
        type: folder
        required: true
        description: "Root directory containing release-relevant artifacts to audit"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: run_operational
        type: boolean
        required: false
        default: true
        description: "Run audit-operational-readiness stage"
      - name: run_api_contract
        type: boolean
        required: false
        default: true
        description: "Run audit-api-contract stage"
      - name: run_test_quality
        type: boolean
        required: false
        default: true
        description: "Run audit-test-quality stage"
      - name: run_observability
        type: boolean
        required: false
        default: true
        description: "Run audit-observability-coverage stage"
      - name: run_security
        type: boolean
        required: false
        default: true
        description: "Run audit-security-compliance stage"
      - name: run_data_governance
        type: boolean
        required: false
        default: true
        description: "Run audit-data-governance stage"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate enforcement for remediation verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for multi-run consistency checks"
    access: human
    depends_on:
      - skill: audit-release-readiness
        condition: "active in skill manifest"
      - skill: audit-operational-readiness
        condition: "active in skill manifest (required when run_operational parameter is true)"
      - skill: audit-api-contract
        condition: "active in skill manifest (required when run_api_contract parameter is true)"
      - skill: audit-test-quality
        condition: "active in skill manifest (required when run_test_quality parameter is true)"
      - skill: audit-observability-coverage
        condition: "active in skill manifest (required when run_observability parameter is true)"
      - skill: audit-security-compliance
        condition: "active in skill manifest (required when run_security parameter is true)"
      - skill: audit-data-governance
        condition: "active in skill manifest (required when run_data_governance parameter is true)"
    io:
      inputs: []
      outputs:
        - name: release_readiness_workflow_report
          path: "../../output/reports/analysis/{{date}}-audit-release-readiness.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Consolidated layered release-readiness workflow report with recommendation"
        - name: release_core_audit_report
          path: "../../output/reports/analysis/{{date}}-audit-release-readiness-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Release-core stage report produced by audit-release-readiness"
        - name: operational_readiness_audit_report
          path: "../../output/reports/analysis/{{date}}-operational-readiness-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Operational-readiness stage report (produced if enabled)"
        - name: api_contract_audit_report
          path: "../../output/reports/analysis/{{date}}-api-contract-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "API-contract stage report (produced if enabled)"
        - name: test_quality_audit_report
          path: "../../output/reports/analysis/{{date}}-test-quality-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Test-quality stage report (produced if enabled)"
        - name: observability_coverage_audit_report
          path: "../../output/reports/analysis/{{date}}-observability-coverage-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Observability-coverage stage report (produced if enabled)"
        - name: security_compliance_audit_report
          path: "../../output/reports/analysis/{{date}}-security-compliance-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Security-compliance stage report (produced if enabled)"
        - name: data_governance_audit_report
          path: "../../output/reports/analysis/{{date}}-data-governance-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Data-governance stage report (produced if enabled)"
        - name: release_readiness_audit_bundle
          path: "../../output/reports/audits/{{date}}-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle for layered release-readiness recommendation and done-gate evidence"
  audit-architecture-readiness:
    version: "1.0.0"
    commands: ["/audit-architecture-readiness"]
    parameters:
      - name: target_path
        type: folder
        required: true
        description: "Harmony target path to audit: either .harmony or one top-level bounded-surface domain"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: run_cross_subsystem
        type: boolean
        required: false
        default: true
        description: "Run audit-cross-subsystem-coherence stage when the target is whole-harness"
      - name: run_domain_architecture
        type: boolean
        required: false
        default: true
        description: "Run audit-domain-architecture stage when the target is bounded-domain"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate enforcement for remediation verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for multi-run consistency checks"
    access: human
    depends_on:
      - skill: audit-architecture-readiness
        condition: "active in skill manifest"
      - skill: audit-cross-subsystem-coherence
        condition: "active in skill manifest (required when whole-harness mode + run_cross_subsystem parameter is true)"
      - skill: audit-domain-architecture
        condition: "active in skill manifest (required when bounded-domain mode + run_domain_architecture parameter is true)"
    io:
      inputs: []
      outputs:
        - name: architecture_readiness_workflow_report
          path: "../../output/reports/analysis/{{date}}-audit-architecture-readiness.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Consolidated architecture-readiness workflow report with recommendation"
        - name: architecture_readiness_audit_report
          path: "../../output/reports/analysis/{{date}}-architecture-readiness-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Primary stage report produced by audit-architecture-readiness"
        - name: architecture_readiness_summary_json
          path: "../../output/reports/analysis/{{date}}-architecture-readiness-audit-{{run_id}}.json"
          kind: file
          format: json
          determinism: unique
          description: "Machine-readable summary produced by audit-architecture-readiness"
        - name: cross_subsystem_audit_report
          path: "../../output/reports/analysis/{{date}}-cross-subsystem-coherence-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Cross-subsystem coherence report (produced if enabled and applicable)"
        - name: domain_architecture_audit_report
          path: "../../output/reports/analysis/{{date}}-domain-architecture-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Domain-architecture report (produced if enabled and applicable)"
        - name: architecture_readiness_audit_bundle
          path: "../../output/reports/audits/{{date}}-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle for architecture-readiness recommendation and done-gate evidence"
  audit-documentation:
    version: "3.0.0"
    commands: ["/audit-documentation"]
    parameters:
      - name: docs_root
        type: folder
        required: true
        description: "Root documentation directory to validate"
      - name: template_root
        type: folder
        required: false
        default: ".harmony/scaffolding/runtime/templates/docs/documentation-standards"
        description: "Canonical documentation template root"
      - name: policy_doc
        type: file
        required: false
        default: ".harmony/cognition/governance/principles/documentation-is-code.md"
        description: "Canonical docs-as-code policy document"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate enforcement for remediation verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for multi-run consistency checks"
    access: human
    depends_on:
      - skill: audit-documentation-standards
        condition: "active in skill manifest"
    io:
      inputs: []
      outputs:
        - name: docs_audit_report
          path: "../../output/reports/analysis/{{date}}-audit-documentation.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Consolidated documentation audit report with release recommendation"
        - name: docs_standards_audit_report
          path: "../../output/reports/analysis/{{date}}-documentation-standards-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Input audit report produced by audit-documentation-standards"
        - name: documentation_audit_bundle
          path: "../../output/reports/audits/{{date}}-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle for documentation recommendation and done-gate evidence"
  audit-design-proposal:
    version: "1.0.0"
    commands: ["/audit-design-proposal"]
    parameters:
      - name: proposal_path
        type: folder
        required: true
        description: "Root design proposal directory to validate and harden"
      - name: mode
        type: text
        required: false
        default: "rigorous"
        description: "Pipeline mode: rigorous or short"
      - name: executor
        type: text
        required: false
        default: "auto"
        description: "Executor backend: auto, codex, claude, or mock"
      - name: executor_bin
        type: file
        required: false
        description: "Optional explicit path to the selected executor binary"
      - name: output_slug
        type: text
        required: false
        description: "Optional stable slug for the bounded report bundle"
      - name: model
        type: text
        required: false
        description: "Optional model override forwarded to codex or claude"
      - name: prepare_only
        type: boolean
        required: false
        default: false
        description: "Materialize bundle metadata and prompt packets without invoking the executor"
      - name: summary_root
        type: folder
        required: false
        default: ".harmony/output/reports/analysis"
        description: "Root directory for the top-level summary report"
      - name: bundle_root
        type: folder
        required: false
        default: ".harmony/output/reports/workflows"
        description: "Root directory for the workflow stage-report bundle"
    access: human
    depends_on: []
    io:
      inputs:
        - name: design_proposal
          path: "{{proposal_path}}"
          kind: directory
          format: mixed
          required: true
          description: "Target proposal treated as temporary implementation design material under review, not canonical runtime authority"
      outputs:
        - name: design_proposal_workflow_summary
          path: "../../output/reports/analysis/{{date}}-audit-design-proposal.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Top-level workflow summary with selected mode, readiness verdict, changed files, and next steps"
        - name: design_proposal_audit_bundle
          path: "../../output/reports/workflows/{{date}}-audit-design-proposal-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Workflow bundle containing stage reports, metadata, validation state, and aggregate proposal deltas"
        - name: design_audit_report
          path: "../../output/reports/workflows/{{date}}-audit-design-proposal-{{slug}}/reports/01-design-proposal-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Stage report produced by the design proposal audit prompt"
        - name: design_package_remediation_report
          path: "../../output/reports/workflows/{{date}}-audit-design-proposal-{{slug}}/reports/02-design-proposal-remediation.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Short-mode remediation report and package delta receipt"
        - name: design_red_team_report
          path: "../../output/reports/workflows/{{date}}-audit-design-proposal-{{slug}}/reports/03-design-red-team.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Rigorous-mode adversarial report"
        - name: design_hardening_report
          path: "../../output/reports/workflows/{{date}}-audit-design-proposal-{{slug}}/reports/04-design-hardening.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Rigorous-mode hardening report and package delta receipt"
        - name: design_integration_report
          path: "../../output/reports/workflows/{{date}}-audit-design-proposal-{{slug}}/reports/05-design-integration.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Rigorous-mode integration report and package delta receipt"
        - name: implementation_simulation_report
          path: "../../output/reports/workflows/{{date}}-audit-design-proposal-{{slug}}/reports/06-implementation-simulation.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Buildability simulation report"
        - name: specification_closure_report
          path: "../../output/reports/workflows/{{date}}-audit-design-proposal-{{slug}}/reports/07-specification-closure.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Specification-closure report or explicit no-op receipt"
        - name: implementation_architecture_blueprint
          path: "../../output/reports/workflows/{{date}}-audit-design-proposal-{{slug}}/reports/08-minimal-implementation-architecture-blueprint.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Minimal implementer blueprint extracted from the stabilized package"
        - name: first_implementation_plan
          path: "../../output/reports/workflows/{{date}}-audit-design-proposal-{{slug}}/reports/09-first-implementation-plan.md"
          kind: file
          format: markdown
          determinism: unique
          description: "First production implementation plan derived from the blueprint"
  audit-migration-proposal:
    version: "1.0.0"
    commands: ["/audit-migration-proposal"]
    parameters:
      - name: proposal_path
        type: folder
        required: true
        description: "Root migration proposal directory to validate"
    access: human
    depends_on: []
    io:
      inputs:
        - name: migration_proposal
          path: "{{proposal_path}}"
          kind: directory
          format: mixed
          required: true
          description: "Target migration proposal under review"
      outputs:
        - name: migration_proposal_audit_summary
          path: "../../output/reports/analysis/{{date}}-audit-migration-proposal.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Top-level workflow summary for migration proposal validation"
        - name: migration_proposal_audit_bundle
          path: "../../output/reports/workflows/{{date}}-audit-migration-proposal-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Workflow bundle containing migration proposal audit metadata and validation outputs"
  audit-policy-proposal:
    version: "1.0.0"
    commands: ["/audit-policy-proposal"]
    parameters:
      - name: proposal_path
        type: folder
        required: true
        description: "Root policy proposal directory to validate"
    access: human
    depends_on: []
    io:
      inputs:
        - name: policy_proposal
          path: "{{proposal_path}}"
          kind: directory
          format: mixed
          required: true
          description: "Target policy proposal under review"
      outputs:
        - name: policy_proposal_audit_summary
          path: "../../output/reports/analysis/{{date}}-audit-policy-proposal.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Top-level workflow summary for policy proposal validation"
        - name: policy_proposal_audit_bundle
          path: "../../output/reports/workflows/{{date}}-audit-policy-proposal-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Workflow bundle containing policy proposal audit metadata and validation outputs"
  audit-architecture-proposal:
    version: "1.0.0"
    commands: ["/audit-architecture-proposal"]
    parameters:
      - name: proposal_path
        type: folder
        required: true
        description: "Root architecture proposal directory to validate"
    access: human
    depends_on: []
    io:
      inputs:
        - name: architecture_proposal
          path: "{{proposal_path}}"
          kind: directory
          format: mixed
          required: true
          description: "Target architecture proposal under review"
      outputs:
        - name: architecture_proposal_audit_summary
          path: "../../output/reports/analysis/{{date}}-audit-architecture-proposal.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Top-level workflow summary for architecture proposal validation"
        - name: architecture_proposal_audit_bundle
          path: "../../output/reports/workflows/{{date}}-audit-architecture-proposal-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Workflow bundle containing architecture proposal audit metadata and validation outputs"
  audit-workflow-system:
    version: "1.0.0"
    commands: ["/audit-workflow-system"]
    parameters:
      - name: scope
        type: folder
        required: false
        default: ".harmony/orchestration/runtime/workflows/"
        description: "Root workflow directory to audit."
      - name: severity_threshold
        type: text
        required: false
        default: "high"
        description: "Minimum severity that blocks the done gate: critical, high, medium, low, all."
      - name: include_docs
        type: boolean
        required: false
        default: true
        description: "Include companion workflow docs and rubric context in scope."
      - name: include_governance
        type: boolean
        required: false
        default: true
        description: "Include workflow governance and validation surfaces in scope."
      - name: run_live
        type: boolean
        required: false
        default: true
        description: "Run representative rehearsal scenarios in addition to static analysis."
      - name: scenario_pack
        type: text
        required: false
        default: "representative"
        description: "Representative scenario pack to run when run_live is enabled."
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict convergence verification after remediation."
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability."
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for multi-run consistency checks."
    access: human
    depends_on: []
    io:
      inputs: []
      outputs:
        - name: workflow_system_audit_report
          path: "../../output/reports/analysis/{{date}}-audit-workflow-system.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Narrative report summarizing workflow-system findings and recommendations."
        - name: workflow_system_audit_bundle
          path: "../../output/reports/audits/{{date}}-{{slug}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle for workflow-system findings, scores, scenarios, and done-gate evidence."
  # --- Skeleton entries (existing workflows, not yet fully migrated) ---
  create-harness:
    version: "1.2.0"
    commands: ["/create-harness"]
    access: human
    depends_on: []
  evaluate-harness:
    version: "1.1.0"
    commands: ["/evaluate-harness"]
    access: human
    depends_on: []
  evaluate-ci-latency:
    version: "0.1.0"
    commands: ["/evaluate-ci-latency"]
    parameters:
      - name: window_runs
        type: text
        required: false
        default: "40"
        description: "Number of recent successful PR runs used for latency analysis."
      - name: top_workflows
        type: text
        required: false
        default: "5"
        description: "Number of slow or regressed workflows to inspect for step hotspots."
      - name: gate_scope
        type: text
        required: false
        default: "required"
        description: "Latency scope to emphasize in the report: required or all."
      - name: policy_path
        type: file
        required: false
        default: ".harmony/agency/practices/standards/ci-latency-policy.json"
        description: "Policy contract defining thresholds, window sizes, and issue semantics."
    access: human
    depends_on:
      - skill: audit-ci-latency
        condition: "active in skill manifest"
    io:
      inputs: []
      outputs:
        - name: ci_latency_evaluation_report
          path: "../../output/reports/{{date}}-ci-latency-evaluation-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Read-only evaluation report for GitHub Actions latency posture and tightening candidates."
        - name: ci_latency_evaluation_summary
          path: "../../output/reports/{{date}}-ci-latency-evaluation-{{run_id}}.json"
          kind: file
          format: json
          determinism: unique
          description: "Machine-readable evaluation summary for CI latency status and recommended actions."
  migrate-harness:
    version: "1.1.0"
    commands: ["/migrate-harness"]
    access: human
    depends_on: []
  update-harness:
    version: "1.1.0"
    commands: ["/update-harness"]
    access: human
    depends_on:
      - workflow: evaluate-harness
        condition: "optional but recommended to run first"
  create-mission:
    version: "1.1.0"
    commands: ["/create-mission"]
    access: human
    depends_on: []
  complete-mission:
    version: "1.1.0"
    commands: ["/complete-mission"]
    access: human
    depends_on:
      - workflow: create-mission
        condition: "Mission must exist"
  create-skill:
    version: "2.0.0"
    commands: ["/create-skill"]
    access: human
    depends_on: []
    deprecated: true
    deprecated_by: ".harmony/capabilities/runtime/skills/meta/create-skill/"
  create-workflow:
    version: "1.0.0"
    commands: ["/create-workflow"]
    access: human
    depends_on: []
  create-design-proposal:
    version: "1.0.0"
    commands: ["/create-design-proposal"]
    parameters:
      - name: proposal_id
        type: text
        required: true
        description: "Kebab-case design proposal id and directory name under .proposals/design/"
      - name: proposal_title
        type: text
        required: true
        description: "Human-readable proposal title"
      - name: proposal_class
        type: text
        required: false
        default: "domain-runtime"
        description: "Proposal class: domain-runtime or experience-product"
      - name: promotion_scope
        type: text
        required: true
        description: "Promotion scope: harmony-internal or repo-local"
      - name: promotion_targets
        type: text
        required: true
        description: "Comma-separated durable target paths that will survive proposal removal"
      - name: include_contracts
        type: boolean
        required: false
        description: "Optional override for contracts module selection; omitted uses the class default"
      - name: include_conformance
        type: boolean
        required: false
        description: "Optional override for conformance module selection; omitted uses the class default"
      - name: include_canonicalization
        type: boolean
        required: false
        description: "Optional override for canonicalization module selection; omitted uses the class default"
    access: human
    depends_on: []
    io:
      inputs:
        - name: proposal_request
          path: "{{arguments}}"
          kind: text
          format: cli
          required: true
          description: "Proposal id, title, class, scope, promotion targets, and optional module overrides"
      outputs:
        - name: create_design_proposal_workflow_summary
          path: "../../../../../.harmony/output/reports/{{date}}-create-design-proposal.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Top-level workflow summary with proposal identity, selected modules, validator outcome, and next steps"
        - name: create_design_proposal_workflow_bundle
          path: "../../../../../.harmony/output/reports/workflows/{{date}}-create-design-proposal-{{proposal_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Workflow execution bundle containing stage receipts, validation state, scaffold inventory, and validator evidence"
        - name: create_design_proposal_validator_log
          path: "../../../../../.harmony/output/reports/workflows/{{date}}-create-design-proposal-{{proposal_id}}/standard-validator.log"
          kind: file
          format: markdown
          determinism: unique
          description: "Captured stdout and stderr from the design proposal validator stack"
        - name: design_proposal_root
          path: "../../../../../.proposals/design/{{proposal_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Scaffolded standard-governed design proposal"
        - name: proposal_manifest
          path: "../../../../../.proposals/design/{{proposal_id}}/proposal.yml"
          kind: file
          format: yaml
          determinism: unique
          description: "Root manifest for the scaffolded design proposal"
        - name: design_proposal_manifest
          path: "../../../../../.proposals/design/{{proposal_id}}/design-proposal.yml"
          kind: file
          format: yaml
          determinism: unique
          description: "Subtype manifest for the scaffolded design proposal"
        - name: proposal_registry
          path: "../../../../../.proposals/registry.yml"
          kind: file
          format: yaml
          determinism: shared
          description: "Manifest-governed proposal registry updated with the new active proposal entry"
  create-migration-proposal:
    version: "1.0.0"
    commands: ["/create-migration-proposal"]
    parameters:
      - name: proposal_id
        type: text
        required: true
        description: "Kebab-case migration proposal id and directory name under .proposals/migration/"
      - name: proposal_title
        type: text
        required: true
        description: "Human-readable proposal title"
      - name: promotion_scope
        type: text
        required: true
        description: "Promotion scope: harmony-internal or repo-local"
      - name: promotion_targets
        type: text
        required: true
        description: "Comma-separated durable target paths that will survive proposal removal"
    access: human
    depends_on: []
  create-policy-proposal:
    version: "1.0.0"
    commands: ["/create-policy-proposal"]
    parameters:
      - name: proposal_id
        type: text
        required: true
        description: "Kebab-case policy proposal id and directory name under .proposals/policy/"
      - name: proposal_title
        type: text
        required: true
        description: "Human-readable proposal title"
      - name: promotion_scope
        type: text
        required: true
        description: "Promotion scope: harmony-internal or repo-local"
      - name: promotion_targets
        type: text
        required: true
        description: "Comma-separated durable target paths that will survive proposal removal"
    access: human
    depends_on: []
  create-architecture-proposal:
    version: "1.0.0"
    commands: ["/create-architecture-proposal"]
    parameters:
      - name: proposal_id
        type: text
        required: true
        description: "Kebab-case architecture proposal id and directory name under .proposals/architecture/"
      - name: proposal_title
        type: text
        required: true
        description: "Human-readable proposal title"
      - name: promotion_scope
        type: text
        required: true
        description: "Promotion scope: harmony-internal or repo-local"
      - name: promotion_targets
        type: text
        required: true
        description: "Comma-separated durable target paths that will survive proposal removal"
    access: human
    depends_on: []
  evaluate-workflow:
    version: "1.1.0"
    commands: ["/evaluate-workflow"]
    access: human
    depends_on: []
  update-workflow:
    version: "1.0.0"
    commands: ["/update-workflow"]
    access: human
    depends_on:
      - workflow: evaluate-workflow
        condition: "optional but recommended to run first"
  refactor:
    version: "1.1.0"
    commands: ["/refactor"]
    access: human
    depends_on: []
  create-project:
    version: "1.0.0"
    commands: ["/create-project"]
    access: human
    depends_on: []
  agent-led-happy-path:
    version: "1.0.0"
    commands: ["/agent-led-happy-path"]
    access: human
    depends_on: []
  add-api-endpoint:
    version: "1.0.0"
    commands: ["/add-api-endpoint"]
    access: human
    depends_on: []
  add-ui-feature:
    version: "1.0.0"
    commands: ["/add-ui-feature"]
    access: human
    depends_on: []
  fix-a-bug:
    version: "1.0.0"
    commands: ["/fix-a-bug"]
    access: human
    depends_on: []
  handle-security-issue:
    version: "1.0.0"
    commands: ["/handle-security-issue"]
    access: human
    depends_on: []
  run-data-migration:
    version: "1.0.0"
    commands: ["/run-data-migration"]
    access: human
    depends_on: []
  python-api-foundation:
    version: "1.0.0"
    commands: ["/python-api-foundation"]
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name (lowercase, hyphens)"
      - name: description
        type: text
        required: true
        description: "One-line project description"
      - name: python_version
        type: text
        required: true
        default: "python3.12"
        description: "Python version (e.g., python3.12)"
      - name: services
        type: text
        required: false
        description: "Infrastructure services: postgres, nats, redis, s3, temporal"
      - name: skip
        type: text
        required: false
        description: "Comma-separated skill IDs to skip"
    access: human
    depends_on:
      - skill: python-scaffold-package
        condition: "active in foundations/python-api/"
      - skill: python-contract-first-api
        condition: "active in foundations/python-api/"
      - skill: python-test-harness
        condition: "active in foundations/python-api/"
      - skill: python-dev-toolchain
        condition: "active in foundations/python-api/"
      - skill: python-infra-manifest
        condition: "active in foundations/python-api/"
      - skill: python-contributor-guide
        condition: "active in foundations/python-api/"
    io:
      inputs:
        - name: project_arguments
          path: "{{arguments}}"
          kind: text
          format: cli
          required: true
          description: "Project name, description, python version, and services"
      outputs:
        - name: project_scaffold
          path: "src/{{package_name}}/"
          kind: directory
          format: python-package
          determinism: unique
          description: "Complete Python package structure"
        - name: contracts
          path: "docs/contracts/"
          kind: directory
          format: mixed
          determinism: unique
          description: "OpenAPI spec, JSON schemas, and versioning policy"
        - name: test_suite
          path: "tests/"
          kind: directory
          format: python-tests
          determinism: unique
          description: "Three-tier test pyramid"
        - name: contributor_docs
          path: "AGENT.md"
          kind: file
          format: markdown
          determinism: unique
          description: "AI agent orientation document"
  swift-macos-app-foundation:
    version: "1.0.0"
    commands: ["/swift-macos-app-foundation"]
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name (PascalCase for modules)"
      - name: description
        type: text
        required: true
        description: "One-line project description"
      - name: swift_version
        type: text
        required: true
        default: "5.9"
        description: "Swift version (e.g., 5.9, 6.0)"
      - name: macos_target
        type: text
        required: true
        default: "14"
        description: "macOS deployment target (e.g., 14, 15)"
      - name: dependencies
        type: text
        required: false
        description: "Dependencies: grdb, yams, argument-parser, async-http-client, swift-graph, ulid"
      - name: skip
        type: text
        required: false
        description: "Comma-separated skill IDs to skip"
    access: human
    depends_on:
      - skill: swift-scaffold-package
        condition: "active in foundations/swift-macos-app/"
      - skill: swift-data-layer
        condition: "active in foundations/swift-macos-app/"
      - skill: swift-daemon-service
        condition: "active in foundations/swift-macos-app/"
      - skill: swift-cli-interface
        condition: "active in foundations/swift-macos-app/"
      - skill: swift-test-harness
        condition: "active in foundations/swift-macos-app/"
      - skill: swift-contributor-guide
        condition: "active in foundations/swift-macos-app/"
    io:
      inputs:
        - name: project_arguments
          path: "{{arguments}}"
          kind: text
          format: cli
          required: true
          description: "Project name, description, swift version, macos target, and dependencies"
      outputs:
        - name: package_manifest
          path: "Package.swift"
          kind: file
          format: swift
          determinism: unique
          description: "Swift Package Manager manifest"
        - name: source_tree
          path: "Sources/{{PROJECT_NAME}}/"
          kind: directory
          format: swift-package
          determinism: unique
          description: "Complete Swift source module structure"
        - name: test_suite
          path: "Tests/{{PROJECT_NAME}}Tests/"
          kind: directory
          format: swift-tests
          determinism: unique
          description: "XCTest test suites and fixtures"
        - name: agent_doc
          path: "CLAUDE.md"
          kind: file
          format: markdown
          determinism: unique
          description: "AI agent orientation document"
  promote-from-scratchpad:
    version: "1.0.0"
    commands: ["/promote-from-scratchpad"]
    access: human
    depends_on: []
