# Contracts

Use this module for schema-backed or prose contracts that implementers must obey
without guessing.

## Expected Contents

- contract docs under `contracts/`
- schemas under `contracts/schemas/`
- positive fixtures under `contracts/fixtures/valid/`
- negative fixtures under `contracts/fixtures/invalid/`
