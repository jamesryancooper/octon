# Scaffolding Runtime

Runtime artifacts for scaffolding.

## Contents

- `_ops/scripts/` - executable scaffolding scripts (for example `init-project.sh`)
- `bootstrap/` - canonical repo-bootstrap assets consumed by `/init`
- `templates/` - canonical reusable template bundles consumed by workflows and commands
