# Runtime Knowledge Graph Index
# Machine-readable discovery index for runtime graph artifacts.

schema_version: "1.0"
files:
  - id: graph-overview
    path: README.md
    summary: Runtime graph surface orientation and ownership boundaries.
    when: First entrypoint for runtime graph artifact discovery.

  - id: graph-nodes
    path: nodes.yml
    summary: Canonical node dataset for runtime knowledge graph projections.
    when: When resolving graph nodes for traceability checks.

  - id: graph-edges
    path: edges.yml
    summary: Canonical edge dataset for runtime knowledge graph projections.
    when: When resolving typed relationships between graph nodes.
