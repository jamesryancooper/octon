# Align Bundle

Before executing the rollback-notes stages:

1. resolve the published prompt bundle for
   `octon-decision-drafter-rollback-notes`
2. stop in `auto` or `always` mode when the bundle is stale
3. in `skip` mode, continue only with an explicit degraded-execution note
4. cite the alignment receipt whenever the draft is returned
