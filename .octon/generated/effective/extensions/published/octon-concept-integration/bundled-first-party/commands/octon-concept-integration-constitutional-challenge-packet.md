# Constitutional Challenge Packet

Run the `constitutional-challenge-packet` bundle.

Use this when the concept appears to require changing Octon's kernel-level
rules rather than only its ordinary architecture.
