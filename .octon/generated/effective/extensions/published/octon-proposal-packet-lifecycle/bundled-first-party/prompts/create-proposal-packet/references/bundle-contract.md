# Bundle Contract

This bundle creates standard Octon proposal packets. It may generate prompts
and support files, but durable behavior comes only from promoted targets,
validators, publication outputs, and retained evidence.
