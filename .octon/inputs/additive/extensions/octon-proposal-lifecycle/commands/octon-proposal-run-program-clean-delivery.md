# Program - Run Clean Delivery — Disabled Compatibility Surface

This command is not registered during RP-00. Direct invocation returns
`RP00_CONTAINMENT_PUBLICATION_DISABLED` before route-graph execution, lifecycle
dispatch, or mutation. It never supplies `--execute-routes` or
`target_outcome=cleaned`.

Exact program/child refs, branches, worktrees, rollback handles, evidence, and
unrelated work remain preserved. Historical command text is interpretation-
only and cannot authorize publication, landing, sync, cleanup, Git/GitHub,
provider, archive, or branch-deletion effects.
