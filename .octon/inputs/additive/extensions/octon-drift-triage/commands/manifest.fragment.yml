schema_version: "extensions-commands-fragment-v1"
commands:
  - id: octon-drift-triage
    display_name: Drift Triage
    path: octon-drift-triage.md
    summary: "Turn changed paths into a ranked remediation packet using existing drift, alignment, and publication checks."
    access: agent
    argument_hint: "[--changed-paths <csv>] [--diff-base <ref>] [--diff-head <ref>] [--packet-path <path>] [--mode select|run] [--alignment-mode auto|always|skip]"
