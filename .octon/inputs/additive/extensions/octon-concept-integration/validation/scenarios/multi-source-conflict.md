# Scenario: Multi-Source Conflict

Validate that conflicting source claims are normalized and explicitly resolved
before packetization.

Executable coverage lives in:

- `validation/tests/test-scenario-behavior.sh`
- `validation/tests/test-scenario-fixtures.sh`
