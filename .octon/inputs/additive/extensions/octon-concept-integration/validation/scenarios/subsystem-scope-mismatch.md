# Scenario: Subsystem Scope Mismatch

Validate that subsystem-targeted integration does not silently escape the
declared subsystem or domain scope.

Executable coverage lives in:

- `validation/tests/test-scenario-behavior.sh`
- `validation/tests/test-scenario-fixtures.sh`
