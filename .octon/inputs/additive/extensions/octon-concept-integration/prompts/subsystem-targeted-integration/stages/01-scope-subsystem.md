# Subsystem Targeted Integration: Scope Subsystem

Normalize the requested subsystem or domain scope before any concept extraction.

## Output

Produce a subsystem scope record that defines:

- included paths or domains,
- excluded adjacent domains,
- acceptable cross-subsystem dependencies,
- and the escalation rule for concepts that would escape the declared scope.
