# Bundle Contract

This bundle applies packet-local revisions for review findings and creates a
revision receipt under `support/revisions/`. It keeps or returns the packet to
`in-review` and routes back to review.

The revision receipt is proposal-local evidence. It does not accept the packet,
implement durable targets, or become Octon runtime, policy, or authority.
