# Bundle Contract

The run-implementation bundle promotes durable repository targets from an
accepted proposal packet. It does not approve the proposal by itself, archive
the packet, widen scope, replace proposal manifests, or treat proposal-local
material as runtime, policy, support, or closure authority.
