# Select Creation Route

Prefer existing concept-integration and proposal workflow routes when they
already satisfy the scenario. Use a custom packet creation path only when the
existing route is too generic or the user explicitly needs a packet-specific
creation prompt.

Record the route selection, rationale, and any validators that will prove the
created packet.
