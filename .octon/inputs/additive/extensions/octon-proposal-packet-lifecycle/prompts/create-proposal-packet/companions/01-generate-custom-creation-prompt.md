# Generate Custom Creation Prompt

Use this companion only when a packet-specific creation prompt is useful. The
prompt must cite live proposal standards, preserve source context under
`resources/**`, place itself under `support/proposal-packet-creation-prompt.md`,
and state that it is operational support rather than authority.
