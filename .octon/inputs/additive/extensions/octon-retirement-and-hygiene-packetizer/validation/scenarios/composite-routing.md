# Scenario: Composite Routing

Validate that the dispatcher resolves:

- explicit `flow` selections
- audit-driven defaulting
- gap-analysis defaulting from `gap_scope`
- ablation-plan defaulting from `packet_attachment` or `target_ids`
- scan-to-reconciliation as the no-input default
