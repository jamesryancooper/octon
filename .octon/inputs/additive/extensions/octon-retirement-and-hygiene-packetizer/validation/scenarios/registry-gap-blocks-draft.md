# Scenario: Registry Gap Blocks Draft

Validate that missing or stale retirement coverage is reported as a blocking
gap in extension-authored summaries rather than silently converted into a
delete-ready cleanup plan.
