# Example: Registry Gap Analysis

Intent:

- identify missing or stale retirement coverage
- do not draft ablation steps yet

Example dispatcher input:

```yaml
gap_scope: missing
audit_dir: ".octon/state/evidence/runs/ci/repo-hygiene/example-audit"
```

Expected route:

- `registry-gap-analysis`
