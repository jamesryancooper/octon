### [assurance-bypass|PHASE2_PLAN] L1-01  (BLOCKER_BEFORE_PRIVILEGED_IMPLEMENTATION)
CLAIM: The plan's keystone (route the agent-launch through authorize_execution) does not achieve an unavoidable boundary because authorize_execution computes its Allow/Deny by executing candidate-editable code: compose_policy_receipt (execution.rs:2946) runs `bash <policy_runner> acp-enforce --policy <policy_file>` (policy.rs:432) where policy_runner=.octon/framework/engine/runtime/policy and policy_file=.octon/framework/capabilities/governance/policy/deny-by-default.v2.yml, both repo-tracked and candidate-editable, run un-sandboxed with inherited credentials in the trusted parent process.
REFS: .octon/framework/engine/runtime/crates/authority_engine/src/implementation/execution.rs:2946 ; .octon/framework/engine/runtime/crates/authority_engine/src/implementation/policy.rs:432 ; .octon/framework/engine/runtime/crates/authority_engine/src/implementation/policy.rs:313 ; .octon/framework/engine/runtime/crates/authority_engine/src/implementation/policy.rs:672
CONSEQUENCE: A prompt-injected candidate with repo-write edits deny-by-default.v2.yml (or replaces the policy runner script) to emit decision:Allow for its own ExecutorLaunch or any effect; PP-01 then issues+verifies a valid one-shot token and the boundary is satisfied while granting attacker-chosen authority. The bash <policy_runner> invocation is itself an un-sandboxed credential-inheriting spawn of candidate code more privileged than the agent.
REC: Treat the ACP policy file and policy runner as trust-root: verify them from the base ref / an out-of-tree pin (like PP-06's verifier), or embed the deny-by-default decision logic in the compiled kernel rather than an in-tree bash runner. Route this into PP-01 scope, not just PP-08 merge gates. Add OCTON_POLICY_RUNNER_OVERRIDE/OCTON_POLICY_MODE_OVERRIDE to a deny/env_clear list on the authority process.
TEST: A run in which deny-by-default.v2.yml is mutated to Allow-all (and separately, the policy runner script is replaced) is denied at authorize_execution because the policy/runner digest does not match a base-ref/out-of-tree pinned value.
FDs: FD-003,FD-004,FD-015,FD-017

### [assurance-bypass|PHASE2_PLAN] L1-02  (BLOCKER_BEFORE_PRIVILEGED_IMPLEMENTATION)
CLAIM: A second production agent-spawn path — pipeline.rs run_codex/run_claude (spawn at pipeline.rs:1856) — is not dominated by any ExecutorLaunch one-shot. Its only pre-spawn verify_authorized_effect is a conditional repo-scope effect (`if let Some(repo_effect)` at pipeline.rs:1369 and 1393); the executor spawns even when stage_repo_effect is None, and the verified effect is a RepoMutation, never an ExecutorLaunch.
REFS: .octon/framework/engine/runtime/crates/kernel/src/pipeline.rs:1856 ; .octon/framework/engine/runtime/crates/kernel/src/pipeline.rs:1369 ; .octon/framework/engine/runtime/crates/kernel/src/pipeline.rs:1393 ; .octon/framework/engine/runtime/crates/kernel/src/pipeline.rs:1205
CONSEQUENCE: PP-01 (GAP-01) names only lifecycle_executor and its acceptance test only covers the lifecycle path. The pipeline agent-spawn remains launchable without a one-shot ExecutorLaunch token; the boundary is not unavoidable. PP-02 adds env_clear to both paths but no launch guard to pipeline.
REC: Extend PP-01 to issue+verify a one-shot ExecutorLaunch immediately before run_with_stdin/child.spawn in pipeline.rs, unconditionally (not gated on stage_repo_effect). Update GAP-01's acceptance test to enumerate every agent-spawn site and assert each is dominated.
TEST: run_pipeline with stage_repo_effect=None (and with a missing/expired/revoked/consumed grant) fails to spawn the executor; a test asserts no Codex/Claude spawn site is reachable without a verified ExecutorLaunch.
FDs: FD-003,FD-004

### [fault-injection|CURRENT_REPO] L1-03  (high)
CLAIM: OCTON_POLICY_RUNNER_OVERRIDE (policy.rs:309-313) accepts an arbitrary runner executable path with NO repo-root restriction in production builds: the filter `!cfg!(test) || override_path.starts_with(&cfg.repo_root)` short-circuits to true whenever cfg!(test) is false, so the starts_with check never runs outside tests.
REFS: .octon/framework/engine/runtime/crates/authority_engine/src/implementation/policy.rs:309
CONSEQUENCE: Any actor able to set the authority process's environment points the ACP decision runner at an arbitrary executable that returns Allow — a complete authority substitution. The Phase 2 plan (3.5) hardens only OCTON_ALLOW_STALE_RUNTIME_ROUTE_BUNDLE and never mentions this override.
REC: Remove OCTON_POLICY_RUNNER_OVERRIDE from production entirely (gate behind cfg!(test)), or require the override to resolve to a base-ref/out-of-tree pinned digest. Add it to PP-01/PP-02 env_clear/deny scope.
TEST: Setting OCTON_POLICY_RUNNER_OVERRIDE=/tmp/allow_all in a release binary is ignored/rejected; authorize_execution uses only the pinned runner.
FDs: FD-003,FD-015

### [fault-injection|CURRENT_REPO] L1-04  (high)
CLAIM: OCTON_POLICY_MODE_OVERRIDE (policy_engine/src/lib.rs:969-977) lets any env-setter downgrade policy mode to `shadow`, and OCTON_ALLOW_STALE_RUNTIME_ROUTE_BUNDLE (core/src/config.rs:215) fails OPEN — it replaces verify_runtime_route_bundle with runtime_route_bundle_publication_bypass, disabling the FD-020 digest-drift enforcement the plan lists as a strength.
REFS: .octon/framework/engine/runtime/crates/policy_engine/src/lib.rs:969 ; .octon/framework/engine/runtime/crates/core/src/config.rs:215 ; .octon/framework/engine/runtime/crates/authority_engine/src/implementation/execution.rs:68
CONSEQUENCE: A bare env var (=1/=true) neutralizes either enforcement mode or the digest binding of the effective route bundle. Because agents inherit env today (no env_clear) and these are read in the parent process, the FD-020 dynamic proof and policy enforcement can be turned off from the run environment.
REC: Make the stale path fail CLOSED (deny, require a signed run-scoped exception artifact per plan 3.5) and treat OCTON_POLICY_MODE_OVERRIDE as an operator-only signed override; enumerate ALL OCTON_* allow/override/bypass vars in the safe-intermediate-states prohibited list, not just the stale-bundle one.
TEST: With OCTON_ALLOW_STALE_RUNTIME_ROUTE_BUNDLE=1 and no signed exception, RuntimeConfig construction denies (CapabilityDenied); with OCTON_POLICY_MODE_OVERRIDE=shadow, enforcement mode is unchanged absent a signed operator artifact.
FDs: FD-016,FD-020

### [assurance-bypass|PHASE2_PLAN] L1-05  (REQUIRED_BEFORE_AUTONOMOUS_CLASS_B_PUBLICATION)
CLAIM: PP-06 narrows but does not remove the second credentialed CI plane. pr-auto-merge.yml runs pull_request_target with AUTONOMY_PAT (contents:write, PR-write), checks out PR head, then cargo-builds the kernel from PR-head source (publication-wrapper-common.sh:173) and runs candidate-authored eligibility/merge scripts — so the merge ENFORCER (binary + scripts) is candidate-authored. The plan's 'base-ref eligibility' fix does not address the cargo-build-from-head enforcer, and omits release-please.yml (contents:write + AUTONOMY_PAT on push-to-main) from the second-plane inventory.
REFS: .github/workflows/pr-auto-merge.yml:119 ; .github/workflows/pr-auto-merge.yml:287 ; .github/workflows/pr-auto-merge.yml:327 ; .octon/framework/assurance/runtime/_ops/scripts/publication-wrapper-common.sh:173
CONSEQUENCE: A PR that edits protected-ci auto-merge logic (kernel or scripts) self-certifies its own merge using AUTONOMY_PAT credentials; a second credentialed plane (release-please with contents:write on main) remains outside PP-06's scope. The single-broker invariant is not achieved.
REC: Run protected-ci with a pinned release binary (not cargo build from PR head) or an out-of-tree App; inventory every AUTONOMY_PAT workflow (autonomy-release-health, pr-triage, pr-clean-state-enforcer, pr-auto-merge, release-please) in PP-06 and prove none can be edited by a candidate to change its own merge/publish outcome.
TEST: A PR that rewrites protected-ci-auto-merge.sh / evaluate-pr-autonomy-policy.sh / kernel protected-ci code to always-merge still cannot merge itself, because the enforcer runs from a pinned out-of-tree artifact.
FDs: FD-004,FD-007,FD-011

### [assurance-bypass|PHASE2_PLAN] L1-06  (REQUIRED_BEFORE_TRUST_ROOT_AUTOMATION)
CLAIM: PP-08's 'run gate workflows from base ref, not PR head' has a structural hole for push-triggered guards and in-tree gate parameters. main-change-route-guard.yml runs on: push at github.sha (post-merge, so the change is already effective — not inert per S6) and defines its protected-surface list inline in the workflow YAML (charter_path/ledger_path at 454-455). GitHub always uses the triggering ref's own workflow definition for push events, so a PR editing the guard workflow or the in-tree route/parameter files alters its own gate.
REFS: .github/workflows/main-change-route-guard.yml:454 ; .github/workflows/main-change-route-guard.yml:515 ; .github/workflows/closure-certification.yml:4 ; .octon/framework/product/contracts/default-work-unit.yml:299
CONSEQUENCE: The base-ref mitigation covers called scripts but not (a) the workflow's own trigger/logic on push, (b) the inline protected-surface list, or (c) candidate-editable gate parameter files like default-work-unit.yml route_selection_order — all of which the candidate can edit to weaken its own gate. There is also an epoch-0/bootstrap-trust gap: nothing external verifies the very first base ref's gate definition.
REC: Move protected-surface inventories and route ordering out of candidate-editable tree into an out-of-tree pin; enforce trust-root changes via an inert-landing + separate activation record (S6) so a post-merge push guard is not the only control; define an explicit epoch-0 trust anchor for the base-ref chain.
TEST: A PR that edits main-change-route-guard.yml's charter_path, or reverts default-work-unit.yml route order to direct-main-first, is blocked by an out-of-tree verifier keyed to a pinned inventory/order; a trust-root edit lands inert until a separate activation record selects it.
FDs: FD-010,FD-011,FD-017,FD-018

### [threat|CURRENT_REPO] L1-07  (high)
CLAIM: Threat model (attacker = prompt-injected candidate agent with repo-write + inherited creds). TODAY the candidate can: (a) push/merge/deploy directly with inherited GH_TOKEN/GITHUB_TOKEN/SSH (no env_clear at any spawn; codex.rs:445, policy.rs:37, both self-declared sandbox only); (b) rewrite the ACP policy/runner in-tree to Allow anything (L1-01); (c) set OCTON_POLICY_* / STALE env to substitute the runner, shadow-mode enforcement, or fail-open the route-bundle digest (L1-03/04); (d) plant git hooks/.gitattributes filters/credential-helper/insteadOf that execute under operator creds (git run unsanitized, e.g. workflow.rs:4853/5713); (e) self-certify its own PR merge via pr-auto-merge cargo-build-from-head (L1-05); (f) commit on main (route_selection_order lists direct-main first). AFTER the Phase 2 plan as written, (a) is closed by env_clear+broker, and direct-main/route-verifier gaps narrow — but (b) remains (decision function still in-tree, L1-01), the pipeline spawn (L1-02) remains ungated, the OCTON_POLICY_RUNNER/MODE overrides (L1-03/04) remain unlisted, release-please's second plane (L1-05) remains, and push-triggered self-gate editing (L1-06) remains.
REFS: .octon/framework/engine/runtime/crates/lifecycle_executor/src/codex.rs:445 ; .octon/framework/engine/runtime/crates/authority_engine/src/implementation/policy.rs:37 ; .octon/framework/engine/runtime/crates/kernel/src/workflow.rs:5713 ; .octon/framework/product/contracts/default-work-unit.yml:299
CONSEQUENCE: Even a faithfully-executed Phase 2 leaves at least four residual privileged paths (in-tree decision function, pipeline spawn, unlisted policy-override env vars, in-tree self-gates). The 'one unavoidable boundary' claim would be OVERSTATED at PP-08 completion unless these are added to scope.
REC: Add explicit acceptance gates to PP-01/02/06/08 that a prompt-injected candidate with repo-write cannot (i) alter the authorize_execution decision, (ii) reach any un-guarded spawn, (iii) set a policy-override env var that changes a decision, or (iv) edit its own merge/route gate — and enumerate ALL such surfaces as the pass criterion, not just the two-plane split.
TEST: A red-team run where the candidate is instructed to grant itself an unauthorized durable effect through each of the six vectors above is denied at the boundary for all six, with a journaled denial.
FDs: FD-003,FD-004,FD-006,FD-009,FD-010,FD-011

### [threat|PHASE2_PLAN] L1-08  (medium)
CLAIM: PP-05's sanitized-git adapter list (GIT_CONFIG_NOSYSTEM, GIT_CONFIG_GLOBAL=/dev/null, core.hooksPath=/dev/null, core.fsmonitor=false, protocol.*.allow=never, --no-verify) omits several git executable-extension points the candidate controls via repo-local .git/config and worktree files: core.sshCommand/GIT_SSH_COMMAND, credential.helper, include.path/includeIf, .gitattributes clean/smudge/filter/diff/textconv drivers, url.<base>.insteadOf, core.editor/sequence.editor, core.pager, uploadpack.packObjectsHook, core.alternateRefsCommand, .gitmodules submodule URLs, alias.*, and gpg.program. --no-verify covers hooks but NOT filter/textconv drivers (run on checkout/diff/add) or credential.helper/sshCommand (run on network ops).
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-architecture.md:104 ; .octon/framework/engine/runtime/crates/kernel/src/workflow.rs:4853 ; .octon/framework/execution-roles/_ops/scripts/git/git-autonomy-hooks-install.sh:1
CONSEQUENCE: A candidate that writes a repo-local .git/config (via include.path) or a .gitattributes filter driver / url.insteadOf redirect executes arbitrary code under the broker's credentials when the broker later runs privileged git — bypassing the hooks/protocol subset the adapter sanitizes.
REC: Replace the allow/deny list with an allowlist-only clean environment: run broker git with -c core.hooksPath=/dev/null AND explicit -c for filter.*.process=, credential.helper=, core.sshCommand=, url overrides cleared, plus GIT_CONFIG_SYSTEM=/dev/null, GIT_ATTR_NOSYSTEM=1, and refuse to operate on a working tree containing candidate-authored .gitattributes/.gitmodules unless re-validated. Enumerate the full extension surface in PP-05's negative test suite.
TEST: A planted include.path config, a .gitattributes clean/smudge/textconv filter, a url.insteadOf redirect, and a core.sshCommand each fail to execute when the broker runs push/fetch/checkout through the sanitized adapter.
FDs: FD-009

### [fault-injection|PHASE2_PLAN] L1-09  (REQUIRED_BEFORE_AUTONOMOUS_CLASS_B_PUBLICATION)
CLAIM: PP-07's reconciliation probe (git ls-remote — 'did the ref land?') cannot attribute a landed ref to THIS attempt versus a concurrent actor or an earlier retry, and does not cover the target-branch-movement race the plan names. ls-remote returns the current remote head, not proof that this attempt's push (rather than a duplicate) produced it; the single-use consume it complements is a genuine read-check-write TOCTOU (effects.rs:564) with no lock between check and write.
REFS: .octon/framework/engine/runtime/crates/authority_engine/src/implementation/effects.rs:564 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-architecture.md:122
CONSEQUENCE: The 'push landed but record lost' case can be mis-reconciled: if a concurrent/retry push moved the ref, ls-remote showing the authorized SHA does not prove this attempt did it, so an idempotent retry may either double-apply or wrongly mark success. PP-03's CAS fixes the token TOCTOU but not attempt attribution.
REC: Reconcile against an attempt-bound marker (e.g. a per-attempt pushed ref namespace refs/octon/attempts/<attempt_id>, or the expected exact source SHA + pre-registered target pre-SHA compared atomically at push time), not a bare ls-remote of the branch head. Require the broker to refuse when target pre-SHA moved (target-race) as the plan states, and record the attempt_id in the same transaction as the consume.
TEST: Crash after push-returns/before-terminal-record, with a concurrent actor advancing the ref: reconcile correctly classifies this attempt as landed/not-landed by attempt_id without re-pushing or falsely claiming success.
FDs: FD-011,FD-012

### [simplification|CURRENT_REPO] L2-01  (UNNECESSARY_COMPLEXITY)
CLAIM: 40,366 of 53,070 tracked files are under .octon/state, dominated by 20,939 files across 1,485 per-run dirs under control/execution/runs and 3,142 under evidence/runs — per-run receipt exhaust committed to git. No Phase 2 packet owns run-state exhaust as a first-class simplification; PP-07 'bounded retention' and PP-12 'trim CI gates' do not touch it.
REFS: control/execution/runs=20939 files across 1485 run dirs (git ls-files count) ; evidence/runs=3142 files (git ls-files count) ; .octon/state=40366 of 53809 total tracked (git ls-files count) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:100-111
CONSEQUENCE: For a solo operator this is the dominant clone/index/diagnose burden and it makes git the de-facto authoritative run store — exactly the loose-file substrate FD-005 says to replace. Adding SQLite for grants/tokens while leaving 24k run receipts in git creates a split substrate and does not reduce the file-count tax.
REC: Add a dedicated packet (or fold into PP-03/PP-12) that moves per-run execution/evidence receipts OUT of the tracked tree into the transactional store or an ignored local runs dir with bounded retention + compaction, keeping only a hash-anchored index in git. Make 'run exhaust is not committed' a promotion gate.
TEST: After a dogfood mission set, `git ls-files .octon/state/control/execution/runs` and `.../evidence/runs` grow by O(index entries), not O(files-per-run); fresh clone tracked-file count is bounded and does not scale with historical run count.
FDs: FD-005,FD-024,FD-001

### [simplification|PHASE2_PLAN] L2-02  (USEFUL_OPTIONAL_COMPLEXITY)
CLAIM: The plan splits the credential-custody boundary into three separate components — transactional store (PP-03), broker (PP-04), sanitized git adapter (PP-05) — and counts them toward the '≈6 net-new' total, even though migration-architecture.md:100 already places the git adapter 'inside the broker' and all three share one trust boundary and one single-writer requirement.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-architecture.md:97-105 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-architecture.md:65-69 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:51-86
CONSEQUENCE: Three components/processes where one suffices means cross-process IPC, a second serialization boundary, and independent failure/restart modes for the exact path the whole target rests on. It also weakens the 'exactly one unavoidable chokepoint' property (line 65) into 'three coordinated processes that must all be up'.
REC: Make the broker a single process that owns the rusqlite/WAL connection (sole writer) and runs the sanitized git adapter in-process. Keep PP-03/04/05 as ordered work items but state explicitly they build ONE component with one trust boundary, and revise the net-new count from ~6 to ~3 (broker+store+git-adapter, isolation launcher, activation controller).
TEST: There is exactly one long-lived process holding credentials, holding the single WAL writer handle, and executing all credentialed git; killing it fails all durable effects closed; no second process can open the store for write.
FDs: FD-005,FD-006,FD-009,FD-012

### [simplification|PHASE2_PLAN] L2-03  (USEFUL_OPTIONAL_COMPLEXITY)
CLAIM: The out-of-tree exact-SHA verifier is framed as a net-new Octon subsystem, but its role (verify a broker-pre-registered exact SHA, candidate-immutable) is satisfiable by an existing provider primitive — a least-privilege GitHub App or a protected verifier repo the candidate cannot edit — rather than new Octon code to build and maintain.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-architecture.md:135-138 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:87-99 ; comprehensive-review.md:144-147 (checks live in agent-editable change-route-projection.yml, C-003/C-006)
CONSEQUENCE: The candidate-immutability property comes from WHERE the verifier runs (outside the candidate's write reach), not from it being bespoke Octon software. Building a net-new verifier subsystem adds maintenance the solo operator owns; delegating to a GitHub App gets immutability for free and keeps its policy out of the candidate tree.
REC: Classify the verifier as 'delegate to GitHub (App/protected repo), keyed to a broker-registered SHA' rather than 'new Octon component'. The ONLY new Octon code is the broker's pre-registration of the exact SHA and its refusal on SHA/target-race (already in PP-06). Do not count the verifier toward Octon's net-new-subsystem burden.
TEST: A PR that rewrites every in-tree check to exit 0 still cannot flip its own required-check verdict, because the deciding check runs in a GitHub App/protected repo keyed to the broker-registered SHA (GAP-09 acceptance_test, satisfied without new Octon verifier code).
FDs: FD-011,FD-007,FD-019-adjacent

### [simplification|PHASE2_PLAN] L2-04  (DEFERRED_COMPLEXITY)
CLAIM: Operator doctor and cross-project inbox are listed among net-new components, but `octon doctor --architecture` already exists and a single-project Mission Queue already exists; the crosswalk itself calls the current doctor 'only a self-dev architecture linter'.
REFS: .octon/framework/engine/runtime/crates/kernel/src/commands/mod.rs:150 ; .octon/framework/engine/runtime/crates/kernel/src/commands/mod.rs:475-478 ; .octon/framework/engine/runtime/crates/kernel/src/main.rs:63-65 ; final-decision-to-repository-crosswalk.yml:269-275 (H-05: only a self-dev architecture linter, no cross-project inbox)
CONSEQUENCE: Counting extends as adds inflates the net-new surface and the operator's mental model of 'new subsystems'. The real work is small (add broker/credential/mission/storage health subcommands to the existing doctor; add a cross-root query over the existing Mission Queue), and it belongs in the LATER_OPTIMIZATION / REQUIRED_BEFORE_PRODUCTION tail, not the ESSENTIAL boundary.
REC: Reclassify doctor and inbox as EXTEND (not ADD): add `octon doctor` operator-mode checks to the existing command and a cross-project view to the existing Mission Queue. Keep them in PP-12 and out of the proof-of-architecture-gating set.
TEST: PP-12 delivers new doctor checks and a cross-project inbox as subcommands of existing `Doctor` and `Mission` commands with no new top-level CLI verb and no new crate.
FDs: FD-024

### [simplification|CURRENT_REPO] L2-05  (PERMANENTLY_OUT_OF_SCOPE)
CLAIM: The repo ships a five-verb multi-party federation surface — Trust, Proof, Attest, Delegate, Federation, plus Trust Compacts and Delegated-authority Leases (trust.rs ~1,515 LOC) — in a tool whose stated product is a single solo builder.
REFS: .octon/framework/engine/runtime/crates/kernel/src/commands/mod.rs:136-141 ; .octon/framework/engine/runtime/crates/kernel/src/main.rs:89-122 ; .octon/framework/engine/runtime/crates/kernel/src/commands/trust.rs:2-5 ; .octon/framework/engine/runtime/crates/kernel/src/commands/trust.rs:199-226
CONSEQUENCE: Federation/compacts/cross-domain attestation/portable proof import-export are inter-party trust machinery with no counterparty in a one-person deployment. They enlarge the CLI, the state model, the test surface, and the trust-root attack surface the plan must then protect (PP-08), for zero solo benefit — and they are a future enterprise concern, not this vertical's.
REC: Gate the Federation/Trust-Compact/Delegate-lease/cross-domain-Attest/Proof-import-export verbs OFF by default via one feature flag BEFORE privileged work (S9 permits this), and mark them PERMANENTLY_OUT_OF_SCOPE for the solo vertical with a documented deletion decision. Do not spend PP-08 trust-root-activation budget hardening surfaces that are off.
TEST: With the federation feature off, the solo dogfood mission set completes; `octon federation`, `octon delegate`, `octon proof import`, and `octon trust compact` are unavailable; no state root under control/trust or federation is written.
FDs: FD-001,FD-018,FD-024

### [simplification|CURRENT_REPO] L2-06  (UNNECESSARY_COMPLEXITY)
CLAIM: The 7×5 mission_class×ACP-tier routing maps ordinary repo mutation to ACP-2 'escalate' with 2-party quorum, and the retention 'Class A/B/C' name collides with FD-002's consequence-class name — routine ceremony and a naming hazard for a solo operator.
REFS: final-decision-to-repository-crosswalk.yml:28-38 (FD-002 CONTRADICTED, H-01/H-08) ; .octon/instance/governance/policies/mission-autonomy.yml:146-159 (quorum, machine_quorum_may_advise_only) ; .octon/instance/governance/policies/mission-autonomy.yml (208 LOC 7-mode matrix) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:152-164 (PP-12 crosswalk lands last)
CONSEQUENCE: A solo builder hits 2-party quorum prompts on routine reversible changes — the opposite of the product promise — and must disambiguate two different 'Class A/B/C' meanings. Landing the FD-002 crosswalk in the LAST packet (PP-12) leaves the usability regression live throughout the entire privileged build.
REC: Move the FD-002 crosswalk + Class-A/B/C rename EARLY (into PP-00 baseline-and-claim-correction, which already renames retention classes per its scope). Collapse the 7×5 matrix's solo path so routine reversible isolated-branch work clears with the independent SHA verifier as the second quorum member (zero human prompts), as the plan intends — but do it before, not after, the privileged packets.
TEST: After PP-00, a routine reversible isolated-branch change produces zero human prompts (verifier is the 2nd quorum member); the retention taxonomy and the consequence taxonomy have distinct names with no collision.
FDs: FD-002,FD-024

### [simplification|PHASE2_PLAN] L2-07  (DEFERRED_COMPLEXITY)
CLAIM: PP-03 specifies 'one SQLite/WAL authoritative store for grants/tokens/attempts/reservations/revocations' as REQUIRED_BEFORE_PROOF_OF_ARCHITECTURE, but the tested risks split: the torn-write risk (D-01/D-03, 11 non-atomic fs::write, no core atomic_write helper) needs only atomic-rename+fsync; only the consume+reserve CAS (GAP-06/GAP-12) truly needs a transaction.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:51-62 ; authority_engine has 11 non-test fs::write sites (grep count) ; no atomic_write/tempfile/fsync helper found in crates/core/src (grep, empty result) ; comprehensive-review.md:117-119 (D-01/D-03)
CONSEQUENCE: Requiring a full SQLite/WAL store as a proof-of-architecture prerequisite front-loads schema/migration/compaction work whose main testable payoff (both-or-neither on kill; files always parse) is largely obtained by an atomic-write helper. The genuinely transactional need is narrow (single-use consume + capacity reservation in one txn).
REC: Scope PP-03 to two tiers: (a) an atomic-rename+fsync write helper closing D-01/D-03 for the many single-record writes (small, BLOCKER-adjacent), and (b) a minimal rusqlite transaction ONLY for the consume+reserve CAS (GAP-06/GAP-12). Do not require migrating all grant/token state into SQLite before proof-of-architecture; keep YAML read-through where atomicity suffices.
TEST: Kill-point between two single-record writes leaves both-or-neither and all files parse (satisfied by atomic-write helper); N concurrent consumes of one single-use token yield exactly one success (satisfied by the narrow CAS) — both without a full state migration to SQLite.
FDs: FD-005,FD-012,FD-013

### [simplification|PHASE2_PLAN] L2-08  (DEFERRED_COMPLEXITY)
CLAIM: Signing (FD-014 Option A) is carried as a live design branch through PP-00 and PP-07 (add ed25519/HMAC, signature fields, verify at closeout), when the reviewer's own Phase 1 finding is that tamper-evidence already comes from git/GitHub history and Option B (reword to 'hash-chained and git-anchored') closes the gap with no new crypto subsystem.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-architecture.md:144-153 ; comprehensive-review.md:127-132 (E-06 honest half; real tamper-evidence from git/GitHub, not in-repo crypto) ; final-decision-to-repository-crosswalk.yml:157-167 (FD-014) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:100-111
CONSEQUENCE: Option A introduces a key-management surface (generation, storage in OS keychain, rotation, verification) that a solo operator must own for a benefit git history already provides. Keeping both options open through the privileged packets delays the honest-claim fix (a BLOCKER per GAP-17) behind an optional crypto build.
REC: Default to Option B (reword to hash-chained + git-anchored; identity/binding-verifying quorum) in PP-00 to clear the OVERSTATED claims BLOCKER immediately, and classify Option A signing as USEFUL_OPTIONAL_COMPLEXITY deferred to REQUIRED_BEFORE_PRODUCTION_TRUST_CLAIMS only if an off-git threat model is ever established. Do not gate proof-of-architecture on a signing subsystem.
TEST: After PP-00, no doc/schema claims 'signed/signature/attested' beyond what is implemented; quorum rejects same-actor/forged attestations by identity, not string presence — with zero new signing key material introduced.
FDs: FD-014

### [simplification|PHASE2_PLAN] L2-09  (DEFERRED_COMPLEXITY)
CLAIM: PP-09 workspace-project identity is scoped as a net-new schema + registry + project-profile change, but it owns no safety gap (GAP-16 is LATER_OPTIMIZATION, 'never authority') and the existing singleton Profile already provides per-repo identity.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:125-133 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-gap-register.yml:144-152 (GAP-16 LATER_OPTIMIZATION) ; final-decision-to-repository-crosswalk.yml:213-222 (FD-019 'never authority' half solid)
CONSEQUENCE: Adding a durable project-identity record + registry for a single operator with (typically) one repo is speculative multi-project infrastructure. It creates new administration (registry upkeep, additionalProperties tightening, digest binding) for continuity that is 'undemonstrated' rather than needed now.
REC: Keep PP-09 at LATER_OPTIMIZATION and do not build the registry until a second concurrent project actually exists in the operator's usage. Until then, the existing Profile is sufficient identity; add only the `additionalProperties:false` tightening (cheap, real).
TEST: Solo dogfood completes with the existing Profile as project identity; the workspace-project registry is built only when >1 active project is present, and even then mints no route/effect/grant field.
FDs: FD-019

### [simplification|CURRENT_REPO] L2-10  (UNNECESSARY_COMPLEXITY)
CLAIM: 42 GitHub Actions workflows (H-07) constitute an operating-platform CI surface, and the plan defers trimming them to the very last packet (PP-12 'trim non-load-bearing CI gates'), after all privileged work is built on top of them.
REFS: 42 workflows under .github/workflows (ls count) ; comprehensive-review.md:26 (42 workflows, H-07) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:152-164 (PP-12 trims gates last) ; final-decision-to-repository-crosswalk.yml:265-275 (FD-024, H-07)
CONSEQUENCE: Several workflows are themselves a second credentialed CI plane (pr-auto-merge, GAP-19) or candidate-editable verifiers (change-route-projection, GAP-09). Building the broker/verifier on top of an un-trimmed 42-workflow surface means the plan hardens gates it should be deleting, and the solo operator maintains all 42 throughout.
REC: Bring an explicit CI-surface inventory forward (into PP-06, which already touches pr-auto-merge and change-route-projection): classify each of the 42 workflows keep/merge/delete, retire the second credentialed plane and candidate-editable verifiers as part of the verifier move, and only then add the out-of-tree check — rather than trimming last in PP-12.
TEST: A workflow inventory tags each of the 42 as keep/merge/delete with rationale; the candidate-editable required-check workflow and the AUTONOMY_PAT auto-merge plane are retired before or with PP-06, not deferred to PP-12.
FDs: FD-011,FD-004,FD-024

### [fault-injection|TARGET_DESIGN] L3-F1-CONCURRENT-CONSUME  (FD-012 (GAP-06); severity high)
CLAIM: FD-012 single-use consume must be exactly-once under concurrency. Injection point: verify_authorized_effect single-use check at effects.rs:564 (reads record.status=="consumed") vs the mutating persist at effects.rs:658 (persist_token_record) — today a lock-free read-check-write with no CAS between them (D-02/J-04). Target maps this to the SQLite/WAL transactional CAS (PP-03). Fault: spawn N=8 threads/processes each calling verify_authorized_effect on the SAME single_use token record simultaneously.
REFS: effects.rs:564 ; effects.rs:658 ; effects.rs:1154 ; policy.rs:86
CONSEQUENCE: Without CAS, two racers both pass line-564 before either writes line-658 → two mint_verified_effect → the same one-shot ExecutorLaunch or push authorized twice; the 'single unavoidable boundary' is punctured.
REC: Implement consume as `UPDATE effect_tokens SET status='consumed' WHERE token_id=? AND status<>'consumed'` and treat rows_affected==1 as the sole consume; assert in test.
TEST: 8 concurrent verify_authorized_effect calls on one single_use token → EXACTLY ONE returns a VerifiedEffect; the other 7 return EFFECT_TOKEN_ALREADY_CONSUMED; the SQLite row shows status='consumed' with consumption_count==1. PASS only when count is exactly one; FAIL if two succeed or count>1.
FDs: FD-012,FD-011,FD-004

### [fault-injection|TARGET_DESIGN] L3-F2-SIGKILL-KILLPOINTS  (FD-005 (GAP-05); severity high)
CLAIM: FD-005 both-or-neither atomicity across the four kill-points of one effect. Injection points mapped to HEAD: (a) pre-reserve = before append_effect_token_event (effects.rs:576); (b) post-reserve-pre-effect = between the consumed-event write (effects.rs:605) and the material attempt; (c) post-effect-pre-record = after the external effect but before persist_token_record (effects.rs:658); (d) post-record = after 658 returns. Target: these become BEGIN/COMMIT boundaries in the SQLite store (PP-03) with an attempt row written pending BEFORE the external effect. Fault: SIGKILL the process at each of the four points.
REFS: effects.rs:576 ; effects.rs:605 ; effects.rs:658 ; policy.rs:86
CONSEQUENCE: Bare fs::write (policy.rs:86, no temp+rename/fsync) can leave a truncated/torn token record; a crash between the consumed-event append and the record persist leaves the token appearing un-consumed while the event journal says consumed — split state, replayable authorization.
REC: Wrap reserve+consume+terminal in one SQLite transaction; write the attempt row 'pending' before the external effect; make persistence atomic (WAL). Each state file must always parse after any kill.
TEST: For each of the 4 kill-points: after SIGKILL, the store is in a both-or-neither state (token either fully consumed with a terminal attempt, or fully un-consumed with no attempt) and EVERY state file parses. Kill at post-effect-pre-record leaves exactly one 'pending' attempt awaiting reconcile (feeds F3). PASS iff no torn/partial record and no double-effect on restart.
FDs: FD-005,FD-012

### [fault-injection|TARGET_DESIGN] L3-F3-PUSH-LANDED-RECORD-LOST  (FD-012 (GAP-11); severity high)
CLAIM: FD-012 unknown-outcome reconciliation: push subprocess returned success but the terminal record was lost. Injection point maps to the git push sites at kernel/src/lifecycle_program.rs:19499 and :19518 (and the exact-SHA read at lifecycle_program.rs:5968 rev-parse); today no post-push 'did it land?' probe exists (D-05). Target: the attempt lifecycle + idempotent reconcile in PP-07. Fault: allow the broker's `git push` to succeed on the remote, then SIGKILL before the terminal 'landed' record is committed; on restart run reconciliation.
REFS: kernel/src/lifecycle_program.rs:19499 ; kernel/src/lifecycle_program.rs:19518 ; kernel/src/lifecycle_program.rs:5968 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-gap-register.yml:99
CONSEQUENCE: Naive retry re-pushes an already-landed ref (double effect) or, worse, force-pushes over newer history; in-memory dedup is lost on crash so the guard is gone exactly when needed.
REC: Before any retry, reconcile each 'pending' attempt with `git ls-remote <remote> <ref>` and compare to the authorized landed_commit; classify LANDED (mark terminal, no re-push) / NOT-LANDED (safe retry) / DIVERGED (fail closed, escalate).
TEST: After crash with push-succeeded/record-lost: reconcile probes ls-remote, finds the authorized SHA present, marks the attempt LANDED, and performs ZERO re-push. Instrument the push wrapper with a call-counter; PASS iff post-reconcile push-count for that ref == 0 and the terminal record is now present.
FDs: FD-012

### [fault-injection|TARGET_DESIGN] L3-F4-DUPLICATE-DISPATCH  (FD-012 (GAP-11); severity medium)
CLAIM: FD-012 idempotency: duplicate dispatch of the same logical effect (same idempotency_key) must collapse to one. Today idempotency_key is an OPTIONAL journal field, not an enforced dedup key (D-06), and cross-process dedup is an in-memory map. Injection point: the effect-request entry into verify_authorized_effect before effects.rs:576. Target: unique constraint on (idempotency_key) in the SQLite attempts table (PP-03/PP-07). Fault: submit the same effect request twice back-to-back (retry-storm) and once across a process restart.
REFS: effects.rs:576 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-1/final-decision-to-repository-crosswalk.yml:135 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-gap-register.yml:99
CONSEQUENCE: Without a durable dedup key, a retried request after crash performs the external effect a second time (double push/merge), because the only dedup that existed was in-memory and is gone.
REC: Enforce a UNIQUE(idempotency_key) constraint in the store; a duplicate insert returns the prior terminal outcome instead of re-performing the effect.
TEST: Two identical effect requests (same idempotency_key), including one across a restart → exactly one external effect performed; the second returns the recorded outcome. PASS iff external-effect counter==1.
FDs: FD-012

### [fault-injection|TARGET_DESIGN] L3-F5-TARGET-RACE-EXACT-SHA  (FD-011 (GAP-09); severity high)
CLAIM: FD-011 exact source/target binding: the target branch moved between authorize and land. Injection points: the exact-SHA read at kernel/src/lifecycle_program.rs:5968 (rev-parse) and the push at :19499/:19518; today the required exact-SHA check lives in candidate-editable change-route-projection.yml and passes trivially in the no-receipt path (C-003/C-006). Target: broker pushes with exact-SHA binding and an out-of-tree verifier keyed to a pre-registered SHA (PP-06). Fault: advance the target ref (main) by one commit AFTER the authorization records target-pre-SHA but BEFORE the broker push.
REFS: kernel/src/lifecycle_program.rs:5968 ; kernel/src/lifecycle_program.rs:19499 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-1/final-decision-to-repository-crosswalk.yml:122 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/safe-intermediate-states.md:38
CONSEQUENCE: A no-fast-forward or force land onto a moved target silently overwrites intervening history, or lands a change validated against a base that no longer exists — the wrong-SHA landing the safe-intermediate-states doc names PROHIBITED.
REC: Broker push must use compare-and-swap semantics (--force-with-lease=<expected-pre-SHA> or equivalent ref-update precondition) and refuse if resolved target pre-SHA != authorized target-pre-SHA, or resolved head != authorized landed_commit.
TEST: Move target ref one commit after authorize; broker push is REFUSED with a target-race error and performs no ref update. PASS iff the remote ref is unchanged and the operation ends failed-safe; FAIL if the push lands.
FDs: FD-011,FD-010

### [fault-injection|TARGET_DESIGN] L3-F6-REVOKE-MID-DISPATCH  (FD-004 (GAP-01); severity high)
CLAIM: FD-004/FD-012 revocation must win a race with consumption. Injection point: the active-revocation load+reject at effects.rs:482-498, which runs BEFORE the consume at 564-658. Fault: write an active revocation ref for the grant concurrently with an in-flight verify_authorized_effect (revoke-mid-flight), and separately revoke AFTER line-498 passed but BEFORE the external effect fires.
REFS: effects.rs:482 ; effects.rs:497 ; effects.rs:564 ; effects.rs:658
CONSEQUENCE: The revocation check (482-498) and the consume (658) are not in one transaction; a revocation landing in that gap can be missed, so a revoked grant still authorizes the external effect. In the target the revocation and the consume must share the SQLite transaction so revocation strictly precedes or blocks consume.
REC: Read active revocations inside the same transaction that performs the CAS consume; a revocation committed before COMMIT must abort the consume. Re-check revocation immediately before the broker fires the external effect.
TEST: (a) Concurrent revoke during verify → the verify returns EFFECT_TOKEN_REVOKED and no external effect fires. (b) Revoke after consume-decided-but-before-broker-effect → broker re-checks and refuses. PASS iff no external effect occurs under any interleaving where the revocation commits before the effect.
FDs: FD-004,FD-012

### [fault-injection|TARGET_DESIGN] L3-F7-NEAR-FULL-DISK-TERMINAL-EVIDENCE  (FD-013 (GAP-12); severity medium)
CLAIM: FD-013 capacity reservation: terminal evidence must record even under near-full disk. Injection point: the write path persist_token_record→write_json at effects.rs:1154 / policy.rs:86 (bare fs::write, no ENOSPC handling — D-04/E-04). Target: capacity reserved in the same operation transaction (PP-03/PP-07). Fault: run on a loopback/quota-limited filesystem filled to a few KB free, then drive a denial/failure/revocation/rollback/closeout that must each write a terminal record.
REFS: effects.rs:1154 ; policy.rs:86 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-1/final-decision-to-repository-crosswalk.yml:146 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-gap-register.yml:108
CONSEQUENCE: A bare fs::write that hits ENOSPC mid-write leaves a torn terminal record or drops the denial/rollback evidence entirely — the system fails without a durable record of failing, defeating fail-closed accountability.
REC: Reserve the evidence-write budget inside the operation transaction before the external effect; if reservation fails, deny the operation up-front (fail-closed) rather than discovering ENOSPC at terminal-write time.
TEST: On a near-full FS: (a) an operation that cannot reserve terminal-evidence budget is DENIED before any external effect; (b) an operation that reserved successfully always writes a complete, parseable terminal record. PASS iff no operation performs an external effect without a durable terminal record.
FDs: FD-013

### [fault-injection|TARGET_DESIGN] L3-F8-SAME-CHANGE-SELF-CERT  (FD-017 (GAP-13); severity high)
CLAIM: FD-017 same-change self-certification must be impossible. Today certification gates check out and run the PR's OWN head ref (F-017-2), and promotion_blockers() (evolution.rs:541) is unwired. Injection point: the self-dev/trust-root merge gate (PP-08) which must run verifiers from the BASE ref. Fault: craft a PR that edits its own validator/gate script to `exit 0` and touches a trust-root surface, then run the merge gate.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-1/comprehensive-review.md:151 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-gap-register.yml:117 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-architecture.md:160
CONSEQUENCE: A change that weakens its own validator and is certified by the weakened validator is the canonical self-trust-root failure; without base-ref execution the entire self-development safety claim is vacuous.
REC: Run all trust-root/self-dev verifiers from the base ref (not PR head); wire promotion_blockers() onto the enforced gate; land trust-root edits inert until a separate activation record selects them.
TEST: A PR that rewrites its own validator to exit 0 is BLOCKED by the base-ref verifier; a trust-root edit remains inert (no runtime effect) until a distinct activation record is applied. PASS iff the weakened-validator PR cannot merge and the edit has no effect pre-activation.
FDs: FD-017,FD-011

### [fault-injection|TARGET_DESIGN] L3-F9-CREDENTIAL-SCRUB-NEGATIVE  (FD-006 (GAP-02); severity high)
CLAIM: FD-006/FD-022 credentialless agents — NEGATIVE test (must fail to see creds). Injection point: the agent spawn at codex.rs:254 via build_executor_command (codex.rs:416), which today applies NO env_clear and runs --cd repo_root (codex.rs:433/463); child inherits GITHUB_TOKEN/GH_TOKEN/SSH/API keys (B-01/J-02). Target: env_clear + allowlist at BOTH spawn paths (PP-02/PP-04). Fault: set a sentinel secret (e.g. OCTON_SENTINEL_SECRET and a fake GH_TOKEN) in the parent, spawn the candidate, have it dump its env and run `gh auth status`.
REFS: codex.rs:254 ; codex.rs:416 ; codex.rs:433 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-gap-register.yml:18
CONSEQUENCE: If the child sees the sentinel, the agent holds durable credentials and can perform durable effects outside the broker — the single-boundary property is false. This is a negative test whose PASS means the capability is ABSENT.
REC: Command::env_clear() + explicit non-credential allowlist at codex.rs:416 (lifecycle path) AND the authority_engine/pipeline spawn path (B-03); verify by sentinel.
TEST: Child env does NOT contain OCTON_SENTINEL_SECRET or any GH_TOKEN/GITHUB_TOKEN/SSH_AUTH_SOCK/API-key var; `gh auth status` in the child FAILS. PASS iff sentinel absent AND gh unauthenticated. This test must be run against BOTH spawn paths (PP-02 exit gate requires both).
FDs: FD-006,FD-022,FD-016

### [fault-injection|TARGET_DESIGN] L3-F10-SANDBOX-ESCAPE-NEGATIVE  (FD-008 (GAP-03); severity high)
CLAIM: FD-008 native isolation — NEGATIVE escape suite. Injection point: the spawn at codex.rs:254 with --cd repo_root (codex.rs:433/463) on canonical .git; today no runtime-applied sandbox exists (grep for sandbox-exec/seatbelt/unshare = 0, B-04/B-05); the only isolation is the third-party Codex CLI's self-declared --sandbox (claude path has none). Target: runtime-applied macOS seatbelt profile + disposable worktree/GIT_DIR (PP-02). Fault: from inside the candidate attempt (a) write outside the disposable worktree, (b) open a network socket to a disallowed host, (c) mutate canonical .git/refs or objects, (d) read the parent process env.
REFS: codex.rs:254 ; codex.rs:433 ; codex.rs:463 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-1/final-decision-to-repository-crosswalk.yml:91
CONSEQUENCE: If any escape succeeds, the candidate can mutate canonical refs directly (bypassing the broker) or exfiltrate — the isolation boundary is illusory. NOTE: this proves denial of the paths tested, NEVER absence of all escapes (see L3-R2).
REC: Apply a runtime-owned seatbelt/App Sandbox profile at spawn (not the executor CLI's flag); launch in a disposable worktree with a separate GIT_DIR; refuse launch on unsupported platforms. Keep the worktree-hygiene classifier as a backstop.
TEST: Each of the 4 escape attempts is DENIED by the runtime-applied sandbox; canonical .git/refs and objects are byte-identical before and after the candidate run. PASS iff all 4 denied AND canonical refs unchanged until brokered promotion.
FDs: FD-008,FD-006

### [fault-injection|TARGET_DESIGN] L3-F11-DIGEST-DRIFT-RESOLVER-DENY  (FD-020 (PP-10); severity medium)
CLAIM: FD-020 digest-drift deny (UF-04) — upgrade the SATISFIED-but-static verdict to executed dynamic proof (dynamic_followup REQUIRES_DYNAMIC_PROOF, G-04). Injection point: the runtime_resolver digest binding (PP-10 preserve-and-prove). Fault: mutate one input to the effective-route bundle so its recomputed digest != the pinned digest, then invoke the resolver.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-1/final-decision-to-repository-crosswalk.yml:223 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:134
CONSEQUENCE: FD-020 is the strongest satisfied decision but the deny path was only statically inspected; if drift did NOT deny, the digest binding is decorative. This is a cheap proof that converts an inference into an executed gate.
REC: Add a test that perturbs a bundle input and asserts the resolver denies (CAPABILITY_DENIED) rather than serving the drifted bundle; also test a forbidden consumer is denied.
TEST: Input drift → resolver returns CAPABILITY_DENIED (executed, not inferred); forbidden consumer → denied. PASS iff drift is denied; this is the one place a solo builder CAN cheaply reach DYNAMICALLY_EXECUTED.
FDs: FD-020

### [fault-injection|TARGET_DESIGN] L3-F12-TRUST-ACTIVATION-ROLLBACK  (FD-018 (GAP-14); severity high)
CLAIM: FD-018 trust-root activation fault at each stage must end old / healthy-new / auto-rollback. Today: no staged rollback-capable activation; approval is presence-only (any --approval-ref string flips status, J-06/F-018-1); promotion_blockers() unwired (F-018-2). Injection point: the new inert-landing + staged activation controller (PP-08). Fault: inject a fault at each activation stage — (a) pre-activation validation, (b) mid-swap, (c) post-swap health-check — and separately submit an activation whose new version would widen its own authority.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-1/final-decision-to-repository-crosswalk.yml:201 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-gap-register.yml:126
CONSEQUENCE: An activation fault with no rollback leaves a half-activated trust root (the PROHIBITED 'trust activation before rollback exists' state); presence-only approval means any string authorizes it, and a self-widening activation certifies itself.
REC: Require operator-preauthorized, previous-version-verified, exact-version activation records; validate the approval ARTIFACT (not its presence); every stage fault must deterministically end at old-healthy or new-healthy or auto-rollback-to-old.
TEST: For each stage fault → final state is one of {old healthy, new healthy, rolled-back-to-old healthy}, never half-activated; a self-widening activation FAILS. PASS iff every fault terminates in a healthy known version and no self-widening succeeds.
FDs: FD-018,FD-017

### [proof-of-architecture|PHASE2_PLAN] L3-P1-PROOF-OF-ARCHITECTURE  (proof-of-architecture; severity high)
CLAIM: Minimum executable end-to-end proof-of-architecture chain (requires PP-01,02,03,04,05,06,07 per proposal-program-packet-map.yml:166). Sequence: operator intent → digest-bound harness factory → authorize_execution grant → ONE-SHOT ExecutorLaunch verified at the spawn (extends verify_authorized_effect/effects.rs:210 to the codex.rs:254 spawn) → credentialless seatbelt-sandboxed candidate in a disposable worktree → agent edits/commits in isolation → brokered exact-SHA branch-no-PR push (lifecycle_program.rs:19499 today) refused on target-race → transactional reserve→consume→terminal in SQLite → clean reconcile on a mid-flight restart.
REFS: effects.rs:210 ; codex.rs:254 ; kernel/src/lifecycle_program.rs:19499 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:165
CONSEQUENCE: This chain is the single artifact that proves 'exactly one unavoidable boundary between agent-wants-effect and effect-happens.' If any link is stubbed, the boundary claim is unproven. Explicit NON-GOALS: it does NOT prove federation/multi-project, distributed consensus, signing-key management, or Linux sandbox parity — those are outside the cheap solo-proof envelope.
REC: Run the full chain as one integration test that must be GREEN before proof-of-architecture is declared; layer F1 (concurrent consume), F5 (target-race), F3 (reconcile), F9 (cred-scrub) into it so the boundary is proven under fault, not just on the happy path.
TEST: One integration run: spawn is DENIED without a verified ExecutorLaunch; with it, the candidate is credentialless+sandboxed, the ONLY durable effect is the brokered exact-SHA push, a target-race is refused, and a mid-flight kill reconciles clean (no double effect). PASS iff every fault sub-assertion (F1/F3/F5/F9) holds inside the chain.
FDs: FD-003,FD-004,FD-005,FD-006,FD-008,FD-010,FD-011,FD-012

### [residual-risk|TARGET_DESIGN] L3-R1-RESIDUAL-EVIDENCE-AUTHENTICITY  (residual-risk; severity medium)
CLAIM: FD-014 real tamper-evidence CANNOT be proven cheaply by a solo builder locally. The in-repo hash chain is recomputable by any writer (E-01/E-02); true immutability rests on GitHub's server-side ref-log, which the operator cannot unit-test. Option A (ed25519/HMAC signing, PP-07) introduces key-management the solo builder must secure; Option B (reword to 'git-anchored', PP-00/GAP-17) must precede any 'signed' claim.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-1/final-decision-to-repository-crosswalk.yml:157 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-gap-register.yml:90
CONSEQUENCE: Claiming 'signed/tamper-evident' before Option A lands, or without acknowledging the git-anchor dependency, is a support-claim overstatement (the PROHIBITED 'claims stronger than proof' state). Phase 3 must state this residual explicitly rather than imply the fault suite covers it.
REC: Do PP-00 rewording first; if Option A is chosen, the test is 'rewritten log fails signature verify with the broker key' and the residual (key custody) is documented; if Option B, the test is 'quorum rejects same-actor/forged attestation' and authenticity is explicitly delegated to git/GitHub history.
TEST: Either (A) a rewritten event log FAILS signature verification, or (B) every sign/attest claim is reworded to git-anchored AND quorum rejects a same-actor/forged attestation. PASS iff no OVERSTATED claim survives at the promotion point.
FDs: FD-014,FD-015

### [residual-risk|TARGET_DESIGN] L3-R2-RESIDUAL-SANDBOX-EXISTENTIAL  (residual-risk; severity medium)
CLAIM: The sandbox-escape suite (F10) and the out-of-tree-verifier immutability (F8/GAP-09) are existential/deployment claims a solo builder CANNOT fully discharge. F10 proves denial of the paths TESTED, never absence of ALL escapes (a negative existential). The out-of-tree verifier is only as immutable as the operator's GitHub App / protected-repo configuration (an ARCHITECTURAL_INFERENCE, not a local test). Near-full-disk (F7) is DEPLOYMENT_LOCAL — it proves the code reserves capacity, not the operator's real disk.
REFS: codex.rs:254 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-gap-register.yml:81 ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-architecture.md:190
CONSEQUENCE: If Phase 3 presents F8/F10 as full proofs, it overstates assurance. The honest posture: the fault suite raises the cost of the tested attacks and proves the code-level control, but seatbelt-completeness and out-of-tree immutability carry standing MEDIUM residual risk the operator must accept.
REC: State the residual explicitly in Phase 3: F10 is a regression suite of KNOWN escape classes (extend over time), not a proof of unescapability; F8/GAP-09 immutability is CONFIGURATION_DERIVED and depends on the operator's App/protected-repo setup being correct; F7 is DEPLOYMENT_LOCAL.
TEST: Phase 3 documents, for F7/F8/F10, an explicit residual-risk statement and the evidence class it can actually achieve (DEPLOYMENT_LOCAL / CONFIGURATION_DERIVED / regression-suite), and does not label them proven-complete. PASS iff each carries its honest residual.
FDs: FD-008,FD-011,FD-013

### [usability|PHASE2_PLAN] L4-01  (high — UNMET by the plan (broker lifecycle/recovery undesign)
CLAIM: The single biggest usability risk: the net-new local broker (OD-05 / PP-04 / GAP-04) is a long-running credential-holding process with NO described process-lifecycle, supervision, or crash-recovery story, and no such daemon pattern exists in the runtime today, so the solo operator must babysit a second process that can die mid-mission.
REFS: grep for UnixListener/launchd/LaunchAgent/daemon across *.rs returned zero matches (STATICALLY_INSPECTED) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/operator-decisions-still-required.yml:60 (OD-05 names IPC+enrollment but not lifecycle) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:63 (PP-04 scope: broker process, no supervision clause) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-architecture.md:97 (broker as separate process)
CONSEQUENCE: If the broker crashes during an unattended/overnight run, every durable effect (push/land/merge/deploy) fails closed with no auto-restart, so the operator returns to a stalled mission and must manually diagnose and restart a process the current file-native system never required — turning 'run a mission' into 'keep a daemon alive.'
REC: Ship the broker as a supervised launchd LaunchAgent (or equivalent) with health-gated auto-restart and an explicit fail-closed-on-restart posture, and define an `octon doctor` broker-health check that reports and can re-enroll in one command. Fold this supervision + doctor into PP-04 rather than deferring the operator surface to PP-12.
TEST: Kill the broker process mid-mission; a supervisor auto-restarts it within 5s, the in-flight effect is reconciled (not duplicated, not lost) on restart, and `octon doctor` reports broker=healthy with zero manual operator steps.
FDs: FD-006,FD-024,FD-001

### [usability|PHASE2_PLAN] L4-02  (high — PARTIAL/DEFERRED (usability layer arrives last))
CLAIM: All hard usability guarantees — fresh-install-to-first-mission bound, zero routine prompts, operator doctor, cross-project inbox, federation-off — are bundled into GAP-18/PP-12, the LAST packet, sitting behind PP-07 (signing) and PP-08 (trust activation), so the operator faces the full added complexity of six net-new subsystems before any usability affordance exists.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/migration-gap-register.yml:162 (GAP-18 owns all usability, owning_packet PP-12) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:152 (PP-12 depends_on PP-07,PP-08,PP-09,PP-10,PP-11) ; .octon/inputs/additive/.incoming/octon-architecture-and-migration-handoff-v2.0.0/decisions/proof-obligations-and-unresolved.yml:22 (PROOF-USABILITY: zero prompts + solo budgets)
CONSEQUENCE: During the entire proof-of-architecture phase (PP-01..PP-07) the operator runs a system with a broker, isolation, SQLite store, out-of-tree verifier and inert trust landing but NO doctor, NO inbox, and NO trimmed gates — the maximum-friction intermediate state — which is exactly when a solo builder is most likely to abandon the tool.
REC: Split PP-12: pull `octon doctor`, broker/store/credential health, and the FD-002 zero-prompt crosswalk forward to land alongside PP-04, and keep only cross-project inbox + gate-trimming as the tail. Make 'operator can diagnose broker/store/credential health in one command' an exit gate of PP-04, not PP-12.
TEST: At the exit of PP-04, `octon doctor` exists and reports broker + store + credential + mission health, and a routine reversible T1 change completes end-to-end with zero human prompts — both verifiable before PP-07/PP-08 land.
FDs: FD-001,FD-002,FD-024

### [usability|PHASE2_PLAN] L4-03  (medium — PARTIAL (policy substrate supports zero-prompt, but)
CLAIM: OD-07's 'verifier-as-second-quorum-member removes the prompt' is only PARTIALLY delivered: the existing mission-autonomy policy already permits a machine second party (ACP-2 = execution_agent + independent verifier/deterministic validator), but the plan does not delete the surviving human-approval defaults — the `default` quorum policy still requires 1 explicit operator approval for approval-bearing routes, and the shipped risk-tiers doc ends every tier at a human 'Approve PR' click.
REFS: .octon/instance/governance/policies/mission-autonomy.yml:147 (ACP-2: execution_agent_plus_independent_verifier_or_deterministic_validator — machine quorum reachable) ; .octon/instance/governance/contracts/quorum-policies/default.yml:6 (default quorum required:1 explicit operator approval) ; .octon/instance/cognition/context/shared/risk-tiers.md:62 (T1 ends in 'Approve (click button)'; T2/T3 likewise human-approve) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/operator-decisions-still-required.yml:83 (OD-07 asserts zero human prompts)
CONSEQUENCE: If the plan maps FD-002 onto ACP-2 but leaves the `default` approval-bearing-route requirement and the tier-based 'Approve PR' step in place, the operator still gets a routine prompt on ordinary commits, contradicting the PROOF-USABILITY 'zero prompts' obligation — the prompt is removed in principle but not in the actual approval path.
REC: In PP-12 (or the pulled-forward crosswalk), explicitly rewire the reversible-isolated-branch route so ACP-2 is satisfied by the deterministic SHA verifier as the second party AND remove the residual `default` operator-approval requirement and the tier 'Approve PR' click for that route class; keep human approval only for ACP-3+/destructive.
TEST: Run 20 consecutive routine reversible T1/T2 missions on isolated branches with the verifier active; measured human prompt count = 0, and a destructive/ACP-3 mission still prompts exactly once.
FDs: FD-002

### [usability|TARGET_DESIGN] L4-04  (medium — PARTIAL (overnight autonomy exists today but new cr)
CLAIM: Safe overnight/unattended work partially REGRESSES under the target: the existing proceed_on_silence model already forbids `credential_change` for silent operation, so the new broker's credential enrollment (OD-05) and any signing-key rotation (OD-01 option A) cannot run unattended, blocking first-run and rotation events during autonomous windows.
REFS: .octon/instance/governance/policies/mission-autonomy.yml:97 (proceed_on_silence.forbidden_action_classes includes credential_change, identity_change) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/operator-decisions-still-required.yml:11 (OD-01 signing adds key custody/rotation) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/operator-decisions-still-required.yml:60 (OD-05 key enrollment)
CONSEQUENCE: A solo operator who starts a mission overnight can hit a credential-enrollment or key-rotation step that the autonomy policy correctly refuses to do silently, so the run stalls until the operator is present — and if signing (OD-01 A) is chosen, recurring rotation becomes a recurring overnight-blocking event, adding maintenance burden the git-anchored option (OD-01 B) avoids.
REC: Prefer OD-01 option B (git-anchored, no key rotation) for the solo vertical to eliminate recurring rotation burden, and make broker credential enrollment a strictly one-time, operator-present, out-of-band step (never on the mission critical path) so unattended runs never encounter a credential_change action.
TEST: Enroll credentials once with the operator present in ≤1 command; then run 3 unattended overnight missions with no credential_change or identity_change action reached, and confirm no rotation prompt occurs within the operator's chosen budget window.
FDs: FD-001,FD-006,FD-014

### [usability|PHASE2_PLAN] L4-05  (medium — UNMET (no maintenance-budget target; trim deferred))
CLAIM: Low monthly maintenance burden is UNMET/unquantified: the plan defers CI-gate trimming (OD-09/GAP-18) to PP-12 and gives no burden budget, while the measured baseline is 42 workflows, 42,257 tracked state files, and 167 state-touching commits in the last 30 days — a churn/upkeep surface characteristic of a platform, not a one-person tool.
REFS: git ls-files .octon count = 53,070; .octon/state+.octon/generated tracked = 42,257 (STATICALLY_INSPECTED) ; git log --since=30d --oneline .octon/state = 167 commits (STATICALLY_INSPECTED) ; .github/workflows count = 42 (STATICALLY_INSPECTED) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/operator-decisions-still-required.yml:106 (OD-09 gate-trim deferred, operator-decided)
CONSEQUENCE: Without a target maintenance budget, the operator inherits ~167 state commits/month of churn, 42 workflows to keep green, and (if OD-01 A) key rotation and quarantine upkeep — so the migrated design can be architecturally correct yet still fail the solo-usability objective because routine upkeep exceeds a one-person budget.
REC: Set an explicit solo maintenance budget as a PP-12 promotion gate (e.g. ≤N minutes/week operator upkeep, ≤M load-bearing workflows for the solo path) and require OD-09 to name which of the 42 gates back a Phase-3 acceptance test vs. which are quarantined behind self-dev/enterprise flags before the vertical is declared usable.
TEST: A defined solo maintenance budget exists and is met: measured operator upkeep over a 4-week dogfood window stays under the budget, and the count of workflows the solo operator must keep green is reduced to the named load-bearing set with the rest quarantined.
FDs: FD-001,FD-024

### [usability|PHASE2_PLAN] L4-06  (medium — UNMET (enrollment UX recommended, not gated))
CLAIM: One-command guided trust enrollment with no manual secret copying (FD-001) is UNMET at the plan level: OD-05 only recommends a mechanism (unix socket + keychain, guided enrollment) but the packet map's PP-04 scope does not carry an enrollment acceptance target, so 'no manual secret copying' is aspirational rather than gated.
REFS: .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/operator-decisions-still-required.yml:65 (OD-05 recommendation only) ; .octon/inputs/exploratory/reviews/architecture-migration/architecture-migration-review-20260712T015106Z-57f6e8/phase-2/proposal-program-packet-map.yml:71 (PP-04 promotion_gate is effect-denial, not enrollment UX) ; .octon/instance/governance/policies/connector-credentials.yml (existing external-credential model the broker interposes on) (STATICALLY_INSPECTED)
CONSEQUENCE: PP-04 can pass its promotion gate (raw effect fails, brokered effect succeeds) while still requiring the operator to hand-copy tokens into the keychain or hand-configure IPC, so the enrollment burden the solo objective was meant to remove survives unmeasured.
REC: Add an explicit enrollment-UX acceptance gate to PP-04: fresh install to a broker holding usable credentials in a single guided `octon enroll` command that reads from the existing gh keychain / connector-credentials model with zero manual secret paste, and count the operator steps.
TEST: On a clean machine, `octon enroll` brings the broker to credential-ready in exactly one command with zero manual secret copy/paste, and the operator step count from clone to first brokered push is recorded and bounded.
FDs: FD-001,FD-006,FD-024
