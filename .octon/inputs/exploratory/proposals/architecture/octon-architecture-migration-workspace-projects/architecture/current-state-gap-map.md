# Current-State Gap Map

## Repository Baseline

The reconciliation inspected commit
`c5b1f5760c78ff521cca6b054e4e8fef5300505b`. The current repository has no
material change to the surfaces below.

| Current surface | Reusable strength | Gap owned by RP-10 |
| --- | --- | --- |
| `.octon/instance/locality/project-profile.yml` | Evidence-backed repository facts and validation strategy | Singleton, path-bearing record does not provide durable identity for multiple logical projects. |
| Runtime and constitutional `project-profile-v1` schemas | Established Profile contract and mirror | No Workspace Project identity/boundary contract or explicit whitelist for fields entering later compilation. |
| Kernel engagement command | Creates Profile source facts and the active Profile | Hard-coded singleton path; no project registry, correction precedence, or two-project adoption. |
| Kernel mission commands | Mission authority, status, queue, resume, and continuity exist | Commands address one mission ID at a time; no project-tagged cross-project inbox. |
| `.octon/state/continuity/repo/missions/**` | Durable non-authoritative mission continuity | Project association is not a complete multi-project selection/index model. |
| Generated mission projections | Readable non-authoritative views | No deterministic cross-project inbox contract; projections must not become authority. |
| Task Harness and route primitives | Strong downstream compilation inputs | RP-11 cannot bind an exact project revision/Profile pair because RP-10 has not created one. |

## Accepted Finding

RF-015 rejects both extremes: the current global route/Profile primitives do
not complete Workspace Projects, while a project layer is not speculative
portfolio administration. The smallest accepted repair is a strict project
record plus a deterministic per-run consumer in RP-11.

## Gap-to-Owner Map

| Gap | Owner | Not owned here |
| --- | --- | --- |
| Stable project identity and strict record | RP-10 | Repository/provider identity issuance mechanisms outside the declared project contract |
| Project inference, refresh, correction, relocation | RP-10 | Canonical authorization and durable effect execution |
| Profile binding and field whitelist | RP-10 | Harness compilation logic |
| Active-run project snapshot | RP-10 produces exact refs/digests | RP-11 binds them into the Harness; RP-01 owns authorization semantics |
| Cross-project mission inbox | RP-10 read model | Mission authority, scheduling, or control transitions |
| Full-input deterministic Harness | RP-11 | No RP-11 source is absorbed into RP-10 |

## Evidence Honesty

The current facts are statically inspected. No two-project run, relocation,
operator-correction race, or project-metadata authority attack has been
dynamically executed. Those remain planned evidence, not present proof.
