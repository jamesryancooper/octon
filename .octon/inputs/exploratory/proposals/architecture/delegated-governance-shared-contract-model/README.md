# Delegated Governance Shared Contract Model

_Status: Accepted child architecture proposal_

This child defines the shared proof-first contract semantics downstream domain
children will apply to authority, runtime, connector, read-model, workflow, and
validator surfaces.
