# Packet Worktree Partitioning Automation

This P1 child packet defines proposal-packet worktree partitioning for
publishable evidence, cleanup-safe residue, protected retained evidence, and
manual-review paths.

The packet is proposal-local input only and does not authorize deletion,
cleanup, parent closeout, or a `cleaned` claim.
