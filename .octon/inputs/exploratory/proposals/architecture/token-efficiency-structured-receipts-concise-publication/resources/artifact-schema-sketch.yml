schema_version: octon-child-artifact-schema-sketch-v1
child_id: token-efficiency-structured-receipts-concise-publication
artifacts:
- name: closeout-projection.yml
  purpose: Concise closeout view backed by full closeout receipt and evidence refs.
  authority_status: non-authoritative evidence/read-model unless implemented as durable spec/template; never replaces source
    of truth
  required_fields:
  - schema_version
  - source_refs
  - source_digests
  - producer
  - consumer
  - validation
  - evidence_refs
  - failure_behavior
  failure_behavior: fail closed on missing source, digest mismatch, stale freshness, or authority-boundary violation
- name: publication-summary.yml
  purpose: Compact publication artifact with refs to retained evidence and on-demand expanded report handle.
  authority_status: non-authoritative evidence/read-model unless implemented as durable spec/template; never replaces source
    of truth
  required_fields:
  - schema_version
  - source_refs
  - source_digests
  - producer
  - consumer
  - validation
  - evidence_refs
  - failure_behavior
  failure_behavior: fail closed on missing source, digest mismatch, stale freshness, or authority-boundary violation
- name: structured-receipt.yml
  purpose: structured-receipt.yml compact artifact for this child
  authority_status: non-authoritative evidence/read-model unless implemented as durable spec/template; never replaces source
    of truth
  required_fields:
  - schema_version
  - source_refs
  - source_digests
  - producer
  - consumer
  - validation
  - evidence_refs
  - failure_behavior
  failure_behavior: fail closed on missing source, digest mismatch, stale freshness, or authority-boundary violation
- name: expanded-report-request.yml
  purpose: expanded-report-request.yml compact artifact for this child
  authority_status: non-authoritative evidence/read-model unless implemented as durable spec/template; never replaces source
    of truth
  required_fields:
  - schema_version
  - source_refs
  - source_digests
  - producer
  - consumer
  - validation
  - evidence_refs
  - failure_behavior
  failure_behavior: fail closed on missing source, digest mismatch, stale freshness, or authority-boundary violation
