# Implementation Plan

1. Validate the exact selected protocol receipt and six accepted packet
   digests. This authorizes protocol implementation only; it claims no proof.
2. Before proof-source entry, require RP-08/RP-09/RP-10/RP-11 exact
   implementation, conformance, drift, and promotion receipts at one commit,
   the optional RP-12/RP-13 receipts for their claim lanes, a current writer
   census, clean read-only proof subjects, and frozen current policy, Harness,
   provider, support-tuple, environment, retention, and corpus identities.
3. Allocate the generation ordinal by expected-old index digest and derive the
   protocol, run, corpus, environment, provider, and mission-attempt identities.
4. Run fresh setup and two-project onboarding; execute the exact ten-spec,
   twenty-attempt paired/counterbalanced equal-floor corpus and ten separately
   identified supervised baselines with no warmup exclusion or retry.
5. Run every route and system fault case and independently reproduce admitted
   primary-provider generic and specialized mappings against start/end snapshots.
6. Operate thirty consecutive 24-hour burden intervals and compute the exact
   duration, nearest-rank percentile, autonomy, human-time, setup, burden, route,
   recovery, preservation, custody, and zero-budget predicates.
7. Challenge false positives, selective omission, stale evidence, producer
   self-attestation, optional-claim leakage, unsafe fallback, history mismatch,
   candidate credentials/head execution, CAS races, `S -> Q`, mirror ordering,
   cleanup races, and lost-work claims.
8. Write normalized bounded evidence, verify the sorted manifest and secret
   scan, commit `generation-complete.yml` and then the generation index, and
   adjudicate each claim by all-of state rules. Route failures to owners and
   emit only a non-authoritative exact-field handoff for separate promotion.
9. Require UE/FD, conformance, drift, and all fifteen child terminal results
   before proof/program completion. Never treat these future outputs as proof
   that the accepted protocol is complete today.
