# Proposal Reading and Precedence Map

Current constitutional and durable repository authority outranks this packet.
`proposal.yml` owns packet identity/lifecycle; `resources/packet-contract.yml`
owns the proof boundary; architecture documents own planned methods and gates;
`resources/solo-dogfood-proof-protocol-receipt.yml` owns the exact selected
proof identity, corpus, measurement, custody, claim-state, and evidence-order
mechanisms; `resources/traceability.yml` maps reconciled IDs. The artifact
catalog and proposal registry are projections. Parent summaries never satisfy
RP-14 proof, and RP-14 summaries never satisfy child receipts or authorize
claims.

The RP-14 evidence-local protocol solely owns equal-floor comparison and the
claim map. RP-05–RP-08 retain implementation semantics. Any CAS, route,
verification, PR, evidence, recovery, mirror, cleanup, or preservation failure
returns to that owner; RP-14 never repairs the proof subject.
