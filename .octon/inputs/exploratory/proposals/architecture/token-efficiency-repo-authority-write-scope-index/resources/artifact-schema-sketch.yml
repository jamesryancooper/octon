schema_version: octon-child-artifact-schema-sketch-v1
child_id: token-efficiency-repo-authority-write-scope-index
artifacts:
- name: repo-authority-graph.yml
  purpose: repo-authority-graph.yml compact artifact for this child
  authority_status: non-authoritative evidence/read-model unless implemented as durable spec/template; never replaces source
    of truth
  required_fields:
  - schema_version
  - source_refs
  - source_digests
  - producer
  - consumer
  - validation
  - evidence_refs
  - failure_behavior
  failure_behavior: fail closed on missing source, digest mismatch, stale freshness, or authority-boundary violation
- name: promotion-target-index.yml
  purpose: promotion-target-index.yml compact artifact for this child
  authority_status: non-authoritative evidence/read-model unless implemented as durable spec/template; never replaces source
    of truth
  required_fields:
  - schema_version
  - source_refs
  - source_digests
  - producer
  - consumer
  - validation
  - evidence_refs
  - failure_behavior
  failure_behavior: fail closed on missing source, digest mismatch, stale freshness, or authority-boundary violation
- name: write-scope-index.yml
  purpose: write-scope-index.yml compact artifact for this child
  authority_status: non-authoritative evidence/read-model unless implemented as durable spec/template; never replaces source
    of truth
  required_fields:
  - schema_version
  - source_refs
  - source_digests
  - producer
  - consumer
  - validation
  - evidence_refs
  - failure_behavior
  failure_behavior: fail closed on missing source, digest mismatch, stale freshness, or authority-boundary violation
