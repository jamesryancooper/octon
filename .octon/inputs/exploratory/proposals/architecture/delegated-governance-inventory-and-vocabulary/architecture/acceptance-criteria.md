# Acceptance Criteria

- Inventory covers authority engine, mission/runtime, connectors, run-health,
  read models, workflows, capabilities, validators, docs, and lifecycle
  reference behavior.
- Every surface has one classification.
- Generic importance is not accepted as a human-only reason.
- Generated outputs and read models are explicitly non-authoritative.
- Downstream contract work has no unresolved vocabulary dependency.
