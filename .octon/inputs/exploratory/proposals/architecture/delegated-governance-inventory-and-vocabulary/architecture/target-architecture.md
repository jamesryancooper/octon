# Target Architecture

Octon has one explicit inventory of delegated governance surfaces. Each surface
is classified as delegated execution, typed human exception, deny-only,
projection-only, or needs-more-evidence.

The inventory distinguishes authority from observation. Generated outputs,
read-model states, dashboards, proposal-local receipts, and agent output can
support evidence but cannot grant authority.
