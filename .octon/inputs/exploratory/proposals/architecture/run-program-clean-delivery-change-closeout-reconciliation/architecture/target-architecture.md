# Target Architecture

Change closeout becomes the single governed reconciliation path for terminal branch publication and landing evidence. A completed clean-delivery run records whether the branch was only published, merged, synced into local main, cleaned up, or already landed, and each state is validated against the default work unit policy and Change receipt schema.

The proposal-program wrapper may cite Change closeout evidence, but it cannot replace it. Host observations remain evidence inputs, not authority.
