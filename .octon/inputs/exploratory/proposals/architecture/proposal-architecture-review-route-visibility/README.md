# Architecture Review Route Visibility

This child packet proposes making strict pre-integration architecture review visible in proposal-program route graph and lifecycle status output whenever a child architecture proposal requires it.

The implementation should surface missing, stale, failing, and passing architecture-review receipt states without changing the receipt authority model.
