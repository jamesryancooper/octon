# Source Prompt

The operator requested a new architecture proposal packet for a first-class
packet-level delivery wrapper and a revision to the existing
`proposal-lifecycle-closeout-friction-remediation` packet so ownership,
dependency, and promotion-target scope are clear.

The requested packet target is:

```text
.octon/inputs/exploratory/proposals/architecture/proposal-packet-delivery-wrapper
```

The proposed wrapper must let an operator invoke one packet delivery route
instead of pasting a full orchestration prompt. It must cover packet
implementation, terminal closeout, archive, branch-no-pr landing, branch
cleanup, final sync, terminal proof, and clean worktree proof.
