schema_version: "architecture-proposal-v1"
architecture_scope: "root repository Git policy, private-remote cutover, workspace release workflow, ownership metadata, host projection tracking, and local-first ignore enforcement"
decision_type: "boundary-change"
decision_summary: "Adopt a private-workspace root policy that prevents public remote targeting, removes unsafe release behavior, validates ownership metadata, keeps host projections regenerable and local by default, and prepares ignore rules for a separate .octon storage untracking packet."
affected_surfaces:
  - ".gitignore"
  - "workspace GitHub workflows"
  - "CODEOWNERS"
  - "release configuration"
  - "host projection roots"
  - ".githooks/ pre-push public-remote guard"
  - "known-writer private-remote cutover and stale-name rejection"
  - "state, generated, and input-subtype ignore rules"
non_authority_statement: "This packet defines a proposed implementation boundary only. It does not authorize implementation, external effects, publication, legal conclusions, or risk acceptance."
