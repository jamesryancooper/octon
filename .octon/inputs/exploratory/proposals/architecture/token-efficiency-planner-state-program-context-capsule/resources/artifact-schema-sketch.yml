schema_version: octon-child-artifact-schema-sketch-v1
child_id: token-efficiency-planner-state-program-context-capsule
artifacts:
- name: planner-state.yml
  purpose: Compact current planner state derived from checkpoints, event heads, child status, and blocker refs.
  authority_status: non-authoritative evidence/read-model unless implemented as durable spec/template; never replaces source
    of truth
  required_fields:
  - schema_version
  - source_refs
  - source_digests
  - producer
  - consumer
  - validation
  - evidence_refs
  - failure_behavior
  failure_behavior: fail closed on missing source, digest mismatch, stale freshness, or authority-boundary violation
- name: program-context-capsule.yml
  purpose: Parent program context handoff with child status table, runnable batch, blockers, route decision, digests, and
    evidence refs.
  authority_status: non-authoritative evidence/read-model unless implemented as durable spec/template; never replaces source
    of truth
  required_fields:
  - schema_version
  - source_refs
  - source_digests
  - producer
  - consumer
  - validation
  - evidence_refs
  - failure_behavior
  failure_behavior: fail closed on missing source, digest mismatch, stale freshness, or authority-boundary violation
- name: compact-completion-capsule.yml
  purpose: compact-completion-capsule.yml compact artifact for this child
  authority_status: non-authoritative evidence/read-model unless implemented as durable spec/template; never replaces source
    of truth
  required_fields:
  - schema_version
  - source_refs
  - source_digests
  - producer
  - consumer
  - validation
  - evidence_refs
  - failure_behavior
  failure_behavior: fail closed on missing source, digest mismatch, stale freshness, or authority-boundary violation
