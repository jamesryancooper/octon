# Current-State Gap Map

checked_at: 2026-06-01T01:50:30Z
source_review_id: proposal-program-runner-terminal-gap-map-review-20260601T014131Z

## Classification Legend

- fixed: live repo evidence satisfies the parent postmortem requirement.
- partial: live repo evidence covers part of the requirement but leaves
  downstream implementation work.
- open: live repo evidence does not yet satisfy the requirement.
- outside-runner: the requirement belongs to a route, workflow, script, or
  operator surface outside runner ownership.

## Gap Matrix

| Gap | Source Requirement | Current Classification | Live Evidence | Downstream Owner | Required Change Or No-Op Rationale | Validation Expectation |
| --- | --- | --- | --- | --- | --- | --- |
| G-001 | Retry archive/promote workflow routes without duplicate canonical workflow run ids. | open | `lifecycle_executor/src/workflow_leaf.rs` derives `workflow_run_id` as `<run_id>-workflow`; `lifecycle_program.rs` builds child run ids as `<program_run_id>-<child_id>` and only records `retry_attempt` inside the request policy. | `proposal-program-runner-workflow-retry-ids` | Add attempt-suffixed or dispatch-unique workflow run ids for fresh attempts, while preserving explicit same-run resume only through replay proof. | A retry fixture attempts the same child workflow route twice and proves distinct workflow run ids or a blocked replay-safe resume decision. |
| G-002 | Resume an existing workflow run only with same-input, same-authority, same-target, replay-safe proof. | partial | `lifecycle_program.rs` has `unsafe-resume` blockers and recovery `replay-verify`; `proposal-program.contract.yml` declares replay validation for recovery recipes. No workflow leaf resume proof currently binds same-input, same-authority, and same-target before reusing a workflow run id. | `proposal-program-runner-workflow-retry-ids` | Add a replay-safe resume gate or make non-proven resume fail closed before workflow dispatch. | Tests cover safe resume, unsafe resume, target drift, authority drift, input drift, and missing replay evidence. |
| G-003 | Declare non-authorizing Change/worktree handoff checkpoints after mutating child implementation or child batches. | partial | `lifecycle.contract.yml` defines non-authorizing handoff interaction profiles; `lifecycle_program.rs` runs closeout worktree hygiene preflight and blocks `artifact-ownership-unclear`; closeout skills remain separate. | `proposal-program-runner-change-handoff-checkpoints` | Add explicit controller checkpoint evidence for route-owned `closeout-change` or `closeout-worktree` handoff after mutating child batches without giving the runner Git or cleanup authority. | Contract and runner tests prove handoff evidence exists, is non-authorizing, and cannot satisfy closeout or cleanup completion. |
| G-004 | Aggregate child terminal blockers should be parent controller evidence and must not replace child-owned receipts. | partial | `lifecycle_program.rs` emits `program_blockers`, `child_states`, `normalized_child_blockers`, `recovery-log.yml`, scheduler decisions, and aggregate closeout receipts. `proposal-program.contract.yml` requires child receipts and aggregate evidence for closeout. | `proposal-program-runner-aggregate-terminal-blockers` | Keep the existing parent evidence flow and add any missing machine-readable terminal blocker ledger fields needed by the parent closeout and operator view. | Tests prove parent blocker summaries do not synthesize child receipts, terminal outcomes, archive metadata, or validation verdicts. |
| G-005 | Bind promotion evidence to selected child identity and receipt lineage before workflow-owned `promote-proposal` dispatch. | partial | `lifecycle_program.rs` writes delegated promotion evidence with `child_id`, registry digest, write-scope digest, authority-zone decision, receipt verdicts, and receipt digests. `promote-proposal` remains workflow-owned. | `proposal-program-runner-promotion-evidence-binding` | Verify the workflow input `promotion_evidence` is derived from or checked against the selected child receipt lineage before dispatch, not merely present as a route input. | Tests prove mismatched child id, stale receipt digest, missing write-scope digest, or unrelated promotion evidence blocks promotion dispatch. |
| G-006 | Classify generated-state freshness before workflow dispatch where possible and route to canonical publication recovery. | partial | `proposal-program.contract.yml` declares `publication-drift` recovery through `refresh-publication-projections`; `lifecycle_program.rs` detects publication drift only from failed child receipts or no-progress post-validation content. Generated/effective locks are digest-bound and non-authority. | `proposal-program-runner-publication-freshness-preflight` | Add a pre-dispatch freshness check where stale generated/effective state is knowable before route execution, and route recovery to canonical publication scripts or the declared recovery action. | Tests prove stale route bundles, extension catalogs, capability routing, and generated proposal registry drift are classified before dispatch when detectable. |
| G-007 | Parent review should not churn because volatile run-control or route-created evidence changed outside the reviewed parent artifact surface. | partial | `validate-proposal-review-gate.sh` computes a packet digest from reviewed packet files and excludes review receipts, revision receipts, generated implementation prompts, implementation runs, conformance/drift receipts, closeout receipts, and validation scratch. It does not include `.octon/state/**`. | `proposal-program-runner-parent-review-churn` | Preserve the digest boundary and add focused tests or docs for parent program artifacts that are intentionally excluded from review freshness. | Review-gate tests mutate volatile run-control and route-created evidence and prove accepted parent review freshness does not change. |
| G-008 | Archive workflow routing observes terminal outcomes after active-path moves or fails closed with blocked archive evidence. | fixed | `lifecycle_executor/src/observer.rs` resolves archived target paths for `archive-proposal` when the active target moved; `archive_completion_observes_archived_target_after_active_target_moves` covers this behavior. | `proposal-program-runner-archive-observation-recovery` | No current runner mutation is required for basic archive target observation; downstream work may add blocked archive evidence shape if missing. | Keep archive observation tests and add fail-closed blocked archive evidence coverage if the downstream packet finds a missing receipt shape. |
| G-009 | Tests and fixtures must prove the terminal routing failure pattern cannot recur silently. | partial | Existing tests cover program route handoff, aggregate closeout, archive observation, recovery recipes, worktree hygiene, and no-progress handling. No evidence found for attempt-suffixed workflow run id collision coverage. | `proposal-program-runner-terminal-routing-tests` | Add a terminal routing regression suite after behavior children land, with duplicate workflow id as a required negative control. | Tests cover every G-001 through G-008 requirement and include the original duplicate workflow id archive retry failure. |

## Downstream Handoff

Downstream children must treat `open` and `partial` classifications as live
starting evidence, not as acceptance criteria already satisfied. A child may
close a gap only by citing current repo evidence, retained validation evidence,
and route-owned receipts for its own packet.
