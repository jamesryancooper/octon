# Proposal Program Runner Change Handoff Checkpoints

Introduce route-owned Change/worktree handoff checkpoints so terminal residue
does not accumulate until parent closeout, while preserving runner
orchestration boundaries.
