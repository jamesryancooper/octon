# Target Architecture

Cleanup for clean delivery starts with residue classification. The classifier partitions changes into owned, foreign, generated, protected, disposable, and preserve-only categories and records a fingerprint that closeout-worktree can bind to a return report.

Clean delivery may proceed only when residue is removed through an authorized route or explicitly preserved by validated disposition evidence. A repeated cleanup preflight blocker must display the blocker evidence and stop blind cycling.
