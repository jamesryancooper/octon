# Program Collision-Ledger Reconciliation Receipt

revision_id: octon-architecture-migration-program-collision-ledger-20260717T215508Z
source_review_id: 20260717-architecture-migration-rp00-candidate-retry-leased-0eea044a
source_review_basis: operator-authorized superseding lifecycle blocker
revision_timestamp: 2026-07-17T21:55:08Z
status: in-review
change_profile: atomic
release_state: pre-1.0
transitional_exception_note: not-required
post_revision_digest: sha256:e0c8cd8f966fd2ef3be84ccbc89842fa7c27ae5fb0a9727ef988cc9f7146dabd
remaining_blocking_count: 0
child_authority_preserved: true
provider_scope_changed: false
credential_scope_changed: false
git_or_github_effect_authorized: false

changed_parent_files:

- `proposal.yml`
- `navigation/artifact-catalog.md`
- `resources/child-packet-index.yml`

changed_packet_files:

- `proposal.yml`
- `navigation/artifact-catalog.md`
- `resources/child-packet-index.yml`

addressed_finding_ids:

- `INVALID-CHILD-REGISTRY-WRITE-SCOPE-COLLISION-LEDGER-001`

validators_rerun:

- `validate-proposal-program-structure.sh`: errors=0 warnings=0
- `test-validate-proposal-program-structure.sh`: passed=32 failed=0
- `cargo test -p octon_kernel collision_ledger`: passed=4 failed=0
- child-registry JSON Schema validation: passed
- `validate-proposal-standard.sh --skip-registry-check`: errors=0 warnings=1
- `validate-proposal-review-gate.sh`: errors=0 warnings=0; review awaited
- `validate-proposal-review-gate.sh --print-digest`: passed
- `git diff --check`: passed

catalog_checksum_registry_refresh: artifact catalog refreshed for this revision receipt; no packet checksum registry exists

## Profile Selection Receipt

The release is `pre-1.0` and the selected profile is `atomic`. The correction
has one parent-owned target state: a complete typed collision ledger over the
existing child/dependency/write-scope projection. No transitional coexistence,
compatibility path, child edit, runtime edit, or provider effect is required.
The untouched candidate commit
`0eea044aed9ca3d2a8913ac2191a8f2f50cb50b7` is the rollback handle.

## Reconciliation

The authorized lifecycle retry stopped before adapter dispatch because the
candidate registry derived 120 collisions but omitted
`write_scope_collision_ledger`. The correction projects the existing reviewed
parent ledger onto the candidate registry rather than inventing a second
ledger or new collision policy.

All 120 candidate collisions have exact antecedents in the preserved reviewed
129-record ledger. The resulting projection contains 115 exact collisions and
five directory-prefix collisions over 409 scope entries and 337 unique paths.
Its canonical registry projection digest is
`sha256:4e572b846a9df01180c32eb252d890a6da438be1edd2deaf3545f0c564fcb835`.

Six reviewed serialization records required reconciliation to the older
candidate dependency graph. `WSC-034` follows the candidate's transitive
candidate-isolation-before-local-broker dependency. `WSC-085`, `WSC-087`,
`WSC-091`, `WSC-093`, and `WSC-095` are candidate-DAG peers and therefore use
the existing stable program write-scope integration lock. The union of direct
dependencies and collision serialization remains acyclic.

The parent status returns from `draft` to `in-review`. This receipt does not
accept the parent; the next parent-owned route remains `review-program`.

## Repository Reconnaissance And Minimal Plan

Searches covered the canonical program structure validator, its 32 fixtures,
the typed Rust registry/scheduler implementation, the child-registry schema,
the proposal-program pattern, and the preserved primary parent ledger. No
owning ledger generator exists. The existing validator derivation and reviewed
ledger were reused. The minimal plan was to project reviewed records, reconcile
only candidate-DAG serialization differences, validate the exact bijection,
and retry the contained lifecycle.

## Impact And Evidence Map

- Code/runtime: unchanged.
- Parent contract: `resources/child-packet-index.yml` gains the mandatory
  typed ledger.
- Parent lifecycle: `proposal.yml#status` becomes `in-review`.
- Children and child receipts: unchanged.
- Tests: the canonical structure validator and all 32 collision-ledger
  positive/negative controls pass.
- Evidence: this receipt plus the retained lifecycle retry bundle and final
  contained retry.
- Rollback: discard this detached correction commit/worktree and return to
  `0eea044aed9ca3d2a8913ac2191a8f2f50cb50b7`.

## Compliance And Minimality Receipt

The correction reuses existing schema, validator, Rust typed model, stable
lock id, and reviewed collision decisions. It adds no dependency, helper,
abstraction, policy, workflow, generated output, child-owned artifact, or
external-tool assumption. No code or retained evidence is deleted. No
publication, cleanup, archive, credential, GitHub, or provider effect is
authorized. The external-tool-integrity result is not-applicable: no external
tool integration changes.

Cleanup review found only the temporary local projection script and YAML under
`/tmp`; they are outside the candidate and are not authority or retained
evidence. No repository cleanup is authorized or required. Remaining
implementation-quality risk: none for the addressed structural finding; parent
acceptance still requires the separate `review-program` route.

## Child Authority Boundary

The parent ledger coordinates physical integration order only. It does not
replace child semantic ownership, lifecycle state, validation verdicts,
receipts, promotion targets, archive metadata, implementation evidence, or
terminal outcomes.
