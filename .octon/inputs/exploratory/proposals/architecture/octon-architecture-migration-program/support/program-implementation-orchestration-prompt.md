prompt_id: octon-architecture-migration-program-implementation-orchestration-20260718T185923Z
generated_at: "2026-07-18T18:59:23Z"
generated_by: octon-proposal-lifecycle-generate-program-orchestration-prompt
generator_route_id: generate-program-implementation-orchestration-prompt
generation_run_id: 20260718-architecture-migration-readiness-01
prompt_set_id: octon-proposal-lifecycle-generate-program-implementation-orchestration-prompt
prompt_bundle_sha256: sha256:4d5f5a3826fc3797ba11147c2556137c43dc4753b117cbf6435b593af32b35f6
prompt_alignment_receipt: .octon/state/evidence/validation/extensions/prompt-alignment/2026-07-11T21-13-56Z-octon-proposal-lifecycle-octon-proposal-lifecycle-generate-program-implementation-orchestration-prompt.yml
target_program: .octon/inputs/exploratory/proposals/architecture/octon-architecture-migration-program
parent_packet_digest: sha256:34dc10786ecb4c63060ab3718acc00ad820c0a424d08910c9475054c5e52959e
generation_baseline_commit: 02712e6e45875f73461b02793ff85c3d1e6c273f
generation_baseline_tree: c9cb2f979a0b2af633c9df317e29db2b2b0add62
artifact_class: operational-aid
authority: non-authoritative
parent_status_at_generation: accepted
execution_mode: gated-parallel
release_state: pre-1.0
change_profile: atomic
worktree_baseline_lease: clean-detached-baseline
required_child_count: 15
accepted_child_count: 15
child_authority_preserved: yes
implementation_started_at_generation: no
program_implementation_orchestration_execution_authorized: separate-operator-invocation-required

# Program Implementation Orchestration Prompt — Octon Architecture Migration

> **Classification.** This generated prompt is a non-authoritative operational
> aid. It does not implement the program, create authority, satisfy a child
> receipt, promote a target, authorize a provider/credential/publication effect,
> or prove that implementation has begun or completed. The live repository,
> constitutional and instance authority, the parent registry, and each child
> packet control. If this prompt disagrees with any current source or receipt,
> stop and route to the owning review/revision lifecycle.

## 1. Objective And Hard Stop Boundary

Under a separate explicit execution authorization, implement the fifteen
required architecture-migration children through their own canonical packet
lifecycles in the parent registry's `gated-parallel` DAG. Preserve exact child
targets, semantic ownership, collision serialization, dependency verification,
safe resting states, rollback/recovery, and evidence classification.

This prompt itself must not be executed by the generation run. A future run may
perform repo-local child implementation and required child-owned lifecycle
receipts only within its separately granted authority. It must stop before any
ungranted provider, credential, host-installation, Git remote, GitHub,
publication, trust activation, production, archive, cleanup, or branch-deletion
effect.

The parent owns coordination, deterministic ordering, write-lease arbitration,
dependency-gate observation, blocker routing, aggregate references, and its own
run receipt. It owns no child implementation or promotion target and may never
repair, synthesize, or satisfy a child receipt.

## 2. Immutable Generation Bindings

Before dispatch, reproduce the current parent digest and require it to equal:

```text
sha256:34dc10786ecb4c63060ab3718acc00ad820c0a424d08910c9475054c5e52959e
```

The following accepted child review digests were fresh at generation. Re-read
the live receipts and require exact equality; any drift routes to that child's
review/revision lifecycle and invalidates this prompt until regeneration.

| RP | Child | Accepted packet digest |
| --- | --- | --- |
| RP-00 | `octon-architecture-migration-containment` | `sha256:cc4ade296cc474c90c53d731cfddeebdbc95adadd5414172da734a0d7c64b073` |
| RP-01 | `octon-architecture-migration-canonical-authority` | `sha256:c5369b2c9d6c1e8addafb2b7851d4609709affe0dbd7a443359d0b21761313a8` |
| RP-02 | `octon-architecture-migration-candidate-isolation` | `sha256:3228da9b1a70c687878a2fd32324cd1fd8729360113024fae48221d66923cada` |
| RP-03 | `octon-architecture-migration-transactional-runtime-store` | `sha256:2a8dc33d9df0b96d431198be9935ecaace428fe8714aa220bcb3b62ea1e4288d` |
| RP-04 | `octon-architecture-migration-local-broker` | `sha256:83e8a4bfcc5d2e65146b3561c5e9b858b6933af7fc81ea5b282246c2d3c50b93` |
| RP-05 | `octon-architecture-migration-sanitized-git` | `sha256:d2299fa953ec57c1842ff41371d7dfca7bf00297ba627e9a8c70c328f700efc0` |
| RP-06 | `octon-architecture-migration-verification-publication` | `sha256:1ebf5b95ddc1a85dfa149e543813ff9a2ccc32994d540eb471330a6257966f60` |
| RP-07 | `octon-architecture-migration-signed-evidence` | `sha256:87fbcceec1ea8956e96335808aef37a9c91b91793328a31d92b1c058703aaf08` |
| RP-08 | `octon-architecture-migration-recovery-class-b` | `sha256:e33cc5d1b1da806f3df10690ce169dac7e0fed2f318a65ce14741948db426e35` |
| RP-09 | `octon-architecture-migration-self-development-trust-activation` | `sha256:f98f8ff4844427e9174c6b89c991ce3c275531854e0f09afcb69ed241369887c` |
| RP-10 | `octon-architecture-migration-workspace-projects` | `sha256:149a717d53ef17879bd6a38793d6f90291dae752433743a485e58d09b4ae38cc` |
| RP-11 | `octon-architecture-migration-harness-factory` | `sha256:be1761195fc652673c8c3d9412f7d24ac794225a449fa1fc2ed9e5bdeddfe1c2` |
| RP-12 | `octon-architecture-migration-extension-supply-chain` | `sha256:8fab57e23160c152925b5b454bfa3a8a5068b50064e8958b7a802520d087d816` |
| RP-13 | `octon-architecture-migration-bounded-child-agents` | `sha256:100cb5178e4c441b71bf16c183aa290469b01daa083a6047d2e2c7663e5e0707` |
| RP-14 | `octon-architecture-migration-solo-dogfood-promotion` | `sha256:6ebaf19f6a0cd5e03af34ee684e3bf6c3f4501399009ddb9d156d65abe0a498d` |

## 3. Mandatory Parent Entry Gates

From the repository root, re-read ingress, the parent, every child, the current
worktree, and the generated discovery projections. Then rerun:

```sh
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-review-gate.sh \
  --package .octon/inputs/exploratory/proposals/architecture/octon-architecture-migration-program \
  --require-implementation-authorization

bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-program-child-readiness.sh \
  --package .octon/inputs/exploratory/proposals/architecture/octon-architecture-migration-program

bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-program-structure.sh \
  --package .octon/inputs/exploratory/proposals/architecture/octon-architecture-migration-program

OCTON_PROPOSAL_REGISTRY_PROJECTION_ONLY=1 \
  bash .octon/framework/assurance/runtime/_ops/scripts/generate-proposal-registry.sh --check

bash .octon/framework/assurance/runtime/_ops/scripts/generate-repo-authority-write-scope-index.sh --check
```

Also reproduce the parent digest with
`validate-proposal-review-gate.sh --print-digest` and compare it to §2. Stop on
any error, stale digest, new warning that indicates missing evidence/scope,
dirty-work ambiguity, lifecycle mismatch, registry/collision drift, or changed
prompt-bundle alignment. Never waive a gate in parent prose.

## 4. Mandatory Child Lifecycle Contract

Before each selected child implementation:

1. Re-read its `proposal.yml`, subtype manifest, source-of-truth map, packet
   contract, target architecture, implementation/validation/cutover/rollback
   plans, acceptance criteria, completeness review, accepted review, and strict
   architecture receipt.
2. Re-run proposal standard, implementation readiness, architecture proposal,
   strict review authorization, and strict architecture receipt validation.
3. Confirm the parent registry has exact ordered equality with the child's
   current `promotion_targets`, dependencies, phase, rollback posture, and write
   scopes.
4. If `support/executable-implementation-prompt.md` is absent, generate and
   validate it through the canonical
   `octon-proposal-lifecycle-generate-packet-implementation-prompt` route. Do
   not invent or execute an ad hoc child prompt.
5. Acquire an exact child write lease. A path shared with another child may be
   edited only under the parent collision record's integration owner and
   serialization mechanism.

After implementation, the child must own and pass:

- `support/implementation-run.md` with exact target and retained-evidence refs;
- every packet-specific validator, negative control, migration, rollback,
  adversarial, crash, and fault-injection requirement;
- `support/implementation-conformance-review.md` with `verdict: pass`;
- `support/post-implementation-drift-churn-review.md` with `verdict: pass`;
- child-owned promotion evidence at its declared retained-evidence target;
- a child closeout receipt that references, but does not absorb, those artifacts.

Run the generic post-implementation floor after child-specific validation:

```sh
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-implementation-conformance.sh \
  --package <child-packet-path>
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-post-implementation-drift.sh \
  --package <child-packet-path>
```

A `dependency_gate: verification` is satisfied only by current child-owned
implementation, validation, conformance, drift/churn, and required promotion
evidence. Parent summaries and sibling receipts never satisfy it. Archive,
cleanup, branch deletion, and external landing remain separately governed even
after child closeout readiness.

## 5. Child Implementation Map

The live registry is authoritative. These summaries aid navigation only and
must never widen current child manifests.

| RP | Phase | Hard dependencies | Targets / proof focus |
| --- | --- | --- | --- |
| RP-00 containment | phase 0 | none | Disable unsafe writers/direct-main/no-PR bridges, correct claims, inventory trust-sensitive surfaces, and prove SI-00 while preserving work. |
| RP-01 canonical authority | phase 1 | RP-00 | Authority/policy semantics, one-shot final launch guards at every census seam, configs/specs, and static/dynamic raw-spawn rejection. |
| RP-02 candidate isolation | phase 1 | RP-00 | Credentialless independent candidate workspace/session, native sandbox, descriptor/environment scrubbing, provider relay, export, escape negatives. |
| RP-03 transactional store | phase 2 | RP-01 | SQLite/WAL schema, sole writer API, T1/T2/UNKNOWN transitions, legacy migration/projections, backup/restore/corruption/capacity proof. |
| RP-04 local broker | phase 3 | RP-01, RP-02, RP-03 | Supervised authenticated broker, Keychain custody contracts, IPC/operation handles, sole deployed writer, health/recovery, disposable effect tests. |
| RP-05 sanitized Git | phase 4 | RP-04 | Closed Git adapter, object quarantine/import, ancestry, true expected-old CAS, conditional refs, mirror primitive, hostile-config negatives. |
| RP-06 verification/publication | phase 5 | RP-01, RP-03, RP-05 | Exact-SHA verifier, immutable route/history policy, protected-PR tuple and `S -> Q`, post-land verification/mirror, projection generator and tests. |
| RP-07 signed evidence | phase 6 | RP-03, RP-04, RP-06 | Role-bound signed observations, checkpoints, monotonic head, terminal reserve, retention/compaction, no-unsigned-fallback proof. |
| RP-08 recovery/Class B | phase 7 | RP-06, RP-07 | Provider result classification, UNKNOWN reconciliation, no route switching/blind retry, conditional cleanup, continuous-operation status, full SI-06 proof. |
| RP-09 trust activation | phase 8 | RP-06, RP-07, RP-08 | Trust inventory, inert content-addressed install, prior-version activation/health/rollback, selector/authority/retention evidence. |
| RP-10 workspace projects | phase 2 | RP-01 | Stable non-authoritative project identity/boundaries, snapshots, correction-preserving inference, rebuildable index, read-only inbox, continuity proof. |
| RP-11 Harness Factory | phase 3 | RP-01, RP-02, RP-10 | Deterministic JCS/SHA-256 Harness graph, compile receipt, exact guard binding, generic prepare/launch/observe/cancel/usage/retire adapter. |
| RP-12 extension supply chain | phase 8 | RP-07, RP-11 | Signed envelope/source/payload verification, private import CAS, availability vs selection, deny-by-default trust, generation rollback/revocation tests. |
| RP-13 bounded children | phase 8 | RP-08, RP-11 | Depth-one credentialless mission children, scope intersection, enforceable budgets, provider mapping, cancel/reconcile/retire, no durable effects. |
| RP-14 solo dogfood | phase 9 | RP-08, RP-09, RP-10, RP-11; RP-12/RP-13 for optional claims/full closeout | Proof-only immutable-input setup/two-project/20-mission/30-day/provider-drift protocol; evidence root and claim handoff only. |

## 6. Deterministic DAG Execution

Dependencies are hard implementation gates. Use registry order as the
deterministic tie-breaker among simultaneously eligible nodes; parallelism is
permitted only when exact write leases and all collision records allow it.

1. Implement and verify RP-00 alone. No privileged later work precedes SI-00.
2. After RP-00 verification, RP-01 and RP-02 are eligible in parallel.
3. After RP-01 verification, RP-03 and RP-10 are eligible in parallel.
4. RP-04 begins only after RP-01/RP-02/RP-03 verification. RP-11 begins only
   after RP-01/RP-02/RP-10 verification. They may overlap only where the
   collision ledger grants disjoint leases and exact serialization.
5. Advance the safety spine RP-04 → RP-05 → RP-06 → RP-07 → RP-08, honoring
   RP-03 and other listed predecessor gates at every step. Production Class B
   remains disabled until RP-05 through RP-08 all verify.
6. After their gates, advance RP-09, RP-12, and RP-13 in registry order; use
   parallel execution only for disjoint leases. RP-12 and RP-13 remain separate
   children and may not absorb each other's semantics.
7. Run RP-14 last for deterministic full-program proof. Its core claims require
   RP-08/RP-09/RP-10/RP-11; optional extension/child claims and full program
   closeout additionally require RP-12/RP-13. RP-14 may report a failure but may
   not fix an owning child.

A failed or blocked child does not invalidate a verified independent sibling,
but blocks every dependent node and a passing parent run. Record the exact
child, failing gate, preserved safe state, correction route, and next eligible
registry node; never skip an earlier open revision route.

## 7. Shared-Surface And Collision Discipline

- The live `resources/child-packet-index.yml` collision ledger is the only
  coordination source for the 126 current physical overlaps. Require its exact
  derived bijection and acyclic combined dependency/serialization graph before
  every parallel wave.
- At candidate-launch seams, RP-01 integrates final guard invocation first;
  RP-02 retains isolation semantics; RP-11 integrates only prepared-handle and
  generic adapter/Harness behavior after both. Sharing a file transfers no
  semantic ownership.
- Cargo manifests/lockfiles, authority/store/evidence modules, contracts,
  assurance directories, generated registries, and other shared surfaces use
  their declared integration owner and one atomic regeneration/validation
  point. Never allow concurrent raw edits to a shared file.
- Refresh derived proposal, repo-authority, host, catalog, or other projections
  only through their canonical owning generators and required token/gates.
  Generated output is discovery/evidence, never authority.
- Inventory the future execution worktree before dispatch. Foreign or ambiguous
  changes may not be reset, stashed, deleted, overwritten, committed, or claimed
  by a child. Isolate or stop when a safe exact lease cannot be proved.
- Use one Conventional Commit per receipt-atomic child lifecycle action and
  preserve unrelated work. A program aggregate commit must not collapse child
  receipts or hide the child-to-target mapping.

## 8. External, Credential, Publication, And Trust Barrier

Repo-local implementation does not authorize live effects. Without separate
explicit authority and platform approval, do not:

- install/start the broker or mutate launchd, Keychain, host trust, selectors,
  or active runtime state;
- acquire, enroll, expose, rotate, or use durable credentials/signing keys;
- create/update Git refs/remotes, GitHub Apps/checks/rulesets/queues/PRs,
  repositories, provider objects, releases, or public artifacts;
- publish generated host projections or enable production no-PR/PR automation;
- run provider dogfood that creates chargeable or durable external state;
- activate a trust root, import a live extension source, promote a support
  claim, archive a packet, delete evidence, or clean/retire user work.

Implement dry-run planners, contracts, fixtures, fakes, negative controls, and
repo-local adapters within child targets. When an acceptance criterion requires
a live external observation, stop at the barrier with exact blocked evidence
unless the future operator separately authorizes that effect. Never fabricate a
provider-observed, dynamically executed, adversarial, or promoted verdict.

## 9. Child-Specific Proof Floor

Run every validator named by each live packet. At minimum preserve these proof
themes:

- RP-00: exhaustive writer/claim inventories and disabled unsafe routes.
- RP-01: one final guard dominates every candidate spawn; unowned raw paths
  fail statically and dynamically.
- RP-02: credential/Git/filesystem/network/process isolation and useful export.
- RP-03: schema migration, one writer, crash consistency, reserve, backup,
  restore, corruption, and truthful UNKNOWN behavior.
- RP-04: authenticated IPC/replay denial, secret non-exposure, sole writer,
  supervised recovery, and disposable scratch effect.
- RP-05: hostile Git environment denial, object closure, ancestry, true
  expected-old CAS, conditional cleanup, and no generic Git surface.
- RP-06: immutable verifier identity/tuple, route freeze, history validation,
  complete PR state, merge atomicity, `S -> Q`, post-land proof, and mirror.
- RP-07: signature/key-role separation, fork/rollback rejection, terminal
  reserve under exhaustion, retention/compaction, and no unsigned fallback.
- RP-08: all provider outcome classes, lost responses, no retry while UNKNOWN,
  route preservation, cleanup-deferred honesty, and bounded recovery.
- RP-09: same-change self-certification denial, inert install, prior-version
  activation, health/rollback, bootstrap, selector, and evidence ordering.
- RP-10: two-project identity/continuity, boundary denial, correction
  preservation, immutable run snapshot, rebuilt index, and non-authority.
- RP-11: canonical graph/JCS bytes, complete input digests, guard equality,
  generic adapter conformance, cancellation/usage/retirement, no new runtime.
- RP-12: signature/archive/payload-tree/source/revocation/compatibility/capability
  checks, import CAS, availability-selection separation, generation rollback.
- RP-13: enforceable admission budgets, scope intersection, depth-one denial,
  provider mapping, cancel/UNKNOWN reconciliation, terminal retirement.
- RP-14: immutable proof inputs, paired/counterbalanced missions, exact timing
  and nearest-rank rules, provider freshness, 30-day burden, custody, claims,
  zero unauthorized effects, work preservation, and owner-routed failures.

Evidence classes remain exact. Proposal text, referenced future validators, or
previous packet evidence never proves a newly implemented change.

## 10. Parent Evidence And Required Run Receipt

Retain aggregate coordination evidence under:

```text
.octon/state/evidence/validation/proposals/octon-architecture-migration-program/
```

It may contain child IDs, digests, receipt references, dependency handoffs,
blockers, safe states, and bounded summaries. It must not copy secrets/raw
provider payloads or become child/runtime authority.

After every required child has current child-owned implementation, validation,
conformance, drift/churn, promotion, and closeout evidence required by its
packet, write the parent-local:

```text
.octon/inputs/exploratory/proposals/architecture/octon-architecture-migration-program/support/program-implementation-orchestration-run.md
```

It must include at least:

```yaml
schema_version: program-implementation-orchestration-run-v1
run_id: <stable run id>
verdict: pass|fail|blocked
implemented_at: <ISO-8601 UTC timestamp or null when not implemented>
promotion_evidence_count: <nonnegative integer>
child_authority_preserved: yes|no
required_child_count: 15
implemented_child_count: <integer>
verified_child_count: <integer>
conformance_pass_child_count: <integer>
drift_churn_pass_child_count: <integer>
promotion_ready_child_count: <integer>
closeout_ready_child_count: <integer>
parent_summary_not_child_evidence: true
child_receipts_remain_child_owned: true
archive_authority_granted: no
cleanup_authority_granted: no
external_effect_authority_granted: <yes|no with exact authority ref>
child_receipt_refs:
  - <child-id>:<receipt-id>:<repo-relative child-owned path>
```

Use `verdict: pass` and `child_authority_preserved: yes` only when every
required child's current evidence passes and remains child/target-owned. Include
commands, exact summaries, blockers or `none`, generator invocations and
freshness evidence, external barriers, and an explicit statement that the
parent receipt did not satisfy a child receipt, target, validation verdict,
promotion, conformance, closeout, terminal outcome, or archive metadata.

## 11. Failure, Rollback, And Correction Routing

Refuse a passing parent run when any review/digest is stale; a predecessor is
unverified; a validator/receipt/promotion target is missing or failing; a child
needs undeclared scope; collision serialization cannot be proved; evidence is
inflated; an external effect lacks authority; or child authority would move to
the parent.

For a child implementation defect, record child-owned evidence and enter that
child's correction/re-verification loop. For parent scope, DAG, ownership, or
collision drift, stop and revise/re-review the parent. For an irreducible human
policy/product decision or unsafe external effect, stop with the exact decision
or authority required. Roll back only through the owning child's declared
posture to the highest proved safe state; never restore a prohibited bridge,
switch an UNKNOWN attempt to PR, delete unlanded work, or relabel missing proof.

## 12. Generation-Time Evidence

This prompt was generated only after:

- the prompt bundle resolver reported `status: fresh`, `safe_to_run: true`, and
  no reason codes for bundle
  `sha256:4d5f5a3826fc3797ba11147c2556137c43dc4753b117cbf6435b593af32b35f6`;
- parent strict implementation authorization passed with zero errors/warnings;
- program child readiness passed with `errors=0 warnings=0`;
- all 16 parent/child packages passed proposal standard, implementation
  readiness, architecture, strict review authorization, and strict architecture
  receipt validation (`80/80` checks);
- parent program structure/collision validation passed with `errors=0
  warnings=0`, 15 children, 30 edges, and 126 collision records;
- proposal registry and repo-authority owner-generator checks passed;
- the live readiness projection passed with two truthful diagnostics: no
  materialized projection supplied and no terminal publication freshness
  required before implementation; and
- Git whitespace and pre-generation worktree checks passed at baseline commit
  `02712e6e45875f73461b02793ff85c3d1e6c273f`.

Missing future promotion targets reported by proposal-standard validators are
truthful pre-implementation warnings, not waivers or implementation evidence.
The final prompt-bearing packet digest is the value in §2 and must remain fresh
after prompt cataloging and receipt refresh.

## 13. Final Reporting Contract For The Later Run

When the separately authorized future run stops or completes, report:

- exact parent start/end digest, commit, tree, and status;
- every child ID, lifecycle outcome, implementation/conformance/drift/promotion/
  closeout receipt paths, and dependency handoff;
- parent run-receipt path, verdict, `implemented_at`,
  `promotion_evidence_count`, and `child_authority_preserved`;
- validators and final summaries, including truthful warnings;
- shared-file leases, integration owners, and generator refresh evidence;
- external/manual effects authorized, performed, denied, or still blocked;
- preserved work, rollback/correction routes, and foreign worktree state;
- exact next canonical lifecycle action; and
- explicit confirmation that parent evidence did not replace child authority.

Do not archive, clean, land, publish, merge, push, delete branches/evidence, or
perform provider/credential/trust/production effects unless a separate current
authority explicitly includes that action.
