# Parent File Change Map

This revision changes only proposal-local parent and materially affected child
artifacts plus a formal revision receipt. It owns no child implementation target. Child durable targets
are discoverable from each child `proposal.yml`; the parent only records
coordination scopes in `resources/child-packet-index.yml` and exact source
ownership in `resources/source-ownership-map.md`.

The shared generated proposal registry is refreshed only by its owning generator
and only if changed manifests/catalog inputs require it. No runtime, workflow,
provider, credential, intake, reconciliation, Revision 2, `.github/**`, or
authoritative contract/source file is modified by this proposal revision.
