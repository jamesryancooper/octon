# Program Reading and Precedence Map

Current constitutional and durable repository authority outrank the entire
program. The intake is the non-authoritative but controlling record of accepted
operator-decision lineage pending formal promotion. The independent reviews and
fixed reconciliation are advisory architecture, packet, engineering, and proof
refinements; they cannot silently reopen accepted intent. The reconciliation's
fifteen-packet DAG remains the coordination baseline, while Revision 2 remains
retained predecessor lineage.

Within this parent, `proposal.yml` owns identity/lifecycle,
`resources/child-packet-index.yml` owns the coordination DAG,
`architecture/child-packet-contract.md` owns parent/child separation, and the
remaining registers are corrected aggregate read models.

`support/operator-decision-acceptance-receipt.md` records the operator's five
accepted bounded postures as proposal-local intent evidence. It is not runtime,
policy, support, trust, provider, or promotion authority.

Every child owns its manifests, detailed boundary, promotion targets,
implementation-grade review, acceptance, implementation authorization, proof,
promotion, conformance, closeout, and archive metadata. Parent summaries and
aggregate evidence never satisfy child gates. The artifact catalog and generated
proposal registry are projections only. Planned proof remains `UNVERIFIED`.
