# Bound Source Context

Preserved verbatim from the create-packet invocation (2026-07-09). This is
raw source context, not authority.

## Bound Creation Input

> Create a single architecture proposal packet with slug
> runtime-environment-override-governance under
> .octon/inputs/exploratory/proposals/architecture/, sourced from retained
> review evidence at
> .octon/state/evidence/validation/architecture/reviews/super-root-balanced-review/20260709-super-root-balanced-review/
> (finding F-01: ambient environment-variable overrides affect authority
> decisions without a declared contract; attachable F-02 stage_only_behavior
> documentation and F-05 spec index; decision options: break-glass contract vs
> removal vs hybrid).

## Operator Task Framing (same session)

The operator's orchestration request additionally required: framing the
central decision as Option A (governed receipted break-glass contract with
inventory, documentation, protected-mode prohibition, negative controls) vs
Option B (remove ambient authority-affecting overrides; replace legitimate
bootstrap with explicit receipted runtime inputs on the narrow publication
path) vs Option C (hybrid only if evidence justifies it); recommending one
option with rationale; defining acceptance criteria including per-override
disposition, protected-mode behavior, receipts, validator expectations,
documentation surfaces, rollback posture, and pre-implementation evidence;
scoping in OCTON_ALLOW_STALE_RUNTIME_ROUTE_BUNDLE,
OCTON_POLICY_MODE_OVERRIDE, OCTON_EFFECTIVE_POLICY_MODE,
OCTON_EXECUTION_ROLE_KIND, OCTON_EXECUTION_ROLE_ID, intent override behavior,
stage_only_behavior documentation, and the env-override invariance negative
control; excluding implementation patches, unrelated refactors, support
widening, generated/effective publication, proposal program creation, and
constitutional amendment drafting; and recording explicitly where human
governance input is required.

## Source Classification

Architecture-review finding set from a completed balanced architecture review
→ scenario `audit-aligned-packet` (each in-scope finding maps to remediation,
acceptance, validation, and closure; deferred findings carry owner and
rationale). Secondary influence: architecture-evaluation source material.

Full review evidence copies: `resources/retained-review-evidence/`
(canonical: `.octon/state/evidence/validation/architecture/reviews/super-root-balanced-review/20260709-super-root-balanced-review/`).
