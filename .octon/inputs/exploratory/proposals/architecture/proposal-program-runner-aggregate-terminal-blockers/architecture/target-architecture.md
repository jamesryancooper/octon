# Target Architecture

The parent controller writes aggregate child terminal blocker evidence that
lists child id, route id, blocker class, receipt freshness, terminal policy
status, hygiene state, retry state, and next route condition. Program routes
may consume this evidence, but child receipts remain child-owned.
