# Acceptance Criteria

- Missing profile and run id produce clear blocked evidence.
- Fresh target-bound workflow evidence can satisfy resume admission.
- Readiness preflight remains required before expensive continuation or mutation.
- Dirty or stale source posture still requires route-owned clean worktree selection and include-path classification.
- Tests reject proposal-local summaries, generated outputs, chat state, and model memory as admission substitutes.
