# Implementation Plan

1. Inventory current delivery workflow admission inputs and validator failures.
2. Define a structured admission diagnostic that reports missing and stale inputs.
3. Bind resume inputs only from fresh target-bound workflow evidence.
4. Preserve readiness preflight and include-path classification requirements.
5. Add fixtures for missing profile, missing run id, stale handoff refs, stale source posture, and dirty worktree posture.
