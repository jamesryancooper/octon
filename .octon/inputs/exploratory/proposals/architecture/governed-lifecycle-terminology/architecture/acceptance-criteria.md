# Acceptance Criteria

- The current product capability is named `Governed Lifecycle Orchestration`.
- Active current-product prose no longer uses `Lifecycle Autopilot` as the
  capability name.
- `Lifecycle Runner`, `Lifecycle Executor Adapter`, and `Lifecycle Phase-Loop
  Model` remain the technical nouns for the runtime component, execution
  component, and contract primitive.
- `Governed Lifecycle Control Loop` appears only in explanatory prose and is
  not introduced as a component, schema, route, file name, lifecycle id, or
  contract primitive.
- The implementation defines governed execution as self-operating only through
  approved runner/executor mechanisms, never self-authorizing.
- Product feature and roadmap validators pass with the renamed capability.
- Proposal lifecycle validators pass for this packet.
- Generated effective projections are refreshed only as derived publication
  outputs if source extension inputs changed.
- Historical, archived, and retained evidence references are preserved unless
  they are part of current active product capability guidance.
