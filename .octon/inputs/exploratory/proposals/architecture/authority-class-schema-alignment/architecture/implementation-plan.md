# Implementation Plan

1. Compare the product feature catalog schema authority classes with the
   topology registry surface classes.
2. Add or align authority-class values needed by the mechanism index without
   widening product feature catalog authority.
3. Update catalog entries only when needed to preserve navigation-only posture.
4. Update the mechanism index authority-class guide with the shared vocabulary.
5. Add validation examples that reject `state/control/**` as retained evidence.
6. Add validation examples that distinguish generated operator read model from
   generated-effective non-authority.

Implementation must stay limited to the listed promotion targets.
