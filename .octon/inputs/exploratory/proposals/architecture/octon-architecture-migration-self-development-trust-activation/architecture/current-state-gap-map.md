# Current-State Gap Map

| Existing surface | Gap | RP-09 change |
| --- | --- | --- |
| Evolution command and promotion runtime/activation specs exist. | Complete semantic trust closure and prior-version dominance are not proved. | Version the trust inventory and exact activation envelope. |
| Host tool resolution provides an `OCTON_HOME`/side-by-side substrate. | Content-addressed install, one selector, kill-safe receipts, and rollback compatibility need binding. | Define install/selector/health/rollback contracts and operations. |
| Self-evolution validator exists. | Rename/build/provider indirect changes, candidate self-widening, reboot, disk-full, and every switch kill point remain UE-009/UE-015 work. | Extend adversarial/fault suites against exact artifacts. |
| RP-01 defines epoch semantics. | ROD-003 now accepts a small content-addressed semantic epoch-zero inventory, one-time human trust anchor/bootstrap, and exact scope/artifact-version/time/budget/verifier/health/rollback preauthorization; encoding, detection, health windows, canaries, and rollback durations still require design and proof. | Consume the accepted boundary and keep safe-automatic disabled/unclaimed until proof and later promotion acceptance. |

Fixed-baseline drift does not touch RP-09 source families.
