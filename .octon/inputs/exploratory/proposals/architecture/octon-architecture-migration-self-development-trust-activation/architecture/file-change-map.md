# File Change Map

| Surface | Ownership/change |
| --- | --- |
| kernel `commands/evolution.rs` | RP-09 installed-version classification/install/activation/rollback orchestration only |
| promotion runtime/activation and new trust/install/selector schemas | RP-09 versioned contracts |
| constitutional runtime/authority entries | RP-09 exact entries via trusted integration lane; RP-01 semantics unchanged |
| instance evolution and host-tool resolution | RP-09 desired policy and side-by-side resolution |
| scaffolding runtime operations | RP-09 content-addressed install/selector functions |
| self-evolution validator/tests | RP-09 adversarial and fault proof |
| deployment `OCTON_HOME` and `.octon/state/control/evolution/**` | runtime outputs, not authored promotion targets |

`.github/**` and provider configuration are observed projections/state, never
RP-09 promotion targets.
