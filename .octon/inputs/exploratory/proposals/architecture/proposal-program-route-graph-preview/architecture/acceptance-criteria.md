# Acceptance Criteria

- Route graph output is available before route execution.
- Route graph output shows parent routes, child routes, review/revise loops, architecture-review gates, delivery handoff, and blockers.
- Route graph output includes non-authority classification.
- Route graph output does not consume an execute-routes step.
- Tests prove route graph output cannot satisfy child receipts, delivery receipts, Change closeout, or terminal proof.
