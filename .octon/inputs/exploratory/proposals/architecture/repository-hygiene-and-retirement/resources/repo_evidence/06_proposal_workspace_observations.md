# Repo Evidence 06 — Proposal Workspace Observations

## Active architecture proposal workspace

**Source:** `/.octon/inputs/exploratory/proposals/architecture/`

- the live active architecture proposal workspace currently contains a single
  directory: `octon_bounded_uec_proposal_packet`
- that active example is useful as historical style context but does not define
  the current manifest-governed contract for new packets

## Archived architecture proposal workspace

**Source:** `/.octon/inputs/exploratory/proposals/.archive/architecture/`

- the archive contains many proposal directories, including
  `proposal-system-integrity-and-archive-normalization`
- the archived `proposal-system-integrity-and-archive-normalization` packet is
  manifest-governed and shows current-style `proposal.yml` and
  `architecture-proposal.yml` use
- the archived packet README explicitly preserves proposal non-canonicity and
  the generated-registry-as-discovery-only posture

## Implication for this packet

The correct approach is to inherit the current standards and validator-backed
contract first, and use packet examples only when they do not conflict with
those higher-precedence sources.
