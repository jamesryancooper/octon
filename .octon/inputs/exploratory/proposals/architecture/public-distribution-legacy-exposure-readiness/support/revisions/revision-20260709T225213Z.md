# Packet Revision Receipt

revision_id: public-distribution-legacy-exposure-readiness-revision-20260709T225213Z
revised_at: 2026-07-09T22:52:13Z
reviser: independent-architecture-review (octon-proposal-lifecycle-revise-packet)
route: revise-packet
prior_status: in-review
status_after_revision: in-review

## Basis

Independent architecture review of the octon-public-distribution-model program
assigned four child-owned findings to this packet (AR-002, AR-004a, AR-011,
AR-012). The parent registry
(`.octon/inputs/exploratory/proposals/architecture/octon-public-distribution-model/resources/child-packet-index.yml`)
was already revised by the parent route and is authoritative for this child's
write scopes; parent child-packet-contract item 11 defines the promotion-target
convention applied here.

## Findings Addressed

- **AR-002 (narrow targets)** — Previous position: `proposal.yml`
  `promotion_targets` listed three broad shared directories
  (`.octon/framework/assurance/runtime/_ops/scripts/`,
  `.octon/framework/assurance/runtime/_ops/tests/`,
  `.octon/framework/constitution/contracts/disclosure/`), claiming ownership of
  entire families that other packets also write into.
  Revised position: targets are the exact deliverable files and leaf
  directories from the parent registry write scopes —
  `validate-legacy-exposure-readiness.sh`,
  `test-legacy-exposure-readiness.sh`,
  `_ops/fixtures/legacy-exposure-readiness/`,
  `legacy-exposure-readiness-v1.schema.json`,
  `legacy-exposure-response-runbook.md` — plus the child evidence root
  `.octon/state/evidence/validation/proposals/public-distribution-legacy-exposure-readiness/`.
  Why this improves alignment: exact targets make promotion auditable,
  eliminate cross-packet ownership collisions inside shared `_ops` directories,
  and satisfy child-packet-contract item 11.
- **AR-004a** — Previous position: the target architecture named only
  directory-level work areas, the synthetic sensitive fixtures and the
  sanitized-history fixture had no declared home, and the revoke-first runbook
  had no named file. Revised position: `target-architecture.md` File-Level Work
  Areas names all five registry-scoped deliverables with their roles, states
  that the synthetic sensitive fixtures and the sanitized-history fixture live
  under `.octon/framework/assurance/runtime/_ops/fixtures/legacy-exposure-readiness/`,
  and identifies the credential revoke-first runbook as
  `.octon/framework/orchestration/governance/legacy-exposure-response-runbook.md`;
  `implementation-plan.md` phases reference the same files explicitly.
  Why this improves alignment: every promoted artifact is now traceable from
  the architecture docs to a registry write scope, so implementation cannot
  invent or misplace deliverables.
- **AR-011** — Previous position: AC-01 read "the inventory covers all
  reachable tracked blobs selected by policy", leaving blob selection to an
  undefined policy and making the criterion subjective. Revised position:
  AC-01 defines the selection policy in the packet — every blob reachable from
  every ref at the exact reviewed revision, enumerated in a deterministic order
  that yields identical output on rerun, with no undeclared exclusion — and the
  Deterministic Automation section mirrors the same rule. Why this improves
  alignment: the criterion is now objectively testable and cannot be satisfied
  by a scanner that silently narrows its own scope.
- **AR-012** — Previous position: AC-03 rejected an "absent maintainer
  decision" without defining what record constitutes that decision, so the gate
  had nothing concrete to reject on. Revised position: the maintainer decision
  record is specified as a required field set of the
  `legacy-exposure-readiness-v1` receipt schema — disposition, credential
  actions, timestamp, and reviewed revision — and AC-03,
  `target-architecture.md`, and the implementation-plan validation plan all
  state that the gate rejects when any field is absent. Why this improves
  alignment: the human-authority gate becomes machine-checkable without moving
  the decision itself away from the maintainer.

## Superseded Content Preserved

- Previous `promotion_targets`:
  `.octon/framework/assurance/runtime/_ops/scripts/`,
  `.octon/framework/assurance/runtime/_ops/tests/`,
  `.octon/framework/constitution/contracts/disclosure/`.
- Previous File-Level Work Areas: the same three directories, unannotated.
- Previous AC-01: "Given an exact repository ref, the inventory covers all
  reachable tracked blobs selected by policy and records its scope digest
  without reading ignored local residue."
- Previous AC-03: "The transition readiness command performs no Git or GitHub
  mutation and rejects unresolved material findings, absent credential
  disposition, or absent maintainer decision."
- Previous Deterministic Automation bullet: "Enumerate refs and tracked blobs
  at an exact commit."
- Previous implementation-plan phases 1–4 named no concrete files; previous
  validation bullet: "The transition gate rejects unresolved findings and
  missing maintainer disposition."

## Non-Effect Confirmation

This revision edited packet-local files only. No parent artifact, sibling
packet, generated projection, state root, runtime file, or intake content was
modified. The packet remains `in-review`, unaccepted, and unauthorized for
implementation.
