# Implementation Plan

1. Add a product README note that product features are a navigation layer.
2. Add feature README language pointing architecture/governance readers to the
   governed cross-surface mechanisms index.
3. Add targeted links from existing feature docs where mechanism boundaries are
   easy to confuse.
4. Keep feature catalog entries navigation-only.
5. Avoid adding runtime/operator mechanisms as product features unless a later
   accepted product contract requires that move.
6. Run product feature catalog validation and link/terminology checks.
