# Source Prompt

The operator asked whether the postmortem follow-up should be an atomic
implementation plan or a packet. The selected answer was to create a packet
with an atomic implementation plan inside it, because the follow-up touches
lifecycle routing, terminal closeout, publication freshness, cleanup
classification, and `branch-no-pr` authorization posture.

The operator then requested:

```text
Create the packet.
```

This source material is informative lineage only. It is not authority for
implementation, acceptance, cleanup, branch landing, or closeout.
