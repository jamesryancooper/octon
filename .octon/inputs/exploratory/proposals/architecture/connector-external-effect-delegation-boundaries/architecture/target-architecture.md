# Target Architecture

Connector effects are machine-delegable only when contract, token, scope,
egress, rollback or compensation, replay, receipt, and fail-closed gates pass.

External irreversible effects remain human-required unless a later contract
proves explicit rollback or compensation and the authority token permits the
effect class.
