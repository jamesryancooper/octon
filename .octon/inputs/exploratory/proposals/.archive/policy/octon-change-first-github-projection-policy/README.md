# Change-First GitHub Projection Policy

Repo-local follow-up proposal for `.github/**` alignment after the Octon
Change-first default work unit policy is promoted.

This packet keeps GitHub workflow edits out of the octon-internal policy
proposal. It scopes GitHub as a projection host for PR-backed Changes and as a
push-validation host for direct-main Changes where repo policy allows local or
workflow evidence without PR metadata.
