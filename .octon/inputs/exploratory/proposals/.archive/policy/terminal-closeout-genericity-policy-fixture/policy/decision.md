## Decision

Use this policy proposal as a terminal-closeout genericity fixture. The packet is distinct from `proposal-program-delivery` by proposal id, proposal kind, path, promotion targets, and support receipts.

The intended effect is validation-only: it exercises the deterministic terminal closeout route against a policy packet with existing durable policy targets.
