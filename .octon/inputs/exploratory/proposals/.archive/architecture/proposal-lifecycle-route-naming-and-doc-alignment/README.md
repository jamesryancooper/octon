# Lifecycle Route Naming And Doc Alignment

This child packet proposes aligning operator-facing command names, effective route ids, lifecycle runner messages, skill docs, and command docs for proposal-program clean delivery.

The goal is to reduce surprise without renaming authority surfaces casually.
