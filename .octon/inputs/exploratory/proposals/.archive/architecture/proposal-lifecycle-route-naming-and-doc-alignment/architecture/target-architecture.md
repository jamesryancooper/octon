# Target Architecture

Docs and help text consistently map:

- operator command names
- effective lifecycle route ids
- skill ids
- prompt-set ids
- workflow ids
- alias names
- cleaned request versus cleaned claim semantics

The mapping helps operators read route graph output and resume blocked runs. It does not redefine route authority.
