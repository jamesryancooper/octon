# Portable Proof Interop Analysis

Proof bundles require bundle ID, issuer/domain, subject, scope, proof class, evidence manifest, digest manifest, freshness metadata, redaction manifest where required, revocation refs, source authority refs, disclosure refs, and local acceptance state after import.
