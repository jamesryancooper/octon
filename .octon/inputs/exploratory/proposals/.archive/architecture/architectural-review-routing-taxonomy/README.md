# Architectural Review Routing Taxonomy

This child defines when Octon uses each architectural review, audit, packet,
evaluator, or constitutional challenge route. The target is deterministic
routing that avoids collapsing distinct review types into one vague workflow.
