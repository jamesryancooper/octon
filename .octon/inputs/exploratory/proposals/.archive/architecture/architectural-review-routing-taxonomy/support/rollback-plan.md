# Rollback Plan

Remove routing doctrine, validators, tests, and fixtures introduced by this
child. Keep previously existing audit and extension route behavior unchanged
unless a separate accepted child owns that change.
