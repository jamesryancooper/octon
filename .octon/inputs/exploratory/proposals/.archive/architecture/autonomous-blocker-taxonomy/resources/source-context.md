# Source Context

Parent program:
`.octon/inputs/exploratory/proposals/architecture/autonomous-lifecycle-blocker-recovery`

Source findings:

- recoverable invalid enum values should not require human intervention;
- stale receipts, freshness drift, and cleanup residue need autonomous routing;
- hard blockers must remain limited to authority, ownership, destructive,
  external, unsupported-scope, and unrecoverable validation cases;
- parent summaries must not become child receipts.

This file is proposal-local context only and does not authorize runtime
behavior.
