# Git Mutation Sandbox Preflight

This P1 child packet defines diagnostics for permission-sensitive git mutation
steps such as fetch, checkout, landing, sync, cleanup, and branch deletion.

The packet is proposal-local input only and does not authorize git mutation,
branch deletion, parent closeout, archive, cleanup, publication, or a
`cleaned` claim.
