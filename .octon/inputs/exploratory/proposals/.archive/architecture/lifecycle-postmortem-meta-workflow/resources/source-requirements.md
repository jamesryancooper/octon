# Source Requirements

- Provide a way to run the lifecycle postmortem after a lifecycle process has
  run.
- Bind the evaluator to retained evidence rather than chat memory.
- Keep evaluator outputs as evidence only.
- Support all lifecycle types that have sufficient retained evidence.
- Fail closed or report low confidence when required evidence is missing.
- Do not add the postmortem as a mandatory closeout gate by default.
