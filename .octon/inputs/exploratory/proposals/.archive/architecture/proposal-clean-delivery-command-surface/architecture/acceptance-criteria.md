# Acceptance Criteria

- One explicit operator surface requests proposal-program clean delivery.
- The command delegates to the existing lifecycle runner.
- The command requires explicit execution or clearly documents execution behavior.
- The command does not treat `target_outcome=cleaned` as proof.
- Tests show missing delivery readiness, child receipts, Change closeout, cleanup proof, or terminal proof blocks cleaned claims.
