# Executable Implementation Prompt

Act as a senior Octon implementation orchestrator and implement the Continuous
Stewardship Runtime v3 proposal packet in a single governed migration.

Do not create an infinite loop. Implement Stewardship Program, finite
Stewardship Epochs, trigger normalization, admission decisions, idle decisions,
renewal decisions, stewardship ledger, stewardship evidence profiles,
stewardship-aware Decision Requests, campaign coordination hooks, CLI/runtime
surfaces, validators, and documentation.

Preserve all Octon boundaries: framework/instance authored authority,
state/control operational truth, state/evidence retained proof,
state/continuity resumable context, generated derived projections only, inputs
non-authoritative. Stewardship must not execute material work directly; admitted
work must hand off to v2 Mission Runner and existing run authorization.

Stop only when validation passes and a final implementation report confirms no
rival control plane was introduced.
