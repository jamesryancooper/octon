# Rollback Plan

Rollback validators, fixtures, publication changes, and generated projections
introduced by this child. Do not remove prior child implementation surfaces
unless that child owns the rollback evidence.
