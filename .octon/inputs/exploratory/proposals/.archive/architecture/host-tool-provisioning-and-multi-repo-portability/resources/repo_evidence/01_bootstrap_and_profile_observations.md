# Repo Evidence 01: Bootstrap and Profile Observations

- `/.octon/instance/bootstrap/START.md` defines `bootstrap_core`,
  `repo_snapshot`, and `pack_bundle` as repo-local profile surfaces.
- `/init` is documented as generating repo-local bootstrap files, not as
  provisioning host binaries.
- The bootstrap docs already distinguish authored repo surfaces from generated
  or runtime-facing outputs.
