# Target Architecture

Before dispatching routes known to require fresh generated/effective runtime
state, the controller can classify stale extension catalogs, pack routes,
runtime route bundles, and capability routing drift. Recovery guidance cites
canonical publication scripts or declared recovery actions only.
