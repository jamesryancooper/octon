# Churn Class Table

| Path family | Authority classification | Likely producer | Current problem | Proposed solution | Cleanup eligibility | Proposed owner | Packet/dependency |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `.octon/generated/cognition/projections/materialized/runs/**` | Derived operator read model, non-authoritative | Run-health read-model generator and validators | One generated file per retained run caused about 1010 tracked modified files in the audit range. | Add changed-run-only generation, stable no-op writes, compact indexes, and active/recent materialization windows where safe. | Generator-owned only; do not hand-edit or generic-clean tracked projections. | Run-health read-model owner | `proposal-churn-run-health-read-model-compaction`; external dependency `run-program-clean-delivery-test-hermeticity` |
| `.octon/generated/effective/runtime/**`, `capabilities/**`, `extensions/**`, `governance/**`, `locality/**`, locks, receipts | Runtime-facing generated/effective output, freshness-gated, non-authoritative | Runtime route, pack route, capability routing, governance/locality, and extension publishers | Runtime-facing outputs can rewrite even when semantic inputs are unchanged; locks and receipts make churn high-risk. | Make publishers digest-aware and write only on semantic change while preserving freshness, locks, receipts, traceability, and resolver validation. | Not cleanup; republish only through owning producer. | Effective publication owners | `proposal-churn-effective-publication-idempotency` |
| `.octon/generated/effective/extensions/published/**` | Copied derived extension publication payloads | Extension state publisher | Copied payload trees create repeated materialization and file fanout. | Use manifest or digest-addressed payload reuse and changed-extension-only publication. | Publisher-owned prune only; not generic cleanup. | Extension publication owner | `proposal-churn-extension-payload-compaction` |
| `.octon/generated/effective/capabilities/filesystem-snapshots/**` | Derived capability snapshot output | Filesystem snapshot service | Snapshot history can accumulate or churn when identity and retention are not explicit. | Add stable snapshot identity, no-op detection, retention budget, and producer-owned GC proof. | Producer-owned retention only. | Filesystem snapshot producer | `proposal-churn-filesystem-snapshot-retention` |
| `.octon/generated/proposals/**` | Derived proposal registry and artifact indexes | Proposal registry and artifact generators | Registry, artifact indexes, and spines grow and rewrite as proposal/archive history changes. | Make generation archive-aware, changed-packet-only, and index-first. | Publisher-owned regeneration or pruning only. | Proposal lifecycle artifact owner | `proposal-churn-proposal-artifact-compaction` |
| `.octon/generated/.tmp/**` | Rebuildable local scratch/build cache | Publication wrapper, engine build/cache, validation runners | Huge transient file and byte counts can occur even when ignored and absent from landed diffs. | Add TTL, size budgets, cache placement discipline, cleanup command, and rebuildability proof. | Yes, only when classified rebuildable/unreferenced by owning route. | Engine build/cache and validation runner owners | `proposal-churn-tmp-engine-cache-hygiene`; external dependency `run-program-clean-delivery-cleanup-disposition` |
| `.octon/state/evidence/validation/**` | Retained validation/publication evidence | Validators and publication receipt writers | Repeated timestamped receipts, including prompt-alignment and publication receipts, cause fanout and token overhead. | Dedupe equivalent receipts, use compact indexes/latest pointers, and content-address full logs with retrieval proof. | Not generic cleanup; retained evidence unless owning route classifies exact stale/unreferenced files. | Validation evidence framework | `proposal-churn-receipt-fanout-compaction` |
| `.octon/state/evidence/runs/**`, `.octon/state/control/execution/**`, `.octon/state/continuity/**` | Retained evidence, control truth, continuity | Run lifecycle, closeout, evidence-store, continuity writers | Operational trees can grow, but latest audit showed narrow landed churn and these surfaces include retained truth. | Keep adjacent to core program; add indexing/retention only with owning cleanup authority and reference-integrity proof. | No generic deletion. Only exact stale/unreferenced files with owning authority. | Runtime evidence/control owners | Optional `proposal-churn-retained-run-evidence-efficiency`; external dependencies `proposal-program-loop-breaker`, `closeout-worktree-autonomous-partition-evidence` |
| `.claude/**`, `.codex/**`, `.cursor/**` | Generated-like host projections, user-facing, non-authoritative | Host projection publisher | About 33 host projection files changed in the audit range. | Make publishing idempotent, host-aware, and changed-file-only while preserving parity and non-authority notices. | Publisher-owned prune only; not generic cleanup. | Host projection publisher | `proposal-churn-host-projection-idempotency` |
| `.octon/framework/**`, `.octon/instance/**`, `.octon/inputs/**`, proposal archives | Authored authority, instance policy, raw input, retained proposal lineage | Source changes, extension source edits, proposal lifecycle/archive routes | These surfaces can trigger downstream churn but are not generated cleanup candidates. | Track as causal inputs for invalidation and metrics; do not delete or compact through churn cleanup. | Not cleanup candidates for this program. | Owning source/proposal authorities | Program guardrail only |
