# Acceptance Criteria

- Extension shared method prompt no longer owns native review doctrine after the
  native doctrine child lands.
- Architecture Revision Packet remains extension-owned.
- Extension routing references native review modes without satisfying native
  gates.
- Extension validation rejects claims that packetization replaces proposal
  lifecycle gates.
- No permanent differently named `architecture-readiness-audit` aliases remain.
