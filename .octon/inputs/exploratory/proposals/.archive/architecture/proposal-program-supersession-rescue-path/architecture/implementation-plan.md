# Implementation Plan

1. Add polluted-run detection inputs from ownership baseline, blocker ledger, and worktree classification evidence.
2. Define freeze evidence shape and non-authority classification.
3. Add child-owned receipt carry-forward refs and digest checks.
4. Add deliverable partitioning rules that separate owned deliverables from foreign/manual residue.
5. Add successor-run creation path for continued proposal-program lifecycle execution.
6. Add route to normal Change closeout when deliverable changes are ready and lifecycle execution no longer needs the polluted parent.
7. Add delivery profile and receipt validation coverage.

This child must not grant cleanup authority or implement closeout-worktree partition report changes.
