# Authority Class Schema Alignment

This child proposal packet aligns authority-class vocabulary for governed
cross-surface mechanisms.

It preserves the parent program boundary: product feature catalog entries are
navigation-only, `state/control/**` is mutable operational truth,
`state/evidence/**` is retained evidence, generated-effective surfaces are
non-authority runtime handles, and generated operator read model surfaces are
visibility-only.

The packet is exploratory input until promoted through normal proposal
lifecycle gates.
