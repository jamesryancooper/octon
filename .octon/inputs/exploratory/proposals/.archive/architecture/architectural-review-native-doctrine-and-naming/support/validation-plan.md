# Validation Plan

- Run `validate-proposal-standard.sh --package` for this packet.
- Run `validate-architecture-proposal.sh --package` for this packet.
- Run `validate-proposal-implementation-readiness.sh --package` for this packet.
- Add `validate-architectural-review-naming.sh`.
- Add negative controls for stale `architecture-readiness-audit` references
  after retirement.
- Regenerate proposal registry and compact artifact projections.
