# Target Architecture

Extension publication should separate semantic extension state from repeated
payload copies.

The target can use:

- digest-addressed payload references;
- manifest-based payload indexes;
- changed-extension-only publication;
- stable serialization;
- write-if-changed behavior;
- receipt linkage for publication and compatibility proof.
