# Validation Plan

- Run existing proposal validators.
- Add schema validation tests for report, routing decision, and support receipt.
- Add negative controls for missing evidence, omitted validators, stale digest,
  placeholder language, unresolved blockers, and prose-only pass.
- Add compatibility tests proving `review-finding-v1` and
  `review-disposition-v1` are referenced.
- Regenerate proposal registry and compact artifact projections.
