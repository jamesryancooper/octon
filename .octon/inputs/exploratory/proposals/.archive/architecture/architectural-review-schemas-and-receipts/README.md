# Architectural Review Schemas And Receipts

This child creates the strict receipt layer required before proposal lifecycle
gate wiring. It makes architectural review receipts schema-backed and
validator-enforced, while reusing the existing `review-finding-v1` and
`review-disposition-v1` contracts.
