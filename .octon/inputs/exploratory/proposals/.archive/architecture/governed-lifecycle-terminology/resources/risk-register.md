# Risk Register

| Risk | Mitigation |
| --- | --- |
| Renaming `Lifecycle Autopilot` breaks validators that hard-code the old path. | Update validators and tests in the same implementation. |
| `Governed Lifecycle Orchestration` is shortened to ambiguous `orchestration`. | Add terminology sweep and explicit wording rules. |
| `Governed Lifecycle Control Loop` becomes a component name. | Keep it prose-only and test with search. |
| Archived evidence is rewritten unnecessarily. | Preserve historical references unless active product guidance depends on them. |
| Feature id rename breaks navigation. | Update product catalog, alignment profiles, validators, and registry together. |
