# Implementation Plan

This packet proposes future work only.

1. Inventory current parent summary, blocker ledger, checkpoint, and child
   receipt references.
2. Define the minimal required child-owned receipt reference fields.
3. Define replayable evidence pointer behavior when control state is cleaned.
4. Add compact event summary schema or validator expectations.
5. Add negative controls rejecting parent-summary-only child state.
