# Source Context

Parent program:
`.octon/inputs/exploratory/proposals/architecture/autonomous-lifecycle-blocker-recovery`

Source findings:

- final parent summary preserved child authority but could tempt misuse;
- parent summaries must never be child receipts;
- control checkpoints may be cleaned, so retained evidence needs replayable
  pointers;
- failure evidence should be compact but not lossy.

This file is proposal-local context only.
