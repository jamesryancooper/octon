# Implementation Plan

1. Parse current `catalog.yml` and feature notes.
2. Inspect authored evidence roots: commands, skills, workflows, runtime specs,
   runtime code, contracts, validators, tests, and instance governance policies.
3. Classify changed surfaces against product-feature criteria.
4. Compare expected catalog coverage with actual catalog refs.
5. Emit a result consumable by the feature-catalog drift receipt.
6. Add positive and negative-control fixtures.
