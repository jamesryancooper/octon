# Target Architecture

The validator receives proposal implementation evidence, changed paths, and
current feature catalog state. It classifies documentation obligations without
treating generated projections, raw inputs, host UI state, chat/model memory,
or tool availability as authority.

The validator returns machine-readable findings consumed by the closeout gate.
