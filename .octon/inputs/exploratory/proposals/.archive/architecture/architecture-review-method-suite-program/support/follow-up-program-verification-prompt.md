---
prompt_id: architecture-review-method-suite-program-program-verification-20260710T1538Z
generated_at: "2026-07-10T15:38:27Z"
generated_by: octon-proposal-lifecycle-generate-program-verification-prompt
generator_route_id: generate-program-verification-prompt
generation_run_id: 20260709-arms-program-clean-delivery-04
target_program: .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program
artifact_class: operational-aid
authority: non-authoritative
parent_status_at_generation: implemented
program_execution_mode: gated-parallel
child_authority_preserved: yes
program_verification_execution_authorized: gated-rerun-required
verdict: ready-for-verification
---

# Follow-Up Program Verification Prompt — Architecture Review Method Suite Program

> **Classification.** Non-authoritative operational aid generated by the
> `generate-program-verification-prompt` route. This file is an execution guide
> and evidence pointer; it is **not** a source of truth and it does not itself
> constitute verification. The authoritative coordination surfaces are
> `proposal.yml`, `architecture-proposal.yml`, and
> `resources/child-packet-index.yml`; child outcomes are owned by each child's
> own receipts. Where this prompt disagrees with those files, the live
> repository, constitutional surfaces, or a child-owned packet, those sources
> win and this prompt (or the registry/design docs) needs revision — not silent
> execution.
>
> Program packet: `.octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program/`
> Execution mode: `gated-parallel` · Change profile: `atomic` · Release state: `pre-1.0`

## 1. Purpose and Authority Boundary

Run **parent aggregate verification** for the Architecture Review Method Suite
Program after program implementation orchestration. Program verification checks
**both** the parent coordination level and the child outcome level, but parent
status can never replace a child validation verdict, and aggregate receipts are
**parent-local summaries only** — they cite child receipt state by path and
digest, they never satisfy it.

This prompt does **not** authorize parent closeout, archive, Proposal Program
Delivery, Change closeout, hosted landing, branch cleanup, worktree cleanup,
publication, deletion, generated-output refresh, Git mutation, promotion, or a
`cleaned` claim. The later verification route may write or refresh **only** these
two parent-local aggregate receipts:

- `support/program-implementation-orchestration-conformance-review.md`
- `support/program-post-implementation-orchestration-drift-churn-review.md`

Those receipts may **summarize** child state by path and digest, but they must
not satisfy or replace child manifests, child receipts, child validation
verdicts, child promotion targets, child archive metadata, child closeout
evidence, or child lifecycle outcomes.

## 2. Verification Target

Verify the implemented proposal program at:

```text
.octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program
```

Packet identity (re-confirm against the live `proposal.yml`; do not trust this
snapshot):

- `proposal_id`: `architecture-review-method-suite-program`
- `proposal_kind`: `architecture`
- `status`: `implemented`
- `program_execution_mode`: `gated-parallel`
- `seed_reference_child`: `architecture-lens-bank-foundation`
- `required_child_count`: `6` (registry children `required: true`)
- conditional child: `architecture-review-command-facades` (`required: false`)
- profile: `release_state=pre-1.0`, `change_profile=atomic`

Act as an **independent verifier**. Re-ground every claim against current
repository state before judging the packet. Generation-time observations in this
prompt are context only; current files, validators, and retained evidence win.

## 3. Required Source Reading

Read these parent files before checking child evidence or durable targets:

- `proposal.yml`
- `architecture-proposal.yml`
- `README.md`
- `resources/child-packet-index.yml`
- `resources/child-packet-index.md`
- `architecture/packet-sequence.md`
- `architecture/child-packet-contract.md`
- `architecture/program-closeout-plan.md`
- `architecture/implementation-plan.md`
- `architecture/target-architecture.md`
- `architecture/acceptance-criteria.md`
- `architecture/method-taxonomy.md`
- `architecture/lens-bank-design.md`
- `architecture/integration-and-disposition.md`
- `navigation/source-of-truth-map.md`
- `navigation/artifact-catalog.md`
- `support/proposal-review.md`
- `support/implementation-grade-completeness-review.md`
- `support/pre-integration-architecture-review.yml`
- `support/program-implementation-orchestration-prompt.md`
- `support/program-implementation-orchestration-run.md`
- `support/lifecycle-residue-cleanup.md`

Then inspect each required child packet as **child-owned** evidence. Resolve each
child from the parent registry path to its **current** active or archived packet
path — do not trust the generation-time location. At generation time all six
required children were archived under
`.octon/inputs/exploratory/proposals/.archive/architecture/<child-id>/`; the
later route must re-resolve this against the live tree.

For each required child, inspect these child-owned files (whichever the child's
subtype defines; absence of a child-owned receipt is a child finding, never
something parent text may backfill):

- `proposal.yml`
- `support/proposal-review.md`
- `support/implementation-grade-completeness-review.md`
- `support/implementation-run.md`
- `support/implementation-conformance-review.md`
- `support/post-implementation-drift-churn-review.md`
- `support/pre-integration-architecture-review.yml`
- `support/proposal-closeout.md`
- child archive metadata / terminal closeout evidence

If a required child is missing, no longer terminal, no longer archived when the
parent expects archived evidence, or lacks child-owned implementation,
conformance, drift, validation, closeout, or archive evidence, write **blocked**
parent aggregate receipts and return control to the owning child lifecycle route.

## 4. Two-Level Verification Scope

Program verification has two levels. Every finding must be attributed to exactly
one level (see §9).

**Parent level** — verify the parent's own coordination obligations:

- **Sequence.** The registered `gated-parallel` phase order (phase-0 → phase-1 →
  phase-2 parallel group → phase-3) was honored; creation ahead of gates is
  permitted, implementation ahead of gates is not.
- **Dependency.** Every declared `dependency_gate: verification` hard dependency
  was satisfied before the dependent child began implementation (see §5).
- **Risk.** The program risk posture (`rollback_posture: manual` per child;
  shared-surface collision control; fail-closed method/lens semantics) holds and
  no unmanaged aggregate risk was introduced by coordination.
- **Evidence.** Aggregate program evidence is retained under the declared program
  evidence root and points to child receipts by path and digest without copying
  or replacing them.
- **Deferral.** Both required standing deferrals are recorded with owner and
  re-trigger (see §6).
- **Closeout criteria.** The `architecture/program-closeout-plan.md` conditions
  are met or accurately reported as not-yet-met; the parent has not asserted
  closeout/archive authority it does not own.

**Child level** — verify, **by reference to child-owned receipts only**:

- **Acceptance criteria** each child declared were met (child proves; parent
  records the verdict path).
- **Validation plans** (validator depth + negative controls) executed with
  child-retained evidence.
- **Promotion targets** landed inside the child's declared `write_scopes` under
  `framework/**` (or `.claude/**` for the conditional facades child) through
  governed acceptance.
- **Implementation evidence** (implementation-run, conformance, drift/churn)
  exists, is child-owned, and is coherent.

Parent status must never substitute for any child-level item above.

## 5. Child Registry, Phases, and Dependency Gates

The registry (`resources/child-packet-index.yml`) is authoritative for id, path,
required/deferred, dependencies, phase, group, rollback posture, and write
scopes. Verify against it; the table is a navigation summary.

| # | Child id | Phase / Group | Required | Hard deps (gate = `verification`) |
| --- | --- | --- | --- | --- |
| 1 | `architecture-lens-bank-foundation` *(seed)* | phase-0 / suite-foundation | yes | — |
| 2 | `architecture-review-method-taxonomy-and-routing` | phase-1 / suite-foundation | yes | 1 |
| 3 | `greenfield-reference-architecture-review-method` | phase-2 / method-docs | yes | 2 |
| 4 | `companion-architecture-review-methods` | phase-2 / method-docs | yes | 2 |
| 5 | `architectural-review-schema-extensions` | phase-2 / contracts-and-assurance | yes | 2 |
| 6 | `architectural-review-suite-integration` | phase-3 / integration | yes | 3, 4, 5 |
| 7 | `architecture-review-command-facades` *(conditional)* | phase-3 / integration | **no** | 6 |

Gate rules to confirm:

- The dependency gate is `verification` for every declared dependency: the
  dependency child passed its own verification loop (child-owned receipts) before
  the dependent child began implementation.
- Parent evidence never satisfies a child gate; each child carries its own
  creation, review, implementation, and verification receipts at its canonical
  or archived path.
- A blocked or rejected child does not block siblings without a declared
  dependency on it; the parent records the disposition and re-sequences via
  registry revision.
- Rejection of child 1 (`architecture-lens-bank-foundation`) or child 2
  (`architecture-review-method-taxonomy-and-routing`) invalidates the suite
  premise; the expected disposition is program supersession or rejection, not
  scope narrowing.

## 6. Conditional Child and Standing Deferrals

- `architecture-review-command-facades` (`required: false`) is created and run
  only on demonstrated operator demand for direct method-invocation facades.
  Otherwise its terminal disposition is a **program-local no-action** with
  rationale, recorded at closeout. Verify the disposition is present and honest —
  either a landed facade packet with child-owned receipts, or a recorded
  no-action with rationale.
- Two standing deferrals must be recorded (owner `octon-maintainers`) with a
  re-trigger condition:
  1. **Command-facades no-action** — re-trigger: demonstrated operator demand
     for direct method invocation.
  2. **Boundary/Authority generic (adopted-repo) mode** — re-trigger: first
     concrete adopted-repo review need. Boundary/Authority ships Octon-only in
     this program.

Absence of a required deferral record is a **parent-level, not-deferrable**
finding.

## 7. Parent-Level Verification Questions

Answer each with cited evidence:

- Does `proposal.yml` remain `status: implemented`, with packet-local lifecycle
  authority in `proposal.yml` rather than subtype metadata?
- Does `support/program-implementation-orchestration-run.md` report
  `verdict: pass`, `required_child_count: 6`, `terminal_child_count: 6`,
  `parent_summary_not_child_evidence: true`,
  `child_receipts_remain_child_owned: true`, and
  `child_authority_preserved: yes`, with `archive_authority_granted: no`,
  `cleanup_authority_granted: no`, and `git_mutation_authority_granted: no`?
- Do all six required child packets remain terminal (`closed`, `superseded`, or
  `rejected`) with child-owned review, implementation, conformance, drift, and
  closeout evidence at their current resolved paths?
- Is the conditional facades child either a landed facade packet or a recorded
  program-local no-action with rationale (§6)?
- Does child sequencing preserve every dependency gate in
  `resources/child-packet-index.yml` (§5)?
- Do child-owned implementation and validation receipts cover the suite
  acceptance criteria (lens bank + lens-reference validator; naming v2 + routing
  v2 fail-closed on `unknown_method` / `missing_method_record`; Greenfield method
  doc reference-architecture-only boundary; four companion method docs with
  boundary statements vs readiness and surface-audit doctrine; additive v2
  report/routing-decision schemas with v1 coexistence; suite integration
  navigation-only, gates unchanged) without a parent-only proof gap?
- Did each child stay inside its registry-declared `write_scopes`, with no silent
  cross-child scope expansion and no shared-file collision in the parallel
  phase-2 group?
- Were generated projections refreshed **only** through canonical publication
  scripts, and do generated read models, proposal paths, generated prompts, host
  projections, chat, dashboards, and model memory remain non-authoritative?
- Are both standing deferrals recorded with owner and re-trigger (§6)?
- Is aggregate program evidence retained under
  `.octon/state/evidence/validation/proposals/architecture-review-method-suite-program/`,
  pointing to child receipts by path and digest?
- Are cleanup, branch deletion, hosted mutation, publication, archive, final
  sync, terminal cleanliness, and `cleaned` claims absent unless a separate
  owning route holds current retained evidence?

## 8. Required Commands

Run from the repository root. Record exact command, exit code, relevant output
summary, affected paths, and retained evidence location. Re-resolve exact flags
against the live scripts (usage evolves); treat any command below that no longer
exists or no longer accepts a flag as a re-grounding finding, not a silent skip.

**Parent packet structure and gates:**

```text
yq -e . .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program/proposal.yml
yq -e . .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program/architecture-proposal.yml
rg -n "T[O]DO|T[B]D|F[I]XME|\{\{|\[[D]escribe" .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-standard.sh --package .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architecture-proposal.sh --package .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-program-structure.sh --package .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-program-child-readiness.sh --package .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-program-readiness-projection.sh --package .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program --require-terminal-evidence
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-review-gate.sh --package .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program --require-implementation-authorization
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architectural-review-receipts.sh --receipt .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program/support/pre-integration-architecture-review.yml --package .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program --mode pre-integration-architecture-review --require-pass
git diff --check
```

> If a program readiness projection spine exists under
> `.octon/generated/proposals/artifacts/architecture/architecture-review-method-suite-program/`,
> also run the readiness-projection validator with `--projection <spine>
> --require-terminal-evidence`. If no spine exists, record that the
> package-scoped readiness check is the binding one and do not fabricate a
> projection path.

**Suite durable-surface sweep** (the validators the children landed; these prove
the delivered method/lens/schema surface remains coherent and fail-closed — they
self-discover the canonical methodology tree and take no target flag):

```text
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architectural-review-lens-references.sh
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architectural-review-naming.sh
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architectural-review-routing.sh
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architectural-review-receipts.sh --receipt .octon/framework/assurance/runtime/_ops/fixtures/architectural-review/valid-pre-integration-receipt.yml --require-pass
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architectural-review-workflows.sh
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architectural-review-lifecycle-gates.sh
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architectural-review-extension-split.sh
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architectural-review-skills-commands.sh
bash .octon/framework/assurance/runtime/_ops/tests/test-architectural-review-validators.sh
```

**Per resolved child path** (`<child>` = current active or archived packet path):

```text
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-standard.sh --package <child>
bash .octon/framework/assurance/runtime/_ops/scripts/validate-architecture-proposal.sh --package <child>
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-review-gate.sh --package <child> --require-implementation-authorization
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-implementation-conformance.sh --package <child>
bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-post-implementation-drift.sh --package <child>
```

Every child that touched naming, routing, schemas, or validators must have
retained validator evidence **including negative controls** (unknown method id,
undefined lens reference, method without profile, receipt missing method field).
Missing negative-control evidence for an enforcement-surface child is a
not-deferrable finding.

## 9. Finding Attribution and Stable Identity

Use stable finding ids in the `ARMS-PVFY` namespace (`ARMS-PVFY-001`,
`ARMS-PVFY-002`, …). Reuse the same id across reruns for the same root cause; do
not renumber after one resolves; group issues only when they share one correction
and one acceptance test.

Every finding must declare its **attribution level** — exactly one of:

- `parent` — a parent coordination obligation (sequence, dependency enforcement,
  risk, aggregate evidence, deferral, closeout criteria).
- `child:<child_id>` — scoped to one child packet.
- `child-group:<group_id>` — scoped to a registry group
  (`suite-foundation`, `method-docs`, `contracts-and-assurance`, `integration`).
- `cross-packet-dependency:<from>-><to>` — a dependency-gate or handoff defect
  spanning two packets.

Each finding must also include:

- id
- severity: `P0`, `P1`, `P2`, or `P3`
- status: `open`, `resolved`, `blocked`, or `accepted-external`
- attribution level (above)
- affected paths
- evidence
- expected behavior
- correction scope
- acceptance criteria
- deferral eligibility: `eligible` or `not-eligible`

No finding is deferrable if it concerns child authority preservation,
implementation conformance, post-implementation drift, generated/runtime
publication freshness, proposal-path authority leakage, required validator
failure, a required-child terminal-evidence gap, a missing required deferral
record, or fail-closed method/lens semantics.

Open corrections route to `generate-program-correction-prompt` per finding id.

## 10. Required Receipt Shapes

If **all** parent aggregate conformance gates pass, write or refresh
`support/program-implementation-orchestration-conformance-review.md` with at
least this frontmatter:

```markdown
verdict: pass
unresolved_items_count: 0
child_receipt_summary_count: <current count of child-owned receipts inspected>
child_authority_preserved: yes
verified_at: <UTC timestamp>
run_id: <current verification run id>
target_program: .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program

# Program Implementation Orchestration Conformance Review
```

The conformance receipt body must include: scope and authority boundary; the
stable findings table (with attribution levels); checked parent evidence; a child
receipt summary for all six required children plus the conditional child's
disposition; sequence and dependency-gate verification; suite acceptance-criteria
coverage; validator coverage with command summaries and evidence refs;
generated-output and proposal-path non-authority review; the two standing
deferral records; delivery/Change/cleanup/branch/archive/terminal proof
boundaries; explicit exclusions; and a final route recommendation.

If parent aggregate drift/churn gates pass, write or refresh
`support/program-post-implementation-orchestration-drift-churn-review.md` with at
least:

```markdown
verdict: pass
unresolved_items_count: 0
child_receipt_summary_count: <current count of child-owned receipts inspected>
child_authority_preserved: yes
verified_at: <UTC timestamp>
run_id: <current verification run id>
target_program: .octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program

# Program Post-Implementation Orchestration Drift/Churn Review
```

The drift/churn receipt body must include: scope and authority boundary; the
stable findings table; current parent and child state; an active proposal-path
backreference scan across durable surfaces; generated-projection freshness
review; manifest and schema validity; host-projection and generated read-model
boundary review; target-family boundary review across methodology, routing,
schema/contract, workflow, product-feature, mechanism-navigation, validator, and
test surfaces; the lifecycle-residue and worktree-hygiene posture under the
explicit dirty-start lease (§11); churn limited to changes caused by this
verification route; validators run; delivery/Change/cleanup/branch/archive/
terminal proof boundaries; explicit exclusions; and a final route recommendation.

**Pass discipline.** Set `verdict: pass` **and** `child_authority_preserved: yes`
on either receipt **only** when aggregate program evidence is clean **and** child
manifests, child receipts, child promotion targets, child validation verdicts,
and child archive metadata all remain child-owned. If any required parent or
child gate fails, write the receipt(s) with `verdict: blocked` or `verdict: fail`,
set `child_authority_preserved: no` whenever the failure would require parent
substitution for child authority, set `unresolved_items_count` to the true open
count, and record stable findings that name the next owning route. Never fill a
child evidence gap with parent text.

## 11. Lifecycle Residue Handoff Consumption

Re-read the live `support/lifecycle-residue-cleanup.md`. The residue/worktree
posture does **not** block parent aggregate verification receipts by itself, but
the drift/churn receipt must record it accurately and must not claim cleanliness
the residue route did not grant.

Consume the parent handoff as follows:

- If `support/lifecycle-residue-cleanup.md` is `retained`/`blocked` **and no**
  validating parent handoff return exists, record the residue posture as
  `blocked-retained` (or its live verdict) and route the broader worktree residue
  to `closeout-worktree` / operator scope resolution. Do **not** record
  cleanliness, and do **not** treat the residue block as a child-authority or
  conformance failure — it is a delivery/worktree-hygiene boundary, not a
  child-evidence gap.

- The verification loop **may** record
  `resolved-by-validated-parent-closeout-worktree-return` and proceed **only when
  all other aggregate gates pass and** a parent `lifecycle-interaction-return-v1`
  exists that cites a validating `closeout-worktree-report-v1` carrying
  **parent** handoff authorization for **all** of:
  1. the current cleanup receipt,
  2. the current classifier digest **or** foreign fingerprint,
  3. the current residue fingerprint,
  4. a non-mutating disposition, and
  5. the retained path set.

  A `closeout-worktree-report-v1` that is **child-scope** (e.g. a
  program-child-batch handoff that returns authority to owning lifecycle routes)
  is **not** a parent return and does **not** satisfy this condition.

This `resolved-by-validated-parent-closeout-worktree-return` disposition
authorizes **nothing** further: it does not authorize deletion, cleanup, archive
relocation, Git mutation, publication edits, `cleaned` claims, promotion, or any
child-owned evidence. It only records that the parent aggregate verification is
not blocked by residue when a validated parent return is present.

## 12. Return Status

Return exactly one final route status:

- `clean`
- `corrections-needed`
- `needs-packet-revision`
- `blocked`
- `superseded`
- `explicitly-deferred`

Use `clean` **only** when both required parent aggregate receipts exist, both
declare `verdict: pass`, both declare `unresolved_items_count: 0`, both declare
`child_authority_preserved: yes`, all required validators and deterministic
checks pass or are recorded as accepted-external without a parent claim, both
standing deferrals are recorded, and no open `ARMS-PVFY` finding remains.

Use `corrections-needed` for bounded parent-receipt or durable corrections that
stay within accepted child scope (route via `generate-program-correction-prompt`).
Use `needs-packet-revision` when accepted parent or child proposal semantics are
insufficient. Use `blocked` when authority, evidence, or validator state is
missing and cannot be corrected within the route. Use `superseded` if the suite
premise was invalidated (child 1 or child 2 rejected). Use `explicitly-deferred`
only for a recorded, owner-attributed deferral.

## 13. Hard Stops

Stop without writing passing receipts if any of these are true:

- parent `proposal.yml#status` is not `implemented`;
- parent review is missing, stale, not accepted, or lacks implementation
  authorization;
- parent strict pre-integration architecture review is missing, stale, or not
  passing;
- `support/program-implementation-orchestration-run.md` is missing, incomplete,
  not passing, or reports `child_authority_preserved: no`;
- child readiness, program structure, parent standard, or architecture
  validation fails;
- any required child cannot be resolved to a current active or archived packet;
- any child-owned implementation, conformance, drift/churn, closeout, or archive
  evidence is missing or failing;
- any suite durable-surface validator (naming, routing, receipts, workflows,
  lens-references, lifecycle-gates, extension-split, skills-commands) fails,
  or a required negative control is absent;
- a required standing deferral record is missing;
- parent summaries would be the only proof of child state;
- verification would require destructive cleanup, branch deletion, hosted
  provider mutation, generated publication, archive relocation, Change closeout,
  or terminal proof outside the owning route;
- generated outputs, proposal paths, generated prompts, host projections, chat,
  dashboards, or model memory would need to become authority.

## 14. Authority Boundaries (fail-closed)

- Proposal packets, generated prompts, generated support files (including this
  one), the child registry, generated registries/projections, raw inputs, host
  state, chat, and model memory **do not** become Octon authority, control truth,
  runtime policy, or permission.
- Durable authority lives only in declared promotion targets **outside** active
  proposal paths, only after validation, publication, and retained evidence
  support the claim — and only inside the owning child, never the parent.
- Generated effective prompt/projection assets are consumed only through
  freshness-checked handles and remain derived-only; full prompt expansion
  requires an explicit allowed trigger with retained replay evidence.
- Aggregate verification receipts are parent-local summaries; they cite child
  receipts by path and digest and never satisfy a child receipt, promotion
  target, validation verdict, or archive metadata.
- The parent creates no new mechanism, lifecycle gate, routed workflow mode, or
  command facade; it composes with — never modifies or duplicates — readiness and
  surface-architecture audit doctrine.
- Fail closed on missing source, digest mismatch, stale freshness, a blocked or
  stale child, or any authority-boundary violation.
