# acceptance-criteria.md

Canonical packet artifact: `architecture/acceptance-criteria.md`.

This root-level file is a convenience pointer for packet consumers that expect the requested artifact name at packet root. The architecture working document remains under `architecture/` per Octon's architecture proposal convention.
