# Source Context

Parent program:
`.octon/inputs/exploratory/proposals/architecture/autonomous-lifecycle-blocker-recovery`

Source findings:

- invalid child registry enum values should be diagnosed with accepted values;
- stale receipts and review digests need stale cause and refresh target;
- publication freshness drift needs targeted refresh diagnostics;
- runner recovery should not parse verbose prose logs.

This file is proposal-local context only.
