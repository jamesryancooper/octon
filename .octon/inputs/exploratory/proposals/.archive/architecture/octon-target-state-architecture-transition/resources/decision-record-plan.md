# Decision Record Plan

## Required ADR

Create:

`.octon/instance/cognition/decisions/101-target-state-architecture-transition.md`

## ADR contents

The ADR must record:

- accepted severity judgment: focused gap-closing with moderate runtime restructuring;
- preserved invariants;
- rejected re-foundation;
- accepted side-effect inventory and coverage model;
- accepted runtime modularization model;
- accepted support dossier sufficiency model;
- accepted generated map publication model;
- compatibility retirement policy;
- validation evidence roots;
- rollback posture.

## ADR index update

Update `.octon/instance/cognition/decisions/index.yml` to include the new ADR.

## Non-governance note

The ADR is durable decision authority beneath the constitutional kernel. This proposal packet is not.
