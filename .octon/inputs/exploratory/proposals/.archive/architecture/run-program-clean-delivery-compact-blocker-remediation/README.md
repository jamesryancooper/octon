# Run Program Clean Delivery Compact Blocker Remediation

This child packet owns PM-001: blocker handling generated too much residue.

The durable goal is to add compact blocker-remediation mode so repeated
recoverable blockers constrain artifact production while preserving required
route-owned evidence.
