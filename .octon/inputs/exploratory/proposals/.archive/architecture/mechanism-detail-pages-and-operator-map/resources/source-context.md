# Source Context

This optional child derives from the parent documentation plan's later
refinement work for per-mechanism detail pages and operator read models.

Required readiness phrases:

- operator read models
- navigation only
- non-authority
