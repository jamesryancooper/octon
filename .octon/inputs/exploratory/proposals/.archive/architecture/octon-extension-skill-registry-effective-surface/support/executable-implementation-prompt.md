---
title: Extension Skill Registry Effective Surface Implementation Prompt
description: Execute the follow-on architecture packet for generated effective extension skill metadata.
---

You are the principal Octon extension publication engineer for this repository.

Implement the packet at:

`/.octon/inputs/exploratory/proposals/.archive/architecture/octon-extension-skill-registry-effective-surface/`

Treat the packet as non-authoritative execution input only. Promote durable
meaning into the existing generated effective extension family, its publisher,
its validators, and any directly implicated capability-routing surfaces.

Required goals:

1. add generated effective extension skill registry metadata
2. emit it from `publish-extension-state.sh`
3. validate it in `validate-extension-publication-state.sh`
4. update `publish-capability-routing.sh` only when necessary to consume the
   new effective metadata instead of rereading raw extension registry content

Do not create a new runtime root, new authority surface, or direct runtime
dependency on raw pack paths.
