# Execution constitution conformance card

## Claim posture

This packet proposes only paths that remain inside Octon’s observed constitutional model.

## Conformance checks

| Check | Result | Notes |
|---|---|---|
| Single `/.octon/` super-root preserved | Pass | No descendant-local roots proposed |
| Authority restricted to `framework/**` and `instance/**` | Pass | All durable meaning placed there |
| Live control truth restricted to `state/control/**` | Pass | Review dispositions and mission classification explicitly land here where needed |
| Retained proof restricted to `state/evidence/**` | Pass | Failure/distillation/output-envelope receipts all land here |
| `generated/**` remains derived-only | Pass | Distillation summaries are optional and non-authoritative |
| `inputs/**` remains non-runtime and non-policy truth | Pass | Proposal packets never become control or policy truth |
| No second mission control plane | Pass | Existing mission/run control roots are extended rather than duplicated |
| No approval-boundary bypass | Pass | Broad access / approval bypass is rejected outright |
| No shadow memory | Pass with conditions | Distillation is proposed only as evidence -> proposal -> promotion, never runtime memory |
| Overlay-aware placement respected | Pass | Proposed repo-specific changes fit enabled governance and agency-runtime overlays or existing instance-native surfaces |

## Residual conformance sensitivities

- The distillation workflow must never become a direct runtime memory feed.
- Review findings must remain evidence unless and until dispositions are materialized into run control.
- Mission classification must refine `mission-autonomy.yml`, not supersede it with a second classifier.

## Verdict

Conformant **if** promoted changes follow the file-change map and validation plan in this packet.
