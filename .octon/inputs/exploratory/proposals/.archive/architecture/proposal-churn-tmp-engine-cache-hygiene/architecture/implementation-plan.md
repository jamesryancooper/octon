# Implementation Plan

Implementation is not authorized by this proposal packet.

When authorized, this child should:

1. Baseline `.tmp` file and byte counts across representative runs.
2. Define explicit scratch classification and budgets.
3. Update wrappers or cleanup helpers to keep cache output bounded.
4. Add dry-run and refusal tests.
5. Prove pruned outputs are rebuildable.
6. Record post-run residue metrics.
