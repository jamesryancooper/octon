# Target Architecture

The operator surface should support explicit invocation against a target such as:

- proposal packet;
- proposal program;
- lifecycle run;
- feature or command surface;
- touched path set.

The output should be an advisory report path or inline summary. The surface must name the report as advisory and cite the canonical evaluator source.
