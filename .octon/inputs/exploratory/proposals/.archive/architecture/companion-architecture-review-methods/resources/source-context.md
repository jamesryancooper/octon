# Source Context (Bound Input, Preserved)

This packet was created by the proposal-lifecycle `create-packet` route as
child `companion-architecture-review-methods` of program run
`20260709-arms-program-clean-delivery-04`. Source material is preserved here as
lineage only; it is never authority. All claims were re-grounded against the live
repository before this packet was written (see
`../architecture/current-state-gap-map.md`).

## Bound Input Envelope

- `proposal_id`: `companion-architecture-review-methods`
- `proposal_kind`: `architecture`
- `target`:
  `.octon/inputs/exploratory/proposals/architecture/companion-architecture-review-methods`
- `source` (bound):
  `.octon/inputs/exploratory/proposals/architecture/architecture-review-method-suite-program/`
- `invocation_authority`: `unattended` (program-child-route)

## Bound Source

The bound `source` is the parent program packet directory, itself a retained
(non-authoritative) proposal packet. Rather than copy the entire multi-file
program verbatim, this packet preserves the **governing slices** that scope this
child, and points to the full source, which remains retained on disk at the
program path above:

- Child charter and obligations — preserved in `./child-charter.md` (verbatim
  extract of the program's `resources/child-packet-index.yml` entry,
  `architecture/child-packet-contract.md` per-child charter, and the obligations
  every child must meet).
- Companion method specifications — preserved in
  `./method-taxonomy-companions.md` (verbatim extract of the program's
  `architecture/method-taxonomy.md` §§3–6 plus its "Composition With Adjacent
  Doctrine" section).
- Non-authoritative intake unit (root lineage):
  `.octon/inputs/additive/.incoming/architecture-review-method-suite/`.

The full program design set (`architecture/intake-evaluation.md`,
`lens-bank-design.md`, `integration-and-disposition.md`, `packet-sequence.md`,
`target-architecture.md`, `program-closeout-plan.md`, and the navigation/support
files) remains available at the program packet path for reviewers; nothing in it
is treated as authority by this child.

## Required Source Claims Extracted (as traceable inputs)

- **Thesis.** Author the four remaining companion architecture-review methods as
  first-class methodology surfaces under the existing Architectural Review
  Mechanism method layer, with one shared contract shape and one shared lens bank.
- **Scope discipline / non-goals.** No new mechanism, routed workflow mode,
  lifecycle gate, evidence root, schema, validator, or command facade. Balanced
  remains default. Readiness and surface-audit doctrine are composed with, never
  modified or duplicated. Boundary/Authority ships Octon-only in v1.
- **Explicit boundary requirements.** Failure-Mode Review vs the readiness audit's
  failure-mode assessment; Boundary/Authority Review vs the surface-architecture
  audit's single-unit classification.
- **Validation expectation.** Methodology-docs child ⇒ a doc-consistency check
  against `naming.yml` and `lens-bank.yml` (no new validator required).
- **Closure condition.** Four docs authored, wired, and consistency-proven; child
  reaches a governed terminal outcome with child-owned evidence.

See `./traceability-map.md` for the finding/requirement → artifact map.
