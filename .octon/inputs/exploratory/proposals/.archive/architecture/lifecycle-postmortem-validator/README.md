# Lifecycle Postmortem Validator

This child packet defines deterministic validation for lifecycle-postmortem
outputs.

The validator should make the postmortem evaluator safe to run repeatedly by
checking report completeness, evidence references, final judgment values, and
authority boundary statements.
