# Implementation Plan

_Status: In-review proposal packet artifact_


## Workstream 1 — `.octon/README.md` foundation

1. Add governed workflow runtime framing immediately after super-root identity.
2. Preserve class-root table and non-authority rules.
3. Add one sentence on task-specific execution harnesses as target framing, not live schema.

## Workstream 2 — repo-root companion projection

1. Keep repo-root `README.md` and `AGENTS.md` out of this packet's active
   promotion targets.
2. Preserve candidate product-facing wording for a linked repo-local companion
   proposal.
3. Preserve current-state/pre-1.0 caveat.
4. Move agent-centered language after workflow control has been established.
5. Add a short "What this means for agents" paragraph.

## Workstream 3 — `.octon` AGENTS adapters

1. Preserve thin-adapter parity requirement.
2. Change "reliable agent execution" to "reliable workflow participation by bounded agents."
3. Add rule: workflow state owns control flow; agents do not.
4. Add rule: tools/connectors/Durable Objects/external systems are not permission or authority.

## Workstream 4 — ingress/bootstrap

1. Add boundary-aware operational language.
2. Preserve read order and manifest source-of-truth rule.
3. Clarify that agents execute inside admitted workflow/run/harness boundaries.

## Workstream 5 — terminology and architecture meta

1. Add Governed Workflow Runtime definition.
2. Add task-specific execution harness and bounded agent node definitions.
3. Mark "Governed Agent Runtime" as compatibility/alias during transition.
4. Add discouraged phrases.

## Workstream 6 — validation

1. Run proposal standard validation.
2. Run architecture proposal validation.
3. Run implementation-readiness validation.
4. Validate manifest completeness and checksums.
5. Validate no runtime scope creep.
6. Validate wording avoids overclaims.
