# Ingress Entry Artifact Edit Plan

_Status: In-review proposal packet artifact_


## Targets

- `.octon/instance/ingress/AGENTS.md`
- `.octon/instance/bootstrap/START.md`

## Proposed ingress addition

Add after the current opening paragraph:

```markdown
Octon's runtime posture is workflow-first: workflow state, run contracts, authorization, evidence, rollback posture, and closeout own consequential control flow. Agents participate only as bounded, evidenced activity nodes inside admitted execution boundaries.
```

## Proposed behavioral rule addition

```markdown
Agents may produce candidate artifacts, summaries, reviews, classifications, patches, repair suggestions, or exception recommendations. They may not authorize effects, own workflow state, schedule themselves indefinitely, mutate control truth, admit connectors, or close work.
```

## Proposed bootstrap addition

Add near the Authority Map:

```markdown
Use this boot sequence to bind the governed workflow surface first. Generated summaries, raw inputs, chat, model memory, host UI state, tool availability, MCP server availability, Durable Object state, and external workflow dashboards are not workflow authority.
```

## Validation

- Ingress manifest parity remains intact.
- Read order unchanged.
- No new runtime behavior.
