# Rejected Scope Items

_Status: In-review proposal packet artifact_


This packet rejects implementing the following now:

- workflow statechart schemas;
- task-specific execution harness schemas;
- `agent-node-v1`;
- workflow history/replay;
- idempotency/retry/compensation;
- connector operation admission;
- Durable Object adapter;
- MCP integration;
- external workflow-engine integration;
- runtime crate behavior changes;
- generated diagrams as authoritative surfaces.

Each may be addressed by a future packet after foundational framing is promoted.
