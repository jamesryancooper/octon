# Run Program Clean Delivery Runner Routing

This child packet plans proposal-program runner changes needed for autonomous
route selection, bounded retry, resume, and clean delivery handoff.

The packet is limited to the proposal-program runner and its lifecycle
contract, command, skill, and evidence boundaries. It does not own delivery
workflow mutation, generated publication, Change closeout, branch cleanup,
archive relocation, residue deletion, terminal proof, or any `cleaned` claim.
