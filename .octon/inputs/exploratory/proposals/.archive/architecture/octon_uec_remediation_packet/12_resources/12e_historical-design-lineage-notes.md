# Historical Design Lineage Notes

This packet treats prior proposal / design material as **design lineage and acceptance-delta context only**.

The live repo outranks historical packet language whenever they conflict. The remediation program therefore does **not** attempt to restore historical structures for their own sake. It only uses older packet intent where the live repo is silent or where the audited blocker set requires clarifying the original target-state promise.

Implications:
- The constitutional kernel under `/.octon/framework/constitution/**` is supreme.
- Historical bootstrap / objective surfaces remain lineage artifacts or shims unless the live registry explicitly elevates them.
- The goal is not to re-open architectural debates already settled by the live kernel; the goal is to finish the final consistency work the live kernel still requires.
