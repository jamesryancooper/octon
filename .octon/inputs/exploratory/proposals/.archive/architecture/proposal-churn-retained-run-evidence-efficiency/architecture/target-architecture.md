# Target Architecture

Retained evidence efficiency should use indexes and retention classification,
not deletion-by-path.

The target architecture:

- preserves retained evidence and control truth;
- builds retrieval indexes as derived aids;
- classifies stale/unreferenced files only through owning cleanup authority;
- proves reference integrity before any deletion;
- records negative controls showing generated indexes cannot replace retained proof.
