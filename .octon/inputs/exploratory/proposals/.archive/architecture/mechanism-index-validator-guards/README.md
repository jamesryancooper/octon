# Mechanism Index Validator Guards

This child proposal packet defines validation coverage for the governed
cross-surface mechanisms documentation architecture.

It focuses on negative controls: state/control not retained evidence, lifecycle
interaction receipts not authorization, feature catalog navigation-only
posture, generated-effective non-authority, operator read-model non-authority,
raw input non-authority, and parent evidence not satisfying child receipts.
