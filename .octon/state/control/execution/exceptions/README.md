# Exception Control Roots

`state/control/execution/exceptions/**` is the canonical live control family
for normalized `ExceptionLease` artifacts.

Wave 2 keeps the old root-level exception-lease file only as a compatibility
projection while runtime resolves this directory as the canonical authority
surface.
