# Closeout Worktree Run Log

- run_id: `closeout-worktree-20260521T173023Z`
- selected_candidate_id: `candidate-governed-branch-landing`
- delegated_change_ref: `evidence://runs/skills/closeout-change/governed-branch-landing-20260521T173023Z/change-receipt.json`
- final_report_ref: `.octon/state/evidence/validation/analysis/20260521T173023Z-closeout-worktree-report.yml`

## Summary

The wrapper inventoried the dirty worktree, selected the single coherent
governed branch landing implementation candidate, delegated it to singular
`closeout-change`, and re-inventoried afterward.

The delegated candidate was committed and pushed to
`origin/chore/change-closeout-state-machine` as
`191d360f1d30cf40a54991872920c0ba822b50ca`, authorized through retained
`branch-landing-authorization-v1` evidence, and fast-forward landed to
`origin/main`. Local `main` was synchronized to `origin/main`.

The default target was `cleaned`, but the actual outcome remains `landed` /
`continued` because branch cleanup deletion was rejected at the execution
approval boundary. Remaining publication/control run artifacts, prior closeout
evidence, current closeout evidence, and ignored local residue are retained or
foreign. The wrapper did not perform material route actions itself; commit,
push, hosted landing, and sync happened inside the singular `closeout-change`
delegation.
