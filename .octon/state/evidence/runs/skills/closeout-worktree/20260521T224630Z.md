# Closeout Worktree Run Log

run_id: closeout-worktree-20260521T224630Z
target: change-closeout-state-machine closeout blockers
direct_material_actions_performed: false

## Summary

The dirty worktree was inventoried and partitioned without mutation. `main`,
`HEAD`, and `origin/main` were aligned at
`a54673f88b341291d0bab4069e18b965d9e4f925`, so the blocker is worktree hygiene,
not branch synchronization.

The proposal packet hygiene classifier initially still reported
`worktree_hygiene_verdict: blocked` with
`worktree_hygiene_in_scope_path_count: 2`,
`worktree_hygiene_foreign_path_count: 712`, and fingerprint
`sha256:980ac6fc609ffad856f3fba5fca65c80d4e45bc01430026893bf5d536efa27e9`.
A re-run after this wrapper evidence was added reported
`worktree_hygiene_foreign_path_count: 714` with fingerprint
`sha256:b59b0084288ee59b5e6db08771c25f711d73bc2601196a1bdc33509e319ac74f`,
which is expected because the wrapper report and run log are themselves new
untracked retained evidence until the worktree residue is resolved.

The repo-hygiene local-run cleanup helper was run in dry-run summary mode and
reported `607` cleanup candidates, `28` protected referenced paths, and `43`
manual-review paths. Destructive `--confirm` cleanup was not performed.

## Disposition

The target proposal closeout remains blocked. The blocker cannot be fully
removed inside the proposal closeout route because that route forbids staging,
committing, deleting, resetting, archiving, or cleaning worktree paths.

Wrapper report:
`.octon/state/evidence/validation/analysis/20260521T224630Z-closeout-worktree-report.yml`
