# Unsupported Deny

- fixture: `unsupported-deny`
- tuple: `MT-B / WT-4 / LT-REF / LOC-EN`
- adapters: `repo-shell` + `repo-local-governed`
- retained decision artifact:
  `/.octon/state/evidence/control/execution/authority-decision-uec-closure-unsupported-deny-20260330.yml`

The unsupported tuple remains denied by default and is retained here as the
negative proof that unsupported work does not silently widen into the certified
claim.

Result: PASS
