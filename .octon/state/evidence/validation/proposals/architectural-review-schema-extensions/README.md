# Child Promotion Evidence — Architectural Review Schema Extensions

Retained validation evidence for the phase-2 child `architectural-review-schema-extensions`
(parent program `architecture-review-method-suite-program`). All artifacts here are
retained evidence only; they carry no authority. Durable authority lives in the
promoted framework artifacts.

Paths below reference the packet by id only; the raw packet path is deliberately
kept out of this evidence root (the post-implementation drift validator scans each
promotion target — including this directory — for packet backreferences).

## Promotion targets landed

- `.octon/framework/constitution/contracts/assurance/architectural-review-report-v2.schema.json` (new)
- `.octon/framework/constitution/contracts/assurance/architectural-review-routing-decision-v2.schema.json` (new)
- `.octon/framework/constitution/contracts/assurance/README.md` (additive: two v2 entries)
- `.octon/framework/assurance/runtime/_ops/scripts/validate-architectural-review-receipts.sh` (additive: v2 awareness)
- `.octon/framework/assurance/runtime/_ops/fixtures/architectural-review/schema-extensions/` (positive + three negative-control + v1-coexistence fixtures)

## Evidence index (logs/)

| Log | Contents | Result |
| --- | --- | --- |
| `01-schema-wellformedness.txt` | `jq -e` parse of both v2 schemas | both well-formed JSON |
| `02-additive-superset-report.diff` | diff report-v1 → report-v2 | only `$id`, `title`, `schema_version` const, and the two additive required fields differ |
| `02-additive-superset-routing-decision.diff` | diff routing-decision-v1 → routing-decision-v2 | same four allowed change kinds only |
| `03-positive-control.txt` | validator over `pass/report-v2.yml` + `pass/routing-decision-v2.yml` | both `errors=0` |
| `04-negative-controls.txt` | NC-1 unknown_method, NC-2 undefined_lens, NC-3a/NC-3b receipt_schema_drift | all fail closed (non-zero exit, `errors=1`) |
| `05-v1-coexistence.txt` | validator over `coexistence-v1/report-v1.yml` + `routing-decision-v1.yml` | both validate `errors=0` without method/lenses |
| `06-binding-proofs.txt` | live naming.yml catalog slugs vs v2 method enums; live lens ids vs pass-fixture lenses | method enum == six catalog slugs; every pass lens id in the lens bank |
| `07-assurance-dir-status.txt` | `git status --porcelain` of the assurance contracts dir | only README modified + two v2 schemas new; v1 report/routing-decision/support-receipt schemas absent (byte-for-byte unchanged) |
| `07-readme-schema-entries.txt` | README schema list | v1 entries retained, v2 entries added beside them |
| `08-no-regression-suite.txt` | full `validate-architectural-review-{naming,routing,lens-references,workflows,lifecycle-gates,extension-split,skills-commands}.sh` | all seven `errors=0` |
| `09-review-gate-rerun.txt` | `validate-proposal-review-gate.sh --require-implementation-authorization` | `errors=0 warnings=0` (support-receipt path preserved end-to-end) |
| `11-support-receipt-no-regression.txt` | receipts validator over the existing `valid-pre-integration-receipt.yml` (must pass) and `invalid-placeholder-receipt.yml` (must fail) | valid passes `errors=0`; placeholder fails closed `errors=1` |

## Verification evidence

- `verification/verification-pass-1-receipt.md` — sanitized closure receipt for the
  `run-packet-verification-and-correction-loop` route (run
  `20260709-arms-program-clean-delivery-04-architectural-review-schema-extensions`).
  Verdict `clean`, 0 unresolved findings, terminal state `clean`; every AC re-verified
  by executing the live validators (no support artifact accepted as proof of its own
  claim). Raw transcript retained under
  `runs/skills/octon-proposal-lifecycle-run-packet-verification-and-correction-loop/`.

## Acceptance-criteria mapping

- **AC-1 / AC-2** — v2 report + routing-decision schemas authored as strict additive
  supersets → `01`, `02-*`.
- **AC-3** — method enum bound to the phase-1 naming catalog; NC-1 `unknown_method`
  fails closed → `06`, `04`.
- **AC-4** — lenses bound to the phase-0 lens bank; NC-2 `undefined_lens` fails closed
  → `06`, `04`.
- **AC-5** — support-receipt schema byte-for-byte unchanged and guarded; NC-3
  `receipt_schema_drift` fails closed (both triggers) → `07-assurance-dir-status.txt`,
  `04`.
- **AC-6** — validator passes on `pass/` and fails on all three negative controls →
  `03`, `04`.
- **AC-7** — v1 coexistence preserved → `05`, `07-assurance-dir-status.txt`.
- **AC-8** — README lists the two v2 schemas without rewriting existing entries →
  `07-readme-schema-entries.txt`.
- **AC-9** — additive-only, no regression, no authority granted → `02-*`,
  `08`, `09`, `11`.
- **AC-10** — evidence retained here.

## Execution transparency

The logs were generated by direct, individually executed commands (the session
sandbox restricts piped/compound and non-allowlisted script invocations). The
receipts-validator test harness `test-architectural-review-validators.sh` under
`_ops/tests/` is not runnable in this session's sandbox; its two receipt-validator
assertions are exercised directly in `11-support-receipt-no-regression.txt`, and the
same support-receipt path is exercised end-to-end by the review-gate re-run in
`09-review-gate-rerun.txt`. No durable change was made to that test harness (it is
outside this child's promotion targets).
