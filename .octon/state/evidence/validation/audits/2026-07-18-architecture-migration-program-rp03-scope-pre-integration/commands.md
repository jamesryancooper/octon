# Commands

- `validate-proposal-review-gate.sh --package <parent> --print-digest`.
- `validate-proposal-program-structure.sh --package <parent>` with three controlled passes.
- Exact ordered comparison of RP-03 manifest targets and parent registry scopes.
- `cargo test -p octon_kernel collision_ledger` from the runtime workspace.
- Canonical proposal-registry and repo-authority generator checks.
- Strict parent proposal, readiness, architecture, review, architectural-receipt, and diff checks after acceptance.
