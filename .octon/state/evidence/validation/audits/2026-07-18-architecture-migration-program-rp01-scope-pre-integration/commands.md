# Commands

- `validate-proposal-review-gate.sh --package <parent> --print-digest`.
- `validate-proposal-program-structure.sh --package <parent>` with controlled
  seeds 17, 29, and 43.
- `cargo test -p octon_kernel collision_ledger` from the runtime crate workspace.
- Exact ordered-list comparison of RP-01 manifest targets and registry scopes.
- `generate-proposal-registry.sh --check` in projection-only mode.
- `generate-repo-authority-write-scope-index.sh --check`.
- Receipt-atomic parent standard, readiness, architecture, review, and strict
  architectural-receipt validation after acceptance materialization.
- `git diff --check` and exact path/symbol ownership review.
