# Commands

- Freeze digest and compare child/parent target counts.
- Run proposal-standard, readiness, and architecture validators.
- Inspect all packet files, canonical reconciliation, current broker/effect/credential surfaces, Cargo metadata, and macOS SDK headers.
- Run three architecture-lens passes and `git diff --check`.
