# Commands

- packet and parent file census with `rg`, `sed`, and YAML parsing
- proposal review digest and structural validators
- current lifecycle-executor dispatch/spawn symbol census
- exact ordered parent/child scope comparison
