# Commands

- `rg --files`, `find`, and `wc -l` over the complete RP-01 packet.
- `validate-proposal-review-gate.sh --print-digest` at the stable packet.
- Three controlled `validate-proposal-standard.sh` passes.
- `validate-proposal-implementation-readiness.sh` and
  `validate-architecture-proposal.sh`.
- `validate-proposal-program-structure.sh` for the parent collision graph.
- Ordered `yq` extraction plus `diff` for child and parent target equality.
- Targeted source inspection of all four candidate spawn helpers and the
  non-candidate dynamic utility launchers.
- Receipt-atomic strict review and architecture-receipt validation after
  acceptance materialization.
- Canonical generated discovery projection write/check and `git diff --check`.
