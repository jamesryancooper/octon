# Profile Selection Receipt

- `release_state`: `pre-1.0`
- `change_profile`: `atomic`
- `cutover_mode`: `bounded-hardening-recertification`
- `selection_rationale`: The hardening packet preserves one bounded live model,
  repo ingress defaults `pre-1.0` work to `atomic`, and support widening
  remains frozen during recertification.

