# Phase 6 Simplification Decisions

- Keep `framework/constitution/**`, ingress, and the orchestrator contract as
  the only required agency execution path.
- Keep `DELEGATION.md` and `MEMORY.md` as supporting overlays, not peer
  constitutional layers.
- Keep `framework/agency/governance/CONSTITUTION.md` only as a historical shim.
- Delete active and scaffolded `SOUL.md` files instead of preserving them as
  default overlays.
- Delete autonomy/AI-gate label mirrors from live GitHub workflows so required
  checks and canonical control artifacts carry the merge/gate posture.
