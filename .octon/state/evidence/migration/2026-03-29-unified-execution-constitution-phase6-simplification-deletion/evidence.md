# Unified Execution Constitution Phase 6 Evidence

This bundle captures the Phase 6 simplification and deletion pass.

Key proof points:

- the orchestrator contract is now the clear kernel execution profile beneath
  the constitutional kernel and ingress
- active and scaffolded `SOUL.md` overlays are deleted from runtime agent paths
- agency `CONSTITUTION.md` is explicitly historical and outside the required
  path
- autonomy/AI-gate label mirrors are removed from live GitHub control-plane
  flows
- validators and CI now enforce the simplified orchestrator-first path
