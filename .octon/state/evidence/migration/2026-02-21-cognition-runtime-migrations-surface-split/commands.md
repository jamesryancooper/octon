# Commands

Migration command receipts for `2026-02-21-cognition-runtime-migrations-surface-split`.

Legacy migrations converted from single-file evidence format may retain command blocks in `evidence.md` only.
For new migrations, capture exact command invocations here.
