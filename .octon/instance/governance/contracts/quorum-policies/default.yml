schema_version: authority-quorum-policy-v1
policy_id: default
policy_ref: ".octon/instance/governance/contracts/quorum-policies/default.yml"
quorum_levels:
  default:
    required: 1
    rules:
      - "one explicit operator approval is required for approval-bearing authority routes"
