# Instance Execution-Role Runtime Overlays

This directory is reserved for repo-specific execution-role runtime overlays
that are valid only when covered by enabled framework overlay points.

Current repo-owned execution-role runtime overlays include:

- `tool-output-budgets.yml` for compact tool-output envelope sizing and raw
  payload offload requirements
