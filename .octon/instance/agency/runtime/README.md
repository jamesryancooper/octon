# Instance Agency Runtime Overlays

This directory is reserved for repo-specific agency runtime overlays that are
valid only when covered by enabled framework overlay points.

Current repo-owned agency runtime overlays include:

- `tool-output-budgets.yml` for compact tool-output envelope sizing and raw
  payload offload requirements
