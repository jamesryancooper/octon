# Unified Execution Constitution Global Completion Plan

## Profile Selection Receipt

- `release_state`: `pre-1.0`
- `change_profile`: `atomic`
- `atomic_mode`: `clean-break`
- `transitional_exception_note`: not applicable
- `selection_rationale`: ingress defaults pre-1.0 work to `atomic`, and this
  cutover must converge as one branch-local target state with no mixed live
  constitutional mode, bounded-envelope fallback, or liminal critical support
  surface after closeout

## Goal

Land the authored authority, runtime, disclosure, assurance, and retirement
surfaces required for Octon to truthfully claim a globally complete Unified
Execution Constitution across the retained finite support universe.

## Atomic Scope

1. Replace bounded-envelope support claims with an explicit final support
   universe and governance exclusions doctrine.
2. Promote retained host adapters, model adapters, locale/context classes, and
   capability packs to fully governed supported status.
3. Add explicit replay, telemetry, and runtime-bus service roots and bind final
   disclosure to runtime-backed evidence.
4. Re-open and then close certification with pass-1/pass-2 summaries, a final
   closure certificate, and a release HarnessCard that carries no bounded claim
   caveat.
5. Close out build-to-delete posture so no architecture-critical final claim
   depends on an active shim or mirror.

## Impact Map

- `framework/constitution/**`
- `framework/engine/runtime/**`
- `framework/capabilities/packs/**`
- `framework/assurance/**`
- `framework/lab/**`
- `framework/observability/**`
- `instance/charter/**`
- `instance/governance/**`
- `state/control/execution/runs/**`
- `state/evidence/**`

## Compliance Receipt

- exploratory packet inputs remain non-authoritative under `.octon/inputs/**`
- all live control, policy, runtime, disclosure, and certification truth lands
  only under `framework/**`, `instance/**`, and retained evidence under
  `state/**`
- no final disclosure or certificate depends on a narrower bounded-envelope
  wording

## Exceptions / Escalations

- repo-root ingress adapters remain allowed only as projection-only ingress
  compatibility surfaces until supported tooling no longer requires them
- unrelated user worktree changes under `.octon/inputs/exploratory/proposals/`
  remain preserved in place and were not reverted
