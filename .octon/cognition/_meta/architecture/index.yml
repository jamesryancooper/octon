# Cognition Architecture Index
# Machine-readable discovery index for architecture reference surfaces.

schema_version: "1.0"
files:
  - id: architecture-overview
    path: README.md
    summary: Canonical architecture reference and structure contracts.
    when: First entrypoint for harness architecture interpretation.

  - id: specification
    path: specification.md
    summary: Canonical architecture specification and contract definitions.
    when: When validating or changing architecture contracts.

  - id: bounded-surfaces-contract
    path: bounded-surfaces-contract.md
    summary: Cross-domain bounded-surface contract and migration posture.
    when: When evaluating topology and domain ownership changes.

  - id: runtime-vs-ops-contract
    path: runtime-vs-ops-contract.md
    summary: Canonical cross-domain rules for runtime and _ops artifact placement.
    when: When deciding whether an artifact belongs in runtime or _ops.

  - id: observability-requirements
    path: observability-requirements.md
    summary: Observability policy and correlation expectations.
    when: When changing telemetry or operational diagnostics contracts.

  - id: architecture-resources
    path: resources.md
    summary: Curated architecture reference corpus.
    when: When researching external patterns and design references.

  - id: cognition-discovery-index-model
    path: discovery-index-model.md
    summary: Finalized cognition discovery architecture and sidecar index contract.
    when: When changing documentation discovery structure or index conventions.

  - id: artifact-surface-overview
    path: artifact-surface/README.md
    summary: Optional artifact-surface architecture overview and scope.
    when: When evaluating optional artifact publication architecture posture.

  - id: artifact-surface-index
    path: artifact-surface/index.yml
    summary: Machine-readable discovery index for artifact-surface architecture docs.
    when: For targeted artifact-surface document lookup.

  - id: architecture-readme-sections
    path: README.index.yml
    summary: Sidecar section index for architecture README content.
    when: For targeted discovery within architecture overview content.

  - id: architecture-resources-sections
    path: resources.index.yml
    summary: Sidecar section index for architecture resources content.
    when: For targeted discovery within architecture resource corpus.

  - id: cognition-discovery-docs-index
    path: ../docs/index.yml
    summary: Discovery docs index including the cognition intent-to-surface atlas.
    when: When routing by intent across cognition runtime, governance, and practices.

  - id: cognition-intent-surface-atlas
    path: ../docs/intent-surface-atlas.md
    summary: Intent-to-surface lookup map for common cognition tasks.
    when: When unsure which cognition surface owns an artifact or action.
