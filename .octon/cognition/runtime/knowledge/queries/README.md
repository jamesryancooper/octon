# Runtime Knowledge Queries

Canonical query contracts for runtime knowledge graph traversal.

## Purpose

- Keep query intent and parameter contracts explicit and discoverable.
- Separate reusable query definitions from query execution results.
- Provide a stable interface for agents and scripts using runtime knowledge data.

## Canonical Index

- `index.yml` - machine-readable discovery index for query artifacts.
