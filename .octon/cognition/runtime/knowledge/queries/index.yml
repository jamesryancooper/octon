# Runtime Knowledge Queries Index
# Machine-readable discovery index for query contract artifacts.

schema_version: "1.0"
files:
  - id: queries-overview
    path: README.md
    summary: Runtime query surface orientation and ownership boundaries.
    when: First entrypoint for runtime query discovery.

  - id: query-catalog
    path: query-catalog.yml
    summary: Canonical catalog of named knowledge graph query contracts.
    when: When invoking deterministic knowledge queries from scripts or agents.
