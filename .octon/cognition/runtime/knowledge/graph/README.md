# Runtime Knowledge Graph

Materialized runtime graph datasets for the cognition knowledge contract.

## Purpose

- Keep machine-readable node and edge snapshots under runtime ownership.
- Make traceability queries deterministic in local repository state.
- Decouple graph data from narrative contract docs.

## Canonical Index

- `index.yml` - machine-readable discovery index for graph artifacts.

## Generation

- Generated by: `/.octon/cognition/_ops/runtime/scripts/sync-runtime-artifacts.sh`
- Artifacts:
  - `nodes.yml`
  - `edges.yml`
