# Runtime Projection Definitions

Canonical contracts for runtime projection assembly.

## Purpose

- Declare projection IDs, source indexes, and refresh semantics.
- Keep materialized read models reproducible from explicit definitions.
- Support change-impact review when source contracts evolve.

## Canonical Index

- `index.yml` - machine-readable discovery index for projection definitions.
