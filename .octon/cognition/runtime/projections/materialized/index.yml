# Runtime Materialized Projections Index
# Machine-readable discovery index for generated projection read models.

schema_version: "1.0"
files:
  - id: materialized-overview
    path: README.md
    summary: Materialized projection surface orientation and freshness expectations.
    when: First entrypoint for materialized projection discovery.

  - id: cognition-runtime-surface-map-latest
    path: cognition-runtime-surface-map.latest.yml
    summary: Latest materialized runtime surface map snapshot.
    when: When querying normalized runtime surface inventory in one file.
