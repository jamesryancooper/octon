# Adapter Evaluations

`framework/lab/adapter-evaluations/**` contains authored lab evaluation records
for replaceable adapter candidates.

These records are boundary and behavior proof inputs only. They do not admit
live support, authorize execution, store control truth, retain canonical
evidence, or make external adapter state authoritative.
