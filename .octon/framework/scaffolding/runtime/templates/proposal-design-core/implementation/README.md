# Implementation

This directory translates the design package into implementer-facing execution
guidance.

Required outputs:

- `minimal-implementation-blueprint.md`
- `first-implementation-plan.md`

These files may begin as placeholders while the package is in `draft` status,
but they must exist from the start so the package keeps a direct path from
design to build.
