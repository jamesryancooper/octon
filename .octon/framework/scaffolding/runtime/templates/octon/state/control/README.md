# State Control

Mutable current-state truth lives under `state/control/**`.

Desired authored configuration remains under `instance/**`, and machine-readable
publication receipts live under `state/evidence/validation/publication/**`.
