---
schema_version: "{{OBJECTIVE_BRIEF_SCHEMA_VERSION}}"
objective_id: "{{OBJECTIVE_ID}}"
intent_id: "{{INTENT_ID}}"
intent_version: "{{INTENT_VERSION}}"
owner: "{{OBJECTIVE_OWNER}}"
approved_by: "{{OBJECTIVE_APPROVED_BY}}"
generated_at: "{{GENERATED_AT}}"
---

# Objective: {{OBJECTIVE_LABEL}}

## Workspace Goal

Use Octon in `{{REPO_NAME}}` as a governed autonomous workspace for mixed or custom objectives where no narrower common pack fits yet.

## What Octon Should Optimize For

- explicit scope, safe execution, and clear evidence for each material change
- fast movement on the next meaningful task without losing governance integrity
- easy evolution from a broad objective to a sharper repo-specific contract later

## In Scope

- planning, implementation, documentation, audits, and maintenance work inside this workspace
- repo-local improvements that clearly support the active objective
- refinement of this objective contract as the workspace intent becomes clearer

## Out of Scope

- widening authority beyond this workspace without explicit approval
- destructive or irreversible actions without the required governance path
- ambiguous work that has no clear contribution to the active objective

## Success Signals

- the workspace has a usable starting objective and machine-readable intent binding
- work stays attributable, bounded, and reviewable as the objective sharpens
- future objective updates can specialize this contract without reworking bootstrap

## Initial Focus

- refine the repo-specific objective after the first real task is understood
- identify the highest-priority safe next step inside the workspace
- keep the objective brief and intent contract aligned as the workspace evolves
