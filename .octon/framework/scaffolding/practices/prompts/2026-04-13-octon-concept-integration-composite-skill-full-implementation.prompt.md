---
title: Octon Concept Integration Composite Skill Full Implementation Prompt
description: Execution-grade prompt for fully implementing the extension-pack composite skill architecture proposal against the live Octon repository.
---

You are the principal Octon extension-pack, composite-skill, and
proposal-packet productization engineer for this repository.

Your job is to fully implement the architecture proposal at:

`/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/`

Treat this as a real implementation, publication, validation, and closeout
program. Do not treat it as a design review, prose rewrite, packet summary,
or partial planning exercise.

The proposal packet lives under `inputs/**` and is non-authoritative. Use it
as the execution specification only.

Promote durable outcomes into the correct live surfaces by class:

- raw reusable pack payload under
  `/.octon/inputs/additive/extensions/octon-concept-integration/**`
- repo-owned desired activation and operator discovery under `/.octon/instance/**`
- retained evidence and optional checkpoints under `/.octon/state/**`
- generated runtime-facing publication only through the canonical publication
  scripts into `/.octon/generated/effective/**`

Do not satisfy the work by editing only the proposal packet. Do not hand-edit
generated publication outputs.

## Working doctrine

1. The landing is an additive extension pack, not a repo-native skill.
2. The composite skill is the reusable execution core.
3. The thin command wrapper is the preferred v1 operator entrypoint.
4. Pack-local prompt assets are the only supported runtime prompt source for
   this capability.
5. Proposal packets generated by the capability remain temporary and
   non-authoritative under `inputs/exploratory/proposals/**`.
6. Live repo truth outranks stale packet assumptions.

## Required reading order

Read these before planning or implementation:

1. `AGENTS.md`
2. `/.octon/instance/ingress/AGENTS.md`
3. `/.octon/README.md`
4. `/.octon/framework/constitution/CHARTER.md`
5. `/.octon/framework/constitution/charter.yml`
6. `/.octon/framework/constitution/obligations/fail-closed.yml`
7. `/.octon/framework/constitution/obligations/evidence.yml`
8. `/.octon/framework/constitution/precedence/normative.yml`
9. `/.octon/framework/constitution/precedence/epistemic.yml`
10. `/.octon/framework/constitution/ownership/roles.yml`
11. `/.octon/framework/constitution/contracts/registry.yml`
12. `/.octon/instance/charter/workspace.md`
13. `/.octon/instance/charter/workspace.yml`
14. `/.octon/framework/agency/runtime/agents/orchestrator/AGENT.md`
15. `/.octon/framework/cognition/_meta/architecture/specification.md`
16. `/.octon/framework/cognition/_meta/architecture/inputs/additive/extensions/README.md`
17. `/.octon/framework/engine/governance/extensions/README.md`
18. `/.octon/framework/engine/governance/extensions/trust-and-compatibility.md`
19. `/.octon/framework/cognition/_meta/architecture/generated/effective/extensions/README.md`
20. `/.octon/framework/capabilities/README.md`
21. `/.octon/framework/capabilities/_meta/architecture/comparison.md`
22. `/.octon/framework/capabilities/runtime/skills/composite-skills.md`
23. `/.octon/instance/extensions.yml`
24. `/.octon/instance/bootstrap/catalog.md`
25. `/.octon/generated/effective/extensions/catalog.effective.yml`
26. `/.octon/generated/effective/capabilities/routing.effective.yml`
27. `/.octon/inputs/additive/extensions/octon-concept-integration/prompts/README.md`
28. `/.octon/inputs/additive/extensions/octon-concept-integration/prompts/prompt-set-current-state-alignment-and-conflict-audit.md`
29. `/.octon/inputs/additive/extensions/octon-concept-integration/prompts/proposal-packet-executable-implementation-prompt-generator.md`
30. `/.octon/inputs/additive/extensions/octon-concept-integration/prompts/proposal-packet-implementation-and-closeout.md`
31. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/README.md`
32. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/proposal.yml`
33. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/architecture-proposal.yml`
34. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/navigation/source-of-truth-map.md`
35. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/resources/current-state-observations.md`
36. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/architecture/target-architecture.md`
37. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/architecture/current-state-gap-map.md`
38. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/architecture/file-change-map.md`
39. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/architecture/validation-plan.md`
40. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/architecture/acceptance-criteria.md`
41. `/.octon/inputs/exploratory/proposals/architecture/octon-concept-integration-composite-skill/architecture/implementation-plan.md`

## Profile Selection Receipt

Record and follow this profile before implementation:

- `release_state`: `pre-1.0`
- `change_profile`: `atomic`
- `selection_rationale`: this packet introduces a new additive pack and
  invocation surface without support-target widening, without a new runtime
  root, and without justification for a transitional dual-truth model
- `transitional_exception_note`: not applicable unless a true hard blocker
  forces temporary coexistence or temporary dual-source prompt resolution

Emit a Profile Selection Receipt before implementation and retain it in your
execution evidence and output.

## Known repo and packet sensitivities

1. The runtime prompt source for this capability is now the pack-local prompt
   set under
   `/.octon/inputs/additive/extensions/octon-concept-integration/prompts/**`.
   Do not reintroduce any dependency on a root-level prompt-set copy.
2. Existing extension publication already carries `routing_exports` for command
   and skill contributions. Use the live publication path; do not invent a
   second extension routing model.
3. The current live publication model clearly surfaces extension command and
   skill routing candidates, but it does not obviously expose full extension
   skill registry metadata as a first-class effective surface. Preserve the
   packet's preferred v1 posture: composite skill as reusable core, thin
   command wrapper as preferred operator entrypoint.
4. `/.octon/instance/extensions.yml` currently seeds bundled packs off by
   default. Preserve that pattern unless live repo evidence and packet closure
   requirements justify a different final desired-state posture.
5. The packet contains a real ambiguity: it prefers a disabled-by-default
   final state but also asks for published routing proof. Resolve this
   explicitly. A bounded enable/publish/validate cycle is allowed for proof,
   but the final desired-state posture must be chosen intentionally and
   documented.
6. As of this prompt's creation, `generate-proposal-registry.sh --write` is
   blocked by an unrelated active proposal,
   `/.octon/inputs/exploratory/proposals/architecture/octon_instruction_layer_execution_envelope_hardening`,
   whose base manifest does not satisfy the `proposal-v1` contract. If this
   affects validation of generated sample proposal packets, record it as
   existing repo debt rather than attributing it to this capability unless you
   intentionally fix that unrelated blocker in the same branch.

## Core objective

Fully implement the concept-integration extension-pack capability so that the
live repository has a real reusable pack, a composite execution core, a stable
command entrypoint, and end-to-end proof that the landed capability can produce
validated proposal packets and packet-specific implementation prompts.

Completion means all of the following are true in substance, not only in
documentation:

1. A valid first-party bundled extension pack exists at
   `/.octon/inputs/additive/extensions/octon-concept-integration/`.
2. The current concept-integration prompt set is internalized into the pack's
   `prompts/` bucket.
3. A composite skill named `octon-concept-integration` exists in the pack and
   owns the bounded pipeline contract.
4. A thin command wrapper named `/octon-concept-integration` exists in the pack
   and is the preferred v1 operator entrypoint.
5. Repo-owned desired extension state and bootstrap discovery surfaces are
   updated where required.
6. Extension publication, capability routing, and host projections validate
   cleanly for the landed pack posture.
7. A bounded end-to-end run can generate:
   - extraction output
   - verification output
   - a manifest-governed architecture proposal packet
   - a packet-specific executable implementation prompt
8. No landed runtime path depends on the root `.prompts/**` location.
9. No generated effective surface is hand-authored to fake success.

## Architectural facts you must preserve

Assume and verify all of the following:

1. `framework/**` and `instance/**` remain the only authored authority roots.
2. `inputs/additive/extensions/**` is the correct raw reusable pack surface.
3. `instance/extensions.yml` remains the repo-owned desired activation surface.
4. Runtime and policy consumers must read extension publication only from
   `generated/effective/extensions/**`.
5. Runtime-facing capability discovery must continue to flow through
   `generated/effective/capabilities/**`.
6. Proposal packets remain non-canonical even when structurally valid.
7. No new `runtime/pipelines/` or parallel orchestration subsystem is allowed.

## Required implementation surfaces

Implement or update at minimum the following packet targets.

### 1. Pack shell and pack-local prompt assets

Create the pack root:

- `/.octon/inputs/additive/extensions/octon-concept-integration/pack.yml`
- `/.octon/inputs/additive/extensions/octon-concept-integration/README.md`
- `/.octon/inputs/additive/extensions/octon-concept-integration/prompts/**`
- `/.octon/inputs/additive/extensions/octon-concept-integration/context/octon-concept-integration-overview.md`
- `/.octon/inputs/additive/extensions/octon-concept-integration/validation/README.md`

Rules:

- Use `octon-extension-pack-v3`.
- Keep `origin_class: first_party_bundled`.
- Carry compatibility, provenance, and trust-hint fields exactly as the live
  pack contract requires.
- Copy or normalize the current root prompt-set assets into the pack. The
  landed pack must not depend on the root prompt path at runtime.

### 2. Composite skill implementation

Create:

- `/.octon/inputs/additive/extensions/octon-concept-integration/skills/manifest.fragment.yml`
- `/.octon/inputs/additive/extensions/octon-concept-integration/skills/registry.fragment.yml`
- `/.octon/inputs/additive/extensions/octon-concept-integration/skills/octon-concept-integration/SKILL.md`
- `/.octon/inputs/additive/extensions/octon-concept-integration/skills/octon-concept-integration/references/**`

The composite skill must own this bounded flow:

1. source intake and scope normalization
2. prompt-set alignment preflight when required
3. concept extraction
4. live-repo verification
5. selected-concept resolution
6. proposal packet generation
7. packet-specific executable implementation prompt generation
8. proposal-packet validation and evidence retention

Do not collapse the flow into a thin prose wrapper. Make it a real reusable
skill contract with explicit composition semantics where required.

### 3. Thin command wrapper

Create:

- `/.octon/inputs/additive/extensions/octon-concept-integration/commands/manifest.fragment.yml`
- `/.octon/inputs/additive/extensions/octon-concept-integration/commands/octon-concept-integration.md`

The command wrapper must:

- present a stable host-visible entrypoint
- gather or normalize operator arguments
- route execution into the composite skill
- avoid duplicating the full pipeline logic already owned by the skill

### 4. Repo-owned desired state and discovery updates

Update:

- `/.octon/instance/extensions.yml`
- `/.octon/instance/bootstrap/catalog.md`

If you need a temporary enablement cycle for validation, make it explicit.
Do not silently leave the pack enabled or disabled without documenting why that
final posture is correct.

### 5. Validation and publication path

Use the live publication path. At minimum run and evaluate:

- `bash .octon/framework/assurance/runtime/_ops/scripts/validate-extension-pack-contract.sh`
- `bash .octon/framework/orchestration/runtime/_ops/scripts/publish-extension-state.sh`
- `bash .octon/framework/assurance/runtime/_ops/scripts/validate-extension-publication-state.sh`
- `bash .octon/framework/capabilities/_ops/scripts/publish-capability-routing.sh`
- `bash .octon/framework/assurance/runtime/_ops/scripts/validate-capability-publication-state.sh`
- `bash .octon/framework/capabilities/_ops/scripts/publish-host-projections.sh`
- `bash .octon/framework/assurance/runtime/_ops/scripts/validate-host-projections.sh`

Do not hand-edit `generated/effective/extensions/**` or
`generated/effective/capabilities/**`.

### 6. End-to-end functional proof

Run a bounded sample execution of the landed capability.

If no explicit external source artifact is available for validation, use a
small inline validation fixture that is clearly labeled as test input and
retain it only in run evidence or checkpoint artifacts, not as durable repo
authority.

The proof run must show that the capability can produce:

- extraction output
- verification output
- a manifest-governed architecture proposal packet under
  `/.octon/inputs/exploratory/proposals/architecture/<proposal_id>/`
- a packet-specific executable implementation prompt as packet support output

Validate the generated packet with:

- `bash .octon/framework/assurance/runtime/_ops/scripts/validate-proposal-standard.sh --package <generated-packet-path>`
- `bash .octon/framework/assurance/runtime/_ops/scripts/validate-architecture-proposal.sh --package <generated-packet-path>`

If proposal-registry refresh fails because of the unrelated active proposal
blocker described above, record that explicitly and continue evaluating the
generated packet itself.

## Preferred Change Path

The default implementation target is:

1. pack-local prompt internalization
2. real composite skill contract
3. thin command wrapper
4. repo-owned desired state and catalog updates
5. publication and routing proof
6. end-to-end generated-packet proof

Preserve this as the default landing.

## Minimal Change Path

Use a narrower path only if live repo evidence proves the broader landing is
incorrect or currently blocked.

The only defensible v1 narrowing already anticipated by the packet is:

- do **not** generalize the effective extension publication model to expose a
  new first-class extension skill registry surface unless the existing routing
  model is insufficient for real operator use of the landed capability

This does **not** justify dropping:

- the composite skill
- the command wrapper
- the pack-local prompt assets
- or the end-to-end packet-generation proof

## Non-negotiable negative constraints

Do not do any of the following:

1. Do not implement this as a repo-native skill instead of an extension pack.
2. Do not implement this as a workflow-first subsystem instead of a composite
   skill with a thin command wrapper.
3. Do not leave a runtime dependency on the root `.prompts/` path.
4. Do not hand-author or directly patch `generated/effective/**` outputs to
   manufacture publication success.
5. Do not create a new capability-pack family, support-target tier, host
   adapter, model adapter, locale, or workload claim.
6. Do not treat proposal packets, generated views, or chat history as
   authority.
7. Do not mark the capability implemented if the landing is docs-only,
   placeholder-only, or packet-only.
8. Do not silently resolve the seeded-disabled versus publication-proof tension.
   Make the validation posture and final desired-state decision explicit.
9. Do not “solve” end-to-end proof by using the root prompt set directly after
   the pack exists.
10. Do not stop at analysis if implementation is feasible.

## Required evidence and closure burden

Retain or update evidence proving the landed capability is real.

At minimum capture:

- extension-pack contract validation results
- extension publication results
- capability-routing publication results
- host-projection publication or validation results
- bounded sample-run outputs and retained evidence
- proposal-packet validation results for the generated sample packet
- explicit note of any residual blocker, including the unrelated proposal
  registry generation blocker if still present

Closeout-ready means:

- the landed pack, command, and skill are real and publishable
- the capability can generate a standard-valid proposal packet and packet-
  specific implementation prompt
- the final desired-state posture is explicit and justified
- no unresolved blocker remains for the intended v1 operator path

## Required output structure

Your final output must include:

### 1. Orchestrator Decision

- goal
- plan
- delegations, if any
- verification approach
- immediate next step

### 2. Profile Selection Receipt

- `release_state`
- `change_profile`
- rationale
- any transitional exception note

### 3. Execution Summary

State:

- what packet inputs were used
- what scope was executed
- whether packet drift was detected
- what was implemented
- what final enablement posture was chosen for the pack
- and the resulting closeout status

### 4. Packet Drift Notes

List every material way the live current repository changed the packet's
assumptions, including:

- extension publication realities
- the seeded-disabled versus publication-proof ambiguity
- unrelated proposal-registry blocker effects
- any path changes or already-covered surfaces

### 5. Implementation Ledger

For each in-scope packet item, provide:

- concept or packet item
- execution status
- implemented target surfaces
- Preferred Change Path status
- Minimal Change Path usage, if any
- validation status
- evidence status
- residual blockers
- closeout impact

### 6. Validation And Proof

Summarize:

- which validators and publication scripts were run
- what passed
- what failed
- what could not be run
- what evidence was retained
- whether the bounded sample run successfully generated the expected packet and
  implementation prompt artifacts

### 7. File Change Map

List every durable repo artifact that changed, including raw additive pack
payload, instance-owned desired state, state evidence, and any intentionally
untouched generated outputs.

### 8. Residuals And Revisions

List anything that still needs:

- more implementation
- more validation
- a follow-on packet
- an explicit deferral
- or a separate fix for unrelated repo debt

### 9. Closeout Verdict

Conclude with explicit answers to:

- what was actually implemented
- whether the extension-pack capability is operational
- what remains blocked or deferred
- whether the executed scope is closeout-ready
- what evidence supports that verdict
- what the immediate next step should be

## Final instruction

Fully implement the packet against the live repository when feasible.

If a true hard blocker appears, stop at the correct boundary and say so
plainly. Do not stop at analysis if execution is feasible.

After any turn that changes files, ask exactly:

`Are you ready to closeout this branch?`
