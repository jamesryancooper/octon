---
title: Octon Bounded UEC Proposal Packet Full Implementation Prompt
description: Execution-grade prompt for fully implementing the bounded Unified Execution Constitution proposal packet and its incorporated audit without widening the admitted live support universe.
---

You are the principal repo-local bounded-claim hardening and closure engineer
for Octon.

Your job is to fully implement the proposal packet at:

`/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/`

Treat this as a real implementation, migration, validation, disclosure, and
certification program. Do not treat it as a design review, prose rewrite, or
partial analysis exercise.

The packet lives under `inputs/**` and is therefore non-authoritative. Use it
as the execution spec, but promote all durable outcomes only into canonical
authored authority, runtime, evidence, disclosure, and workflow surfaces
under:

- `/.octon/framework/**`
- `/.octon/instance/**`
- `/.octon/state/**`
- `/.github/workflows/**`

## Required reading order

Read these before planning or implementation:

1. `/.octon/instance/ingress/AGENTS.md`
2. `/.octon/framework/constitution/CHARTER.md`
3. `/.octon/framework/constitution/charter.yml`
4. `/.octon/framework/constitution/obligations/fail-closed.yml`
5. `/.octon/framework/constitution/obligations/evidence.yml`
6. `/.octon/framework/constitution/precedence/normative.yml`
7. `/.octon/framework/constitution/precedence/epistemic.yml`
8. `/.octon/framework/constitution/ownership/roles.yml`
9. `/.octon/framework/constitution/contracts/registry.yml`
10. `/.octon/instance/charter/workspace.md`
11. `/.octon/instance/charter/workspace.yml`
12. `/.octon/framework/agency/practices/commits.md`
13. `/.octon/framework/agency/practices/pull-request-standards.md`
14. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/README.md`
15. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/01_EXECUTIVE_FRAMING.md`
16. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/02_CURRENT_STATE_CONSTITUTIONAL_BASELINE.md`
17. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/03_TARGET_STATE_SPECIFICATION.md`
18. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/04_AUDIT_TO_REMEDIATION_TRACEABILITY_MATRIX.md`
19. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/05_REPOSITORY_CHANGE_MANIFEST.md`
20. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/06_IMPLEMENTATION_AND_MIGRATION_PROGRAM.md`
21. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/07_BIG_BANG_CUTOVER_PLAN.md`
22. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/08_VALIDATION_AND_VERIFICATION_PROGRAM.md`
23. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/09_CLOSURE_CERTIFICATION_PACKAGE.md`
24. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/10_FOLLOW_UP_HARDENING_AND_SUPPORT_WIDENING_GATES.md`
25. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/11_SOURCE_REGISTER.md`
26. `/.octon/inputs/exploratory/proposals/.archive/architecture/octon_bounded_uec_proposal_packet/12_FULL_IMPLEMENTATION_AUDIT_2026-04-09.md`

Use this precedence while executing:

1. The live repo and current canonical surfaces determine implementation
   reality.
2. The constitutional and claim-bearing surfaces under `framework/**`,
   `instance/**`, and live `state/**` define current authority and claim
   boundaries.
3. The latest explicit retained audit bundle remains the authoritative retained
   audit description of open findings.
4. `12_FULL_IMPLEMENTATION_AUDIT_2026-04-09.md` is the packet-contained
   interpretive audit confirming current bounded-claim honesty and sharpening
   remediation priorities.
5. The remaining packet files define the hardened target state, required
   artifacts, implementation phases, and closure gates.

## Profile Selection Receipt

Record and follow this profile before implementation:

- `release_state`: `pre-1.0`
- `change_profile`: `atomic`
- `selection_rationale`: repo ingress says `pre-1.0` defaults to `atomic`
  unless a hard gate requires `transitional`, and this packet preserves one
  live bounded constitutional model while closing hardening debt
- `transitional_exception_note`: not applicable; retained shims, projections,
  and mirrors may remain only as explicitly non-authoritative, disclosed, and
  retirement-tracked residue inside an otherwise atomic hardening program

Emit a Profile Selection Receipt in working notes before implementation.

## Current-state framing you must preserve

The packet-contained audit confirms that the current live claim is already
materially honest and supportable **as a bounded claim about the admitted live
support universe**. Your implementation must preserve that truth while
strengthening machine-verifiability, version coherence, evidence depth,
evaluator breadth, review-lineage freshness, and projection-boundary hygiene.

This is a hardening and normalization program, not a rescue rewrite.

## Core objective

Implement the packet so Octon can truthfully state:

> Octon materially substantiates a fully hardened, normalized,
> evidence-backed Unified Execution Constitution for the admitted live
> support universe.

Completion means all of the following are true in substance, not only in
prose:

1. The constitutional kernel remains singular and authored authority remains
   confined to `framework/**` and `instance/**`.
2. The live support claim remains bounded to the admitted live support
   universe and is never widened during the program.
3. All active claim-bearing artifacts stop using overbroad active wording such
   as `global-complete-finite` or active `full-attainment` semantics where a
   bounded claim is meant.
4. The current explicit audit findings are crosswalked into machine-readable
   live dispositions.
5. The stale 2026-04-06 review lineage is replaced by a fresh review packet
   aligned to the current admitted six-tuple universe.
6. Permanent non-authority surfaces are inventoried in one machine-readable
   register and removable claim-adjacent residue is maturely tracked in the
   retirement register.
7. Objective, runtime, and disclosure family version declarations match live
   artifact usage or explicitly govern coexistence with one active version.
8. Consequential admitted tuples have dossier sufficiency, naturalistic
   representative retained evidence, and at least one non-host-exclusive
   evaluator path.
9. Workflow-hosted approval and evaluator paths remain thin projection shells
   over canonically defined repo-local logic.
10. A new hardened bounded release bundle is generated, validated twice, and
    promoted only if the packet’s certification criteria are actually met.

## Delivery contract

You must satisfy all of the following:

1. Work on one branch only.
2. Continue through implementation, validation, disclosure regeneration, and
   release-close artifacts until the hardened bounded closure gate is either
   green or blocked by a true hard blocker.
3. Preserve the current bounded claim posture while hardening; do not widen
   support targets, admitted tuples, adapters, locales, contexts, or
   capability-pack admissions during this program.
4. Promote durable outcomes into canonical repo surfaces, not back into
   `inputs/**`.
5. Preserve strong surfaces that the audit already found materially correct.
6. If evidence is insufficient for the stronger hardened bounded target,
   tighten disclosure and stop promotion rather than inventing unsupported
   scope.
7. After any turn that changes files, ask exactly:
   `Are you ready to closeout this branch?`
8. Stop only for a true hard blocker:
   - missing authority to edit required paths
   - required destructive approval you do not have
   - invariant conflict that cannot be resolved locally without weakening
     claim honesty
   - unavailable external dependency required to produce mandatory evidence

## Non-negotiable negative constraints

Do not do any of the following:

1. Do not redesign or reopen the constitutional kernel.
2. Do not add a second authority regime, disclosure truth source, closure
   truth source, or support-truth source.
3. Do not widen `support-targets.yml` or admit new support tuples while this
   packet is open.
4. Do not treat exploratory inputs, generated/effective views, historical
   bundles, mirrors, shims, host labels, checks, comments, or workflow state
   as live authority.
5. Do not let GitHub-hosted or other host workflow shells become the sole
   durable definition of approval, evaluator, or closure semantics.
6. Do not preserve stale schema/version declarations as canonical-active when
   live artifacts have already standardized on successors.
7. Do not count safe-stage approval-only or lease-revocation-only exercises as
   the sole representative evidence for consequential tuples.
8. Do not weaken fail-closed behavior to make validators pass.
9. Do not rewrite historical evidence bundles to pretend they were always
   aligned; create new fresh evidence and repoint active lineage.
10. Do not stop at a plan, packet summary, or gap analysis when implementation
    can continue.

## Preserve unchanged by default

Preserve these surface families unless the packet explicitly calls for
targeted hardening, normalization, or disclosure updates inside them:

- `/.octon/framework/constitution/**`
- `/.octon/README.md`
- `/.octon/instance/charter/**`
- `/.octon/instance/orchestration/missions/**`
- `/.octon/state/control/execution/runs/**`
- `/.octon/state/control/execution/{approvals,exceptions,revocations}/**`
- `/.octon/state/continuity/**`
- `/.octon/state/evidence/runs/**`
- `/.octon/state/evidence/control/execution/**`
- `/.octon/state/evidence/disclosure/**`
- `/.octon/state/evidence/lab/**`
- `/.octon/framework/lab/**`
- `/.octon/framework/observability/**`
- `/.octon/framework/engine/runtime/adapters/{host,model}/**`
- `/.octon/framework/capabilities/packs/**`
- `/.octon/instance/governance/support-targets.yml`
- `/.octon/instance/governance/support-target-admissions/**`
- `/.octon/instance/governance/support-dossiers/**`

## Required authored additions and updates

Materialize or update the packet’s implementation surfaces inside existing
roots, including at minimum:

### Governance and contracts

- `/.octon/instance/governance/closure/current-audit-crosswalk.yml`
- `/.octon/instance/governance/non-authority-register.yml`
- `/.octon/instance/governance/contracts/non-authority-surface-policy.yml`
- `/.octon/instance/governance/contracts/projection-shell-boundary-policy.yml`
- `/.octon/instance/ingress/manifest.yml`
- `/.octon/instance/governance/contracts/support-dossier-evidence-depth.yml`
- `/.octon/instance/governance/contracts/evaluator-diversity.yml`
- `/.octon/instance/governance/contracts/hidden-check-breadth.yml`

### Validators and scripts

Add or update validator entry points under
`/.octon/framework/assurance/runtime/_ops/scripts/`, including:

- `validate-bounded-claim-nomenclature.sh`
- `validate-audit-crosswalk.sh`
- `validate-review-packet-freshness.sh`
- `validate-non-authority-register.sh`
- `validate-retirement-register-depth.sh`
- `validate-ingress-manifest-parity.sh`
- `validate-support-dossier-evidence-depth.sh`
- `validate-evaluator-diversity.sh`
- `validate-hidden-check-breadth.sh`
- `validate-contract-family-version-coherence.sh`
- `validate-projection-shell-boundaries.sh`

### Workflow surfaces

Update or thin as needed:

- `/.github/workflows/ai-review-gate.yml`
- `/.github/workflows/pr-autonomy-policy.yml`
- `/.github/workflows/architecture-conformance.yml`

### Active governance and disclosure sources

Update as needed:

- `/.octon/instance/governance/support-targets.yml`
- `/.octon/instance/governance/disclosure/harness-card.yml`
- `/.octon/instance/governance/disclosure/release-lineage.yml`
- `/.octon/instance/governance/closure/unified-execution-constitution.yml`
- `/.octon/instance/governance/closure/unified-execution-constitution-status.yml`
- `/.octon/instance/governance/contracts/closeout-reviews.yml`
- `/.octon/instance/governance/retirement-register.yml`
- `/.octon/framework/constitution/claim-truth-conditions.yml`
- `/.octon/framework/constitution/contracts/registry.yml`
- `/.octon/framework/constitution/contracts/objective/README.md`
- `/.octon/framework/constitution/contracts/authority/README.md`
- `/.octon/framework/constitution/contracts/runtime/README.md`
- `/.octon/framework/constitution/contracts/assurance/README.md`
- `/.octon/framework/constitution/contracts/disclosure/README.md`
- `/.octon/generated/effective/closure/claim-status.yml`
- `/.octon/generated/effective/closure/recertification-status.yml`
- `/.octon/generated/effective/governance/support-target-matrix.yml`
- all live admitted support dossiers under
  `/.octon/instance/governance/support-dossiers/**/dossier.yml`

## Required evidence and release outputs

Produce and maintain all of the following while executing:

1. One fresh build-to-delete packet aligned to the current admitted universe.
2. One fresh bounded hardening audit bundle aligned to the current repo state.
3. One hardened bounded release id minted exactly once and reused
   consistently.
4. One release closure root:
   `/.octon/state/evidence/disclosure/releases/<next-release>/closure/`
5. Required release-close artifacts, including at minimum:
   - `gate-status.yml`
   - `closure-summary.yml`
   - `closure-certificate.yml`
   - `recertification-status.yml`
   - `support-universe-coverage.yml`
   - `support-universe-evidence-depth-report.yml`
   - `proof-plane-coverage.yml`
   - `evaluator-diversity-report.yml`
   - `hidden-check-breadth-report.yml`
   - `review-packet-freshness-report.yml`
   - `non-authority-surface-report.yml`
   - `contract-family-normalization-report.yml`
   - `contract-version-coherence-report.yml`
   - `audit-resolution-report.yml`
   - `projection-shell-boundary-report.yml`
   - `support-widening-blockers.yml`
   - `cross-artifact-consistency.yml`
   - `projection-parity-report.yml`
   - `host-authority-purity-report.yml`
   - `runtime-family-depth-report.yml`
   - `continuity-linkage-report.yml`
   - `disclosure-calibration-report.yml`
   - `retirement-rationale-report.yml`
   - `residual-ledger.yml`
   - `ablation-review-report.yml`
6. Updated authored disclosure sources and generated/effective parity outputs.

## Execution phases

Execute these phases in order. Do not pause between phases unless blocked by a
true hard blocker.

### Phase 0 — freeze and baseline

Primary intent:
freeze the support universe, record the current state honestly, and establish
the execution baseline from live authority plus the packet-contained audit.

Required work:

- freeze support-target amendments and new admissions for the duration of the
  cutover program
- snapshot the active support-target digest, active release bundle, latest
  retained explicit audit bundle, and packet-contained full implementation
  audit
- open the cutover branch
- record a Profile Selection Receipt

Exit gate:

- support scope frozen
- baseline receipts recorded
- branch opened

### Phase 1 — claim-discipline normalization

Primary intent:
eliminate active wording that implies unsupported universality.

Required work:

- introduce one bounded machine enum for the admitted live support universe
- update active claim-bearing artifacts to use bounded wording only
- replace active `full-attainment` semantics where a bounded claim is meant
- preserve historical releases as historical lineage instead of rewriting them

Exit gate:

- no active claim-bearing artifact uses `global-complete-finite` or equivalent
  universalizing active wording

### Phase 2 — audit and review-lineage normalization

Primary intent:
make the retained audit trail, active support universe, and active review
lineage line up.

Required work:

- add `current-audit-crosswalk.yml`
- issue a fresh build-to-delete packet aligned to the admitted universe
- update closeout review pointers and fresh ablation review linkage
- generate `review-packet-freshness-report.yml`
- explicitly trace the packet-contained full implementation audit
  recommendations into implementation work or closure rationale

Exit gate:

- each current audit finding has a machine-readable live disposition
- latest review packet is fresher than the last support-universe change
- active lineage no longer points to stale pre-widening review evidence

### Phase 3 — authority, non-authority, and version normalization

Primary intent:
centralize non-authority rules and remove family-version ambiguity.

Required work:

- add the non-authority surface policy and non-authority register
- deepen retirement-register rationale and review linkage
- normalize registry and family README compatibility/non-authority sections
- align objective/runtime/disclosure family declarations with live active
  versions or explicitly govern coexistence with one active version

Exit gate:

- every permanent non-authority surface is inventoried once
- every claim-adjacent retained removable surface has mature rationale
- no family README or registry entry names a stale version as canonical-active

### Phase 4 — ingress, agency, and projection-shell simplification

Primary intent:
reduce interpretive ambiguity and keep host/workflow surfaces thin.

Required work:

- add `instance/ingress/manifest.yml`
- rewrite ingress adapters to reference the manifest
- keep the default mandatory set to kernel + workspace + orchestrator
- add ingress parity validation
- add and enforce a projection-shell boundary policy
- move durable approval/evaluator semantics into repo-local canonical logic,
  leaving host workflows as thin orchestration/projection shells

Exit gate:

- ingress mandatory vs optional reads are machine-declared
- parity adapters validate
- no host workflow remains the sole durable definition of canonical approval or
  evaluator behavior

### Phase 5 — evidence depth and evaluator hardening

Primary intent:
close bounded-breadth hardening debt without widening the claim.

Required work:

- add support-dossier sufficiency contracts and evaluator/hidden-check
  contracts
- upgrade all live support dossiers with machine-readable sufficiency blocks
- generate additional retained runs where needed
- ensure each consequential tuple has at least one naturalistic current-release
  representative run
- ensure each consequential tuple has at least one non-host-exclusive
  evaluator path
- produce tuple-level and release-level depth/diversity/breadth reports

Exit gate:

- every admitted tuple meets retained-run and scenario minima
- consequential tuples have naturalistic representative evidence
- consequential tuples have at least one non-host-exclusive evaluator path

### Phase 6 — release bundle regeneration

Primary intent:
generate the new hardened bounded release from canonical sources.

Required work:

- generate the new release bundle under the hardened bounded release id
- generate all required closure reports
- regenerate effective projections
- generate a residual ledger that closes packet-scope retained items and leaves
  only future widening blockers or true out-of-scope non-critical work

Exit gate:

- full release bundle exists
- internal cross-artifact consistency is green

### Phase 7 — certification and promotion

Primary intent:
certify and atomically promote the new bounded hardened release.

Required work:

- run pass 1 validators on the frozen cutover branch
- run pass 2 validators from a clean checkout
- issue the new closure certificate only if both passes are clean
- update active release lineage and effective projections
- demote the previous active release to historical evidence only

Exit gate:

- zero unresolved packet-scope claim-critical items
- packet-scope hardening closures complete
- dual-pass clean
- active lineage promoted honestly

## Validation and done gate

You are done only when all of the following are true:

1. The packet’s claim-critical and packet-scope hardening rows are closed,
   superseded with evidence, or explicitly converted into future widening
   blockers outside packet scope.
2. The new active claim remains bounded to the admitted live support universe.
3. Every consequential tuple has sufficient current-release evidence.
4. Contract-version coherence and projection-shell boundary reports pass.
5. The release satisfies the certification package in
   `09_CLOSURE_CERTIFICATION_PACKAGE.md`.
6. Future widening remains explicitly blocked behind fresh audit, fresh review
   packet, refreshed dossiers, regenerated reports, and dual-pass
   recertification.

## Self-check before stopping

Before stopping, verify all of the following:

- you implemented the packet, not just summarized it
- you incorporated the full packet-contained audit in both planning and
  execution
- you did not widen support scope
- you did not weaken the current claim into vagueness or inflate it into
  universality
- you did not leave stale active version declarations in objective/runtime/
  disclosure families
- you did not leave host workflow surfaces as the sole durable approval or
  evaluator logic
- you did not leave consequential tuples dependent only on safe-stage exercise
  evidence
- you did not certify a release with stale review lineage or unresolved
  packet-scope blockers
