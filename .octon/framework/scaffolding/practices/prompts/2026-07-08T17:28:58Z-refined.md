# Refined Prompt: Resolve Current Octon GitHub Issue And PR

## Role

Act as a senior repository maintainer and CI/release closeout engineer for
`jamesryancooper/octon`. Be conservative, evidence-driven, and route work
through the repo's Change-first closeout model.

## Current Context To Re-Verify

At prompt refinement time on 2026-07-08, GitHub showed:

- Open issue: `#596`, "filesystem-interfaces runtime regression detected"
  - URL: https://github.com/jamesryancooper/octon/issues/596
  - Alert run: https://github.com/jamesryancooper/octon/actions/runs/28957482029
  - Observed failure: both Ubuntu and macOS failed at `Validate runtime
    registry and wasm package` with
    `CAPABILITY_DENIED: runtime-effective route bundle validation failed:
    CAPABILITY_DENIED: extensions catalog digest drift detected`.
- Open PR: `#595`, "chore(main): release octon 0.6.102"
  - URL: https://github.com/jamesryancooper/octon/pull/595
  - Release Please branch:
    `release-please--branches--main--components--octon`
  - State observed: open, non-draft, `mergeStateStatus: BLOCKED`.

Do not assume this state is still current. Start by re-checking open issues,
open PRs, PR checks, review threads, labels, and the latest relevant workflow
runs with GitHub.

## Objective

Review, resolve, and close out each currently open GitHub issue and pull
request for `jamesryancooper/octon` in the most efficient safe order.

Prioritize restoring main-branch runtime health before release closeout. If
issue `#596` and PR `#595` are still the only open items, resolve the runtime
regression first, then complete or explicitly supersede the release PR.

## Required Repo Guidance

Before material work, read and follow:

- `AGENTS.md`, then `.octon/instance/ingress/AGENTS.md`
- `.octon/framework/product/contracts/default-work-unit.yml`
- `.octon/framework/execution-roles/practices/standards/git-worktree-autonomy-contract.yml`
- `.octon/framework/execution-roles/practices/pull-request-standards.md`
- `.octon/framework/execution-roles/practices/commits.md`

For this pre-1.0 repository, default `change_profile` to `atomic` unless a hard
gate requires `transitional`. Select the Change route before git mutation and
record the required closeout evidence.

## Execution Plan

1. Inventory live state.
   - Confirm open issues and PRs.
   - Inspect issue bodies, PR bodies, labels, changed files, check status,
     review comments, unresolved review threads, and related workflow runs.
   - Confirm local branch, worktree cleanliness, and sync with `origin/main`.

2. Choose the smallest valid Change route.
   - Use `branch-pr` for work that starts from or must update an existing PR.
   - Use `branch-no-pr` or `direct-main` only if the repo policy and current
     branch protection/ruleset evidence allow it.
   - Preserve unrelated local changes and avoid broad staging.

3. Resolve the runtime-regression issue first.
   - Reproduce or inspect the failing `filesystem-interfaces-runtime` run.
   - Focus first on the observed failure:
     `.octon/framework/engine/runtime/run validate` denying the
     runtime-effective route bundle because of extensions catalog digest drift.
   - Identify the authoritative source and generated/effective artifacts that
     are out of sync. Do not paper over digest drift by weakening validation.
   - Apply the smallest fix that restores source/generated/effective
     consistency.
   - Validate locally at the relevant floor, including:
     - filesystem service Rust crate tests when touched
     - `.octon/framework/engine/runtime/run validate`
     - `.octon/framework/capabilities/runtime/services/_ops/scripts/validate-filesystem-interfaces.sh`
     - determinism, SLO, perf, and cross-OS checks when the touched paths or
       workflow scope require them
   - Push the fix through the selected route and verify the hosted workflow on
     the exact relevant SHA.
   - Let the workflow's `resolve-alert-on-success` path close the issue when
     possible. If the workflow is green but the issue remains open, comment
     with the green run evidence and close it manually.

4. Resolve the release PR.
   - Re-check PR `#595` after the runtime regression is resolved.
   - If it is still current and valid, bring it to a mergeable state without
     mixing unrelated fixes into the release PR unless Release Please or repo
     policy requires regeneration.
   - Ensure all required checks pass, the PR body/checklist satisfies the PR
     standard, no unresolved author-action review threads remain, and GitHub
     rulesets allow merge.
   - Merge only through the allowed protected-main path. If the release PR is
     stale or superseded by the regression fix or a regenerated release PR,
     close it with a concise comment linking the replacement evidence.

5. Close out.
   - Record Change receipt or PR closeout evidence required by the selected
     route.
   - Verify issue and PR terminal state: closed by recovery evidence, merged,
     or explicitly closed as superseded/blocked with evidence.
   - Fetch/sync after landing or merge and verify local `main`, `origin/main`,
     and landed refs align.
   - Clean only route-owned branches or residue that the closeout policy
     authorizes. Report any retained blockers or deferred cleanup explicitly.

## Negative Constraints

- Do not manually close the regression issue before a green recovery run or
  equivalent evidence proves the failure is resolved.
- Do not merge or request auto-merge for a release PR while the relevant
  main-branch runtime workflow is red or blocked.
- Do not treat GitHub labels, comments, host UI state, PR metadata, or workflow
  summaries as repo-local authority.
- Do not weaken validation, bypass digest checks, or convert generated outputs
  into source-of-truth.
- Do not mix unrelated refactors, dependency changes, formatting churn, or
  release artifact edits with the regression fix unless the chosen route
  requires it and the receipt explains why.
- Do not force-push, amend, rebase, delete branches, or delete worktrees unless
  the repo route and current safety evidence authorize that exact action.
- Do not claim closeout from a pushed branch, draft/open PR, failing checks, or
  local patch alone.

## Success Criteria

- Every open issue and PR present at start is either resolved or has a precise
  evidence-backed blocker and next route.
- Issue `#596`, if still present, is closed only after the
  `filesystem-interfaces-runtime` recovery evidence is green or an equivalent
  validated fix is documented.
- PR `#595`, if still present, is merged through the allowed path or closed as
  superseded/invalid with clear evidence.
- Required local and hosted validations are recorded.
- Change receipt, PR body, comments, and final report cite exact SHAs, runs,
  checks, and rollback/cleanup posture.
- The final worktree and branch state are either clean and synced or explicitly
  classified with retained/deferred residue.

## Final Report

Report:

- live issue/PR inventory at start and finish
- root cause and fix summary
- validation run locally and on GitHub
- terminal state for each issue and PR
- Change route, receipt/PR evidence, rollback handle, and cleanup status
- any remaining blockers or follow-up that could not be safely completed
