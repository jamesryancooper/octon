# Skills Registry (Extended Metadata + I/O Mappings)
# Full skill definitions for routing validation and execution.
# Agents load manifest.yml first, then this file after matching.
#
# Load Order:
#   1. manifest.yml      - Skill discovery index
#   2. capabilities.yml  - Capability schema (skill sets, valid capabilities, refs)
#   3. registry.yml      - Extended skill metadata + I/O mappings (this file)
#
# Single Source of Truth:
#   - SKILL.md frontmatter: name, description, allowed-tools (AUTHORITATIVE)
#   - capabilities.yml: skill_set_definitions, valid_capabilities, capability_refs
#   - This file: version, commands, parameters, requires, composition, io
#
# Fields:
#   version          - Semantic version string
#   commands         - Slash commands that invoke this skill
#   parameters       - Input parameters the skill accepts
#   requires.context - Context conditions for skill activation
#   composition      - Optional skill-local composition metadata
#   io.inputs        - Input paths (supports {{placeholders}})
#   io.outputs       - Output paths (supports {{placeholders}})
#
# Parameter Schema:
#   name        - Parameter identifier
#   type        - text | boolean | file | folder
#   required    - true | false
#   default     - Default value (if not required)
#   description - Human-readable description
#
# I/O Schema:
#   path         - File path relative to skills/ dir (supports {{placeholders}})
#   kind         - file | directory (default: file)
#   format       - markdown | json | yaml | text | binary
#   required     - true | false (for inputs, default: true)
#   determinism  - stable (same input = same output) | variable | unique
#   description  - Human-readable description
#
# Directory Structure:
# All operational categories follow the {category}/{skill-id}/ pattern:
#
#   /.octon/instance/capabilities/runtime/skills/configs/{skill-id}/       - Per-skill configuration overrides
#   /.octon/instance/capabilities/runtime/skills/resources/{skill-id}/     - Per-skill input resources (notes, docs, data)
#   /.octon/state/control/skills/checkpoints/{skill-id}/{run-id}/ - Execution state (checkpoints, manifests) for recovery
#   /.octon/state/evidence/runs/skills/{skill-id}/{run-id}.md - Execution logs
#
# Note: The pattern "use skill: {{id}}" is universal and does not need
# per-skill configuration. Agents recognize this pattern automatically.

schema_version: "4.0"

# Global routing rules
routing:
  # If true, only explicit /commands trigger skills (no trigger matching)
  explicit_command_required: false
  # How to handle ambiguous matches: "ask" | "first_match" | "most_specific"
  ambiguity_resolution: "ask"

# Extended skill metadata (keyed by id)
skills:
  synthesize-research:
    version: "1.0.1"
    commands:
      - /synthesize-research
    parameters:
      - name: research_folder
        type: folder
        required: true
        description: "Folder containing research notes (.md files)"
    requires:
      context: []
    io:
      inputs:
        - path: "/.octon/instance/capabilities/runtime/skills/resources/synthesize-research/projects/{{project}}/"
          kind: directory
          required: true
          description: "Project folder containing research notes"
        - path: "/.octon/instance/capabilities/runtime/skills/resources/synthesize-research/{{topic}}/"
          kind: directory
          required: false
          description: "Additional source materials by topic"
      outputs:
        - name: synthesis_document
          path: "/.octon/inputs/exploratory/drafts/{{topic}}-synthesis.md"
          kind: file
          format: markdown
          determinism: stable
          description: "Consolidated findings document with themes and evidence"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/synthesize-research/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log capturing inputs, outputs, and processing notes"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/synthesize-research/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all synthesize-research runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  refine-prompt:
    version: "2.1.1"
    commands:
      - /refine-prompt
    parameters:
      - name: raw_prompt
        type: text
        required: true
        description: "The raw prompt text to refine (inline or file path)"
      - name: execute
        type: boolean
        required: false
        default: false
        description: "Execute the refined prompt after saving"
      - name: context_depth
        type: text
        required: false
        default: "standard"
        description: "How deep to analyze repository context (minimal, standard, deep)"
      - name: skip_confirmation
        type: boolean
        required: false
        default: false
        description: "Skip intent confirmation step"
    requires:
      context: []
    io:
      inputs:
        - path: "/.octon/instance/capabilities/runtime/skills/resources/refine-prompt/prompts/"
          kind: directory
          required: false
          description: "Optional folder containing prompt source files"
      outputs:
        - name: refined_prompt
          path: "/.octon/framework/scaffolding/practices/prompts/{{timestamp}}-refined.md"
          kind: file
          format: markdown
          determinism: stable
          description: "The refined, improved prompt ready for execution"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/refine-prompt/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log with input, context analyzed, and output summary"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/refine-prompt/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all refine-prompt runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  refactor:
    version: "1.0.1"
    commands:
      - /refactor
    parameters:
      - name: scope
        type: text
        required: true
        description: "What to refactor: 'old-pattern → new-pattern'"
      - name: file_types
        type: text
        required: false
        default: "md,yml,yaml,json,ts,js"
        description: "Comma-separated file extensions to search"
      - name: dry_run
        type: boolean
        required: false
        default: false
        description: "Execute phases 1-3 only (audit and plan)"
      - name: exclusions
        type: text
        required: false
        default: "node_modules,.git,dist"
        description: "Comma-separated patterns to skip"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: refactor_artifacts
          path: "/.octon/state/control/skills/checkpoints/refactor/{{refactor_id}}/"
          kind: directory
          format: markdown
          determinism: unique
          description: "Refactor execution state (checkpoint, manifests, reports) for recovery"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/refactor/{{refactor_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this refactor run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/refactor/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all refactor runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-migration:
    version: "2.0.0"
    commands:
      - /audit-migration
    parameters:
      - name: manifest
        type: text
        required: true
        description: "Migration manifest (inline YAML or file path with old→new mappings)"
      - name: manifest_file
        type: file
        required: false
        description: "Optional manifest file name within /.octon/instance/capabilities/runtime/skills/configs/audit-migration/"
      - name: scope
        type: text
        required: false
        default: "."
        description: "Directory to audit (default: current directory)"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: structure_spec
        type: file
        required: false
        description: "Path to documented directory structure for optional structure diff layer"
      - name: template_dir
        type: folder
        required: false
        description: "Path to template directory for optional template smoke test layer"
      - name: partition
        type: text
        required: false
        description: "Partition label for parallel orchestration (e.g., 'docs-architecture')"
      - name: file_filter
        type: text
        required: false
        description: "Glob pattern to narrow scope within scope directory (e.g., '.octon/framework/cognition/_meta/architecture/**')"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs:
        - path: "/.octon/instance/capabilities/runtime/skills/configs/audit-migration/{{manifest_file}}"
          kind: file
          format: yaml
          required: false
          description: "Optional manifest file (alternative to inline YAML)"
      outputs:
        - name: audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-migration-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Structured findings report with severity tiers and fix batches"
        - name: partition_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-migration-audit-{{partition}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Partition-scoped findings report (when partition parameter is set)"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-migration/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-migration/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-migration runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-subsystem-health:
    version: "2.4.7"
    commands:
      - /audit-subsystem-health
    parameters:
      - name: subsystem
        type: folder
        required: true
        description: "Root directory of the subsystem to audit"
      - name: schema_ref
        type: file
        required: false
        description: "Path to schema file (e.g., capabilities.yml); auto-detected if not set"
      - name: docs
        type: folder
        required: false
        description: "Companion documentation directory for doc-to-source alignment checks"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: file_types
        type: text
        required: false
        default: "md,yml,yaml,json"
        description: "Comma-separated file extensions to include in scope"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: health_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-subsystem-health-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Structured findings report with severity tiers and fix batches"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-subsystem-health/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-subsystem-health/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-subsystem-health runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-cross-subsystem-coherence:
    version: "2.0.0"
    commands:
      - /audit-cross-subsystem-coherence
    parameters:
      - name: scope
        type: folder
        required: false
        default: ".octon"
        description: "Root directory to audit for cross-subsystem coherence"
      - name: subsystems
        type: text
        required: false
        default: "agency,capabilities,cognition,orchestration,quality,continuity,runtime"
        description: "Comma-separated subsystem list to include"
      - name: docs
        type: folder
        required: false
        default: ".octon/framework/cognition/_meta/architecture"
        description: "Companion architecture docs root for cross-reference checks"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: coherence_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-cross-subsystem-coherence-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Cross-subsystem coherence findings report with severity tiers and fix batches"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-cross-subsystem-coherence/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-cross-subsystem-coherence/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-cross-subsystem-coherence runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-freshness-and-supersession:
    version: "2.0.0"
    commands:
      - /audit-freshness-and-supersession
    parameters:
      - name: scope
        type: folder
        required: false
        default: ".octon"
        description: "Root directory to audit for staleness and supersession integrity"
      - name: artifact_globs
        type: text
        required: false
        default: "instance/cognition/context/shared/**/*.md,instance/cognition/decisions/**/*.md,/.octon/inputs/exploratory/plans/**/*.md,/.octon/state/evidence/validation/analysis/**/*.md"
        description: "Comma-separated globs selecting artifact families"
      - name: max_age_days
        type: text
        required: false
        default: "30"
        description: "Maximum artifact age before stale classification"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: freshness_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-freshness-and-supersession-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Freshness and supersession findings report with severity tiers and fix batches"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-freshness-and-supersession/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-freshness-and-supersession/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-freshness-and-supersession runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-documentation-standards:
    version: "2.0.0"
    commands:
      - /audit-documentation-standards
    parameters:
      - name: docs_root
        type: folder
        required: true
        description: "Root documentation directory to audit (for example docs)"
      - name: template_root
        type: folder
        required: false
        default: ".octon/framework/scaffolding/runtime/templates/docs/documentation-standards"
        description: "Canonical documentation template root"
      - name: policy_doc
        type: file
        required: false
        default: ".octon/framework/cognition/governance/principles/documentation-is-code.md"
        description: "Canonical docs-as-code policy document"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: docs_standards_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-documentation-standards-audit.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Structured docs standards findings report with severity tiers and fix batches"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-documentation-standards/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-documentation-standards/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-documentation-standards runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-domain-architecture:
    version: "2.0.0"
    commands:
      - /audit-domain-architecture
    parameters:
      - name: domain_path
        type: text
        required: true
        description: "Domain path or canonical .octon domain target to critique"
      - name: criteria
        type: text
        required: false
        default: "modularity,discoverability,coupling,operability,change-safety,testability"
        description: "Comma-separated external evaluation criteria"
      - name: evidence_depth
        type: text
        required: false
        default: "standard"
        description: "Evidence intensity: quick, standard, deep"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: domain_profiles_ref
        type: file
        required: false
        default: ".octon/framework/cognition/governance/domain-profiles.yml"
        description: "Domain profile registry used as baseline for prospective mode"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: domain_architecture_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-domain-architecture-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Independent architecture critique report with evidence-backed findings and recommendations"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-domain-architecture/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this architecture critique run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-domain-architecture/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-domain-architecture runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-surface-architecture:
    version: "1.0.1"
    commands:
      - /audit-surface-architecture
    parameters:
      - name: surface_path
        type: text
        required: true
        description: "Repo-relative path under /.octon/ identifying one durable surface unit"
      - name: surface_kind
        type: text
        required: false
        default: "auto"
        description: "Optional surface kind override: auto, workflow, skill, watcher, automation, service, contract, methodology, other"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: evidence_depth
        type: text
        required: false
        default: "standard"
        description: "Evidence intensity: quick, standard, deep"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: surface_architecture_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-surface-architecture-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Surface-architecture findings report with authority classification, gaps, and target-state recommendation"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings, coverage, convergence, and validation artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-surface-architecture/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this surface-architecture audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-surface-architecture/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-surface-architecture runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-architecture-readiness:
    version: "1.0.1"
    commands:
      - /audit-architecture-readiness
    parameters:
      - name: target_path
        type: folder
        required: true
        description: "Octon target path to audit: either .octon or one top-level bounded-surface domain"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: evidence_depth
        type: text
        required: false
        default: "standard"
        description: "Evidence intensity: quick, standard, deep"
      - name: domain_profiles_ref
        type: file
        required: false
        default: ".octon/framework/cognition/governance/domain-profiles.yml"
        description: "Domain profile registry used for target classification"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: architecture_readiness_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-architecture-readiness-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Architecture-readiness findings report with verdict, gaps, and remediation plan"
        - name: architecture_readiness_summary_json
          path: "/.octon/state/evidence/validation/analysis/{{date}}-architecture-readiness-audit-{{run_id}}.json"
          kind: file
          format: json
          determinism: unique
          description: "Machine-readable architecture-readiness summary"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings, coverage, convergence, and validation artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-architecture-readiness/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this architecture-readiness run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-architecture-readiness/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-architecture-readiness runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-charter:
    version: "1.0.1"
    commands:
      - /audit-charter
    parameters:
      - name: charter_path
        type: file
        required: true
        description: "Path to the charter markdown file to audit"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: high, medium, low, all"
      - name: include_rewrites
        type: boolean
        required: false
        default: true
        description: "Include exact rewrite guidance for material issues"
      - name: include_scores
        type: boolean
        required: false
        default: true
        description: "Include final numeric scorecard"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: charter_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-charter-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Closed-book charter audit with matrices, findings, rewrite pack, and scores"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-charter/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this charter audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-charter/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-charter runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-observability-coverage:
    version: "2.0.0"
    commands:
      - /audit-observability-coverage
    parameters:
      - name: scope
        type: folder
        required: true
        description: "Root directory containing service surfaces and observability artifacts to audit"
      - name: observability_contract_ref
        type: file
        required: false
        default: ".octon/framework/cognition/_meta/architecture/observability-requirements.md"
        description: "Optional observability contract reference used for expected signal and policy checks"
      - name: service_manifest_glob
        type: text
        required: false
        default: "**/SERVICE.md"
        description: "Glob pattern used to discover service manifests in scope"
      - name: slo_artifacts_glob
        type: text
        required: false
        default: "**/slo*.yml,**/slo*.yaml,**/slo*.md,**/contracts/slo-budgets.tsv"
        description: "Comma-separated globs selecting SLO and error-budget artifacts"
      - name: alert_artifacts_glob
        type: text
        required: false
        default: "**/*alert*.yml,**/*alert*.yaml,**/*burn-rate*.yml,**/*burn-rate*.yaml"
        description: "Comma-separated globs selecting alert policy artifacts"
      - name: runbook_artifacts_glob
        type: text
        required: false
        default: "**/runbook.md,**/*runbook*.md,**/operations/**/guide.md"
        description: "Comma-separated globs selecting operational runbook artifacts"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: observability_coverage_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-observability-coverage-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Observability coverage findings report with telemetry, SLO, alert, and runbook readiness gaps"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-observability-coverage/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this observability coverage audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-observability-coverage/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-observability-coverage runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-security-compliance:
    version: "2.0.0"
    commands:
      - /audit-security-compliance
    parameters:
      - name: scope
        type: folder
        required: true
        description: "Root directory containing security and compliance artifacts to audit"
      - name: policy_baseline_ref
        type: file
        required: false
        default: ".octon/framework/assurance/practices/standards/security-and-privacy.md"
        description: "Primary security-policy baseline reference used for policy intent checks"
      - name: control_baseline_ref
        type: file
        required: false
        default: ".octon/framework/cognition/practices/methodology/security-baseline.md"
        description: "Control baseline reference used for control and gate coverage checks"
      - name: policy_artifacts_glob
        type: text
        required: false
        default: "**/assurance/practices/standards/*.md,**/cognition/governance/principles/security-and-privacy-baseline.md"
        description: "Comma-separated globs selecting policy and principle artifacts"
      - name: secrets_artifacts_glob
        type: text
        required: false
        default: "**/services/governance/guard/**,**/services/governance/vault/**"
        description: "Comma-separated globs selecting secrets and sensitive-data safeguard artifacts"
      - name: access_control_artifacts_glob
        type: text
        required: false
        default: "**/services/governance/policy/**,**/policies/**/*.yml,**/policies/**/*.yaml"
        description: "Comma-separated globs selecting authorization and policy-enforcement artifacts"
      - name: dependency_artifacts_glob
        type: text
        required: false
        default: "**/*sbom*.json,**/*sbom*.md,**/*syft*.json,**/knowledge-plane/**"
        description: "Comma-separated globs selecting dependency and supply-chain evidence artifacts"
      - name: evidence_artifacts_glob
        type: text
        required: false
        default: "**/engine/_meta/evidence/*.md,**/continuity/runs/**/evidence/**"
        description: "Comma-separated globs selecting compliance evidence and run receipts"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: security_compliance_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-security-compliance-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Security and compliance findings report with policy, control, secrets, and evidence readiness gaps"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-security-compliance/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this security and compliance audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-security-compliance/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-security-compliance runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-data-governance:
    version: "2.0.0"
    commands:
      - /audit-data-governance
    parameters:
      - name: scope
        type: folder
        required: true
        description: "Root directory containing data-governance artifacts to audit"
      - name: classification_baseline_ref
        type: file
        required: false
        default: ".octon/framework/assurance/practices/standards/data-handling-and-retention.md"
        description: "Primary data-classification and retention baseline reference"
      - name: privacy_baseline_ref
        type: file
        required: false
        default: ".octon/framework/assurance/practices/standards/security-and-privacy.md"
        description: "Primary privacy and sensitive-data safeguards baseline reference"
      - name: classification_artifacts_glob
        type: text
        required: false
        default: "**/assurance/practices/standards/data-handling-and-retention.md,**/assurance/practices/standards/security-and-privacy.md"
        description: "Comma-separated globs selecting data classification and handling artifacts"
      - name: retention_artifacts_glob
        type: text
        required: false
        default: "**/assurance/practices/standards/data-handling-and-retention.md,**/agency/governance/MEMORY.md,**/continuity/_meta/architecture/runs-retention.md"
        description: "Comma-separated globs selecting retention and deletion policy artifacts"
      - name: lineage_artifacts_glob
        type: text
        required: false
        default: "**/cognition/runtime/knowledge/knowledge.md,**/cognition/_meta/architecture/contracts-registry.md"
        description: "Comma-separated globs selecting lineage, provenance, and contract-traceability artifacts"
      - name: privacy_artifacts_glob
        type: text
        required: false
        default: "**/assurance/practices/standards/security-and-privacy.md,**/services/governance/guard/**"
        description: "Comma-separated globs selecting privacy and sensitive-data safeguard artifacts"
      - name: contract_artifacts_glob
        type: text
        required: false
        default: "**/_meta/architecture/contracts-registry.md,**/services/**/SERVICE.md"
        description: "Comma-separated globs selecting data-contract and interface metadata artifacts"
      - name: evidence_artifacts_glob
        type: text
        required: false
        default: "**/cognition/practices/methodology/spec-first-planning.md,**/services/_meta/docs/platform-overview.md,**/continuity/runs/**/evidence/**"
        description: "Comma-separated globs selecting governance evidence and run-receipt artifacts"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: data_governance_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-data-governance-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Data-governance findings report with classification, retention, lineage, privacy, and evidence readiness gaps"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-data-governance/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this data-governance audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-data-governance/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-data-governance runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-test-quality:
    version: "2.0.0"
    commands:
      - /audit-test-quality
    parameters:
      - name: scope
        type: folder
        required: true
        description: "Root directory containing test-quality artifacts to audit"
      - name: testing_baseline_ref
        type: file
        required: false
        default: ".octon/framework/assurance/practices/standards/testing-strategy.md"
        description: "Primary testing-strategy baseline reference"
      - name: quality_gate_baseline_ref
        type: file
        required: false
        default: ".octon/framework/cognition/practices/methodology/ci-cd-quality-gates.md"
        description: "Primary quality-gate and release-readiness baseline reference"
      - name: strategy_artifacts_glob
        type: text
        required: false
        default: "**/assurance/practices/standards/testing-strategy.md,**/*test-plan*.md,**/*testing-strategy*.md"
        description: "Comma-separated globs selecting strategy and policy artifacts"
      - name: test_surface_artifacts_glob
        type: text
        required: false
        default: "**/tests/**,**/*test*.py,**/*test*.rs,**/*spec*.md,**/fixtures/**"
        description: "Comma-separated globs selecting executable and documented test surfaces"
      - name: contract_integration_artifacts_glob
        type: text
        required: false
        default: "**/contracts/**,**/compatibility.yml,**/SERVICE.md,**/*integration*.md"
        description: "Comma-separated globs selecting contract and integration assurance artifacts"
      - name: reliability_artifacts_glob
        type: text
        required: false
        default: "**/*determinism*.md,**/*flake*.md,**/*retry*.md,**/.github/workflows/**"
        description: "Comma-separated globs selecting determinism and flake-control artifacts"
      - name: evidence_artifacts_glob
        type: text
        required: false
        default: "**/continuity/runs/**/evidence/**,**//.octon/state/evidence/validation/analysis/**quality**"
        description: "Comma-separated globs selecting quality evidence and run-receipt artifacts"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enables strict done-gate behavior for convergence verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used for convergence validation"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated seed list for run-to-run consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: test_quality_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-test-quality-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Structured test-quality findings report"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-test-quality/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this test-quality audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-test-quality/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-test-quality runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-operational-readiness:
    version: "2.0.0"
    commands:
      - /audit-operational-readiness
    parameters:
      - name: scope
        type: folder
        required: true
        description: "Root directory containing operational-readiness artifacts to audit"
      - name: operations_baseline_ref
        type: file
        required: false
        default: ".octon/framework/cognition/practices/methodology/reliability-and-ops.md"
        description: "Primary reliability and operations readiness baseline reference"
      - name: incident_baseline_ref
        type: file
        required: false
        default: ".octon/framework/engine/practices/incident-operations.md"
        description: "Primary incident-response and escalation baseline reference"
      - name: ownership_artifacts_glob
        type: text
        required: false
        default: "**/services/**/SERVICE.md,**/*ownership*.md,**/*owner*.md,**/registry.yml"
        description: "Comma-separated globs selecting service ownership and accountability artifacts"
      - name: runbook_artifacts_glob
        type: text
        required: false
        default: "**/*runbook*.md,**/planning/spec/runbook.md,**/engine/practices/release-runbook.md"
        description: "Comma-separated globs selecting operational runbook and procedures artifacts"
      - name: incident_artifacts_glob
        type: text
        required: false
        default: "**/*incident*.md,**/*on-call*.md,**/*oncall*.md,**/*pager*.md,**/engine/practices/incident-operations.md"
        description: "Comma-separated globs selecting incident-response and escalation artifacts"
      - name: slo_capacity_artifacts_glob
        type: text
        required: false
        default: "**/contracts/slo-budgets.tsv,**/*slo*.md,**/*capacity*.md,**/*reliability*.md"
        description: "Comma-separated globs selecting reliability objective and capacity artifacts"
      - name: evidence_artifacts_glob
        type: text
        required: false
        default: "**/continuity/runs/**/evidence/**,**//.octon/state/evidence/validation/analysis/**"
        description: "Comma-separated globs selecting operational evidence and run-receipt artifacts"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enables strict done-gate behavior for convergence verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used for convergence validation"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated seed list for run-to-run consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: operational_readiness_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-operational-readiness-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Structured operational-readiness findings report"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-operational-readiness/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this operational-readiness audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-operational-readiness/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-operational-readiness runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-release-readiness:
    version: "2.0.0"
    commands:
      - /audit-release-readiness
    parameters:
      - name: scope
        type: folder
        required: true
        description: "Root directory containing release-readiness artifacts to audit"
      - name: release_baseline_ref
        type: file
        required: false
        default: ".octon/framework/cognition/practices/methodology/ci-cd-quality-gates.md"
        description: "Primary release-policy and quality-gate baseline reference"
      - name: operations_baseline_ref
        type: file
        required: false
        default: ".octon/framework/engine/practices/incident-operations.md"
        description: "Primary operations and incident-response readiness baseline reference"
      - name: release_policy_artifacts_glob
        type: text
        required: false
        default: "**/cognition/practices/methodology/ci-cd-quality-gates.md,**/*release*.md,**/*launch*.md"
        description: "Comma-separated globs selecting release policy and criteria artifacts"
      - name: change_control_artifacts_glob
        type: text
        required: false
        default: "**/*change-control*.md,**/*approval*.md,**/orchestration/practices/mission-lifecycle-standards.md"
        description: "Comma-separated globs selecting change-control and approval artifacts"
      - name: deployment_artifacts_glob
        type: text
        required: false
        default: "**/.github/workflows/**,**/services/**/SERVICE.md,**/compatibility.yml"
        description: "Comma-separated globs selecting deployment and compatibility safeguard artifacts"
      - name: rollback_artifacts_glob
        type: text
        required: false
        default: "**/*rollback*.md,**/*contingency*.md,**/engine/practices/incident-operations.md"
        description: "Comma-separated globs selecting rollback and contingency artifacts"
      - name: evidence_artifacts_glob
        type: text
        required: false
        default: "**/continuity/runs/**/evidence/**,**//.octon/state/evidence/validation/analysis/**"
        description: "Comma-separated globs selecting release evidence and run-receipt artifacts"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enables strict done-gate behavior for convergence verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used for convergence validation"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated seed list for run-to-run consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: release_readiness_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-audit-release-readiness-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Structured release-readiness findings report"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-release-readiness/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this release-readiness audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-release-readiness/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-release-readiness runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-api-contract:
    version: "2.0.0"
    commands:
      - /audit-api-contract
    parameters:
      - name: scope
        type: folder
        required: true
        description: "Root directory containing API-contract artifacts to audit"
      - name: contract_baseline_ref
        type: file
        required: false
        default: ".octon/framework/cognition/governance/principles/contract-first.md"
        description: "Primary contract-first baseline reference"
      - name: api_design_baseline_ref
        type: file
        required: false
        default: ".octon/framework/scaffolding/governance/patterns/api-design-guidelines.md"
        description: "Primary API-design governance baseline reference"
      - name: spec_artifacts_glob
        type: text
        required: false
        default: "**/contracts/**,**/contract.md,**/openapi*.yaml,**/openapi*.yml,**/schema/**/*.json"
        description: "Comma-separated globs selecting API specifications and contract schema artifacts"
      - name: implementation_artifacts_glob
        type: text
        required: false
        default: "**/service.json,**/SERVICE.md,**/impl/**,**/rust/src/**,**/adapters/**"
        description: "Comma-separated globs selecting implementation artifacts mapped to contracts"
      - name: compatibility_artifacts_glob
        type: text
        required: false
        default: "**/compatibility.yml,**/contracts/invariants.md,**/contracts/errors.yml,**/references/examples.md"
        description: "Comma-separated globs selecting compatibility and invariants artifacts"
      - name: versioning_artifacts_glob
        type: text
        required: false
        default: "**/engine/governance/protocol-versioning.md,**/*version*.md,**/*deprecation*.md,**/contracts-registry.md"
        description: "Comma-separated globs selecting versioning and deprecation artifacts"
      - name: evidence_artifacts_glob
        type: text
        required: false
        default: "**/continuity/runs/**/evidence/**,**//.octon/state/evidence/validation/analysis/**"
        description: "Comma-separated globs selecting API governance evidence and run-receipt artifacts"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enables strict done-gate behavior for convergence verification"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used for convergence validation"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated seed list for run-to-run consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: api_contract_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-api-contract-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Structured API-contract findings report"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-api-contract/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this API-contract audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-api-contract/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-api-contract runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-ci-latency:
    version: "0.1.0"
    commands:
      - /audit-ci-latency
    parameters:
      - name: repository
        type: text
        required: false
        description: "Repository in owner/name format; defaults to the current repository"
      - name: window_runs
        type: text
        required: false
        default: "40"
        description: "Number of recent successful PR runs used for latency analysis"
      - name: top_workflows
        type: text
        required: false
        default: "5"
        description: "Number of slow or regressed workflows to inspect for job-step hotspots"
      - name: gate_scope
        type: text
        required: false
        default: "required"
        description: "Latency scope to emphasize in the final report (required or all)"
    requires:
      context:
        - type: command_available
          command: "gh"
          description: "Requires GitHub CLI installed and authenticated"
    io:
      inputs: []
      outputs:
        - name: ci_latency_audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-ci-latency-audit-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Structured CI latency audit report with hotspots, breach status, and tightening recommendations"
        - name: ci_latency_summary
          path: "/.octon/state/evidence/validation/analysis/{{date}}-ci-latency-audit-{{run_id}}.json"
          kind: file
          format: json
          determinism: unique
          description: "Machine-readable latency summary for workflows, issues, and follow-on automation"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-ci-latency/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this CI latency audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-ci-latency/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-ci-latency runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  audit-ui:
    version: "2.0.0"
    commands:
      - /audit-ui
    parameters:
      - name: target
        type: folder
        required: false
        default: "."
        description: "Directory containing UI files to audit"
      - name: ruleset_url
        type: text
        required: false
        default: "https://raw.githubusercontent.com/anthropics/anthropic-cookbook/refs/heads/main/misc/web_interface_guidelines.md"
        description: "URL of the external design guidelines ruleset"
      - name: file_types
        type: text
        required: false
        default: "tsx,jsx,html,css,vue,svelte"
        description: "Comma-separated UI file extensions to scan"
      - name: severity_threshold
        type: text
        required: false
        default: "all"
        description: "Minimum severity to report: critical, high, medium, low, all"
      - name: post_remediation
        type: boolean
        required: false
        default: false
        description: "Enable strict done-gate verification mode for post-remediation runs"
      - name: convergence_k
        type: text
        required: false
        default: "3"
        description: "Number of controlled reruns used to evaluate convergence stability"
      - name: seed_list
        type: text
        required: false
        description: "Comma-separated deterministic seeds used for consistency checks"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: audit_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-ui-audit.md"
          kind: file
          format: markdown
          determinism: variable
          description: "UI audit findings with file:line violations and rule references"
        - name: bounded_audit_bundle
          path: "/.octon/state/evidence/validation/audits/{{date}}-{{run_id}}/"
          kind: directory
          format: mixed
          determinism: unique
          description: "Authoritative bounded-audit bundle with findings/coverage/convergence artifacts"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/audit-ui/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this UI audit run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/audit-ui/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all audit-ui runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  python-api:
    version: "1.0.0"
    commands: []
    parameters: []
    requires:
      context: []
    io:
      inputs: []
      outputs: []

  swift-macos-app:
    version: "1.0.0"
    commands: []
    parameters: []
    requires:
      context: []
    io:
      inputs: []
      outputs: []

  python-scaffold-package:
    version: "1.0.0"
    commands:
      - /python-scaffold-package
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: python-api
          kind: skill
          ref: python-api
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/python-scaffold-package/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/python-scaffold-package/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all python-scaffold-package runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  python-contract-first-api:
    version: "1.0.0"
    commands:
      - /python-contract-first-api
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: python-api
          kind: skill
          ref: python-api
          role: prerequisite
          required: true
        - id: python-scaffold-package
          kind: skill
          ref: python-scaffold-package
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/python-contract-first-api/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/python-contract-first-api/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all python-contract-first-api runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  python-test-harness:
    version: "1.0.0"
    commands:
      - /python-test-harness
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: python-api
          kind: skill
          ref: python-api
          role: prerequisite
          required: true
        - id: python-scaffold-package
          kind: skill
          ref: python-scaffold-package
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/python-test-harness/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/python-test-harness/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all python-test-harness runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  python-dev-toolchain:
    version: "1.0.0"
    commands:
      - /python-dev-toolchain
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: python-api
          kind: skill
          ref: python-api
          role: prerequisite
          required: true
        - id: python-scaffold-package
          kind: skill
          ref: python-scaffold-package
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/python-dev-toolchain/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/python-dev-toolchain/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all python-dev-toolchain runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  python-infra-manifest:
    version: "1.0.0"
    commands:
      - /python-infra-manifest
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: python-api
          kind: skill
          ref: python-api
          role: prerequisite
          required: true
        - id: python-scaffold-package
          kind: skill
          ref: python-scaffold-package
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/python-infra-manifest/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/python-infra-manifest/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all python-infra-manifest runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  python-contributor-guide:
    version: "1.0.0"
    commands:
      - /python-contributor-guide
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: python-api
          kind: skill
          ref: python-api
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/python-contributor-guide/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/python-contributor-guide/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all python-contributor-guide runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  swift-scaffold-package:
    version: "1.0.0"
    commands:
      - /swift-scaffold-package
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: swift-macos-app
          kind: skill
          ref: swift-macos-app
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/swift-scaffold-package/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/swift-scaffold-package/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all swift-scaffold-package runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  swift-data-layer:
    version: "1.0.0"
    commands:
      - /swift-data-layer
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: swift-macos-app
          kind: skill
          ref: swift-macos-app
          role: prerequisite
          required: true
        - id: swift-scaffold-package
          kind: skill
          ref: swift-scaffold-package
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/swift-data-layer/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/swift-data-layer/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all swift-data-layer runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  swift-cli-interface:
    version: "1.0.0"
    commands:
      - /swift-cli-interface
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: swift-macos-app
          kind: skill
          ref: swift-macos-app
          role: prerequisite
          required: true
        - id: swift-scaffold-package
          kind: skill
          ref: swift-scaffold-package
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/swift-cli-interface/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/swift-cli-interface/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all swift-cli-interface runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  swift-daemon-service:
    version: "1.0.0"
    commands:
      - /swift-daemon-service
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: swift-macos-app
          kind: skill
          ref: swift-macos-app
          role: prerequisite
          required: true
        - id: swift-scaffold-package
          kind: skill
          ref: swift-scaffold-package
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/swift-daemon-service/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/swift-daemon-service/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all swift-daemon-service runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  swift-test-harness:
    version: "1.0.0"
    commands:
      - /swift-test-harness
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: swift-macos-app
          kind: skill
          ref: swift-macos-app
          role: prerequisite
          required: true
        - id: swift-scaffold-package
          kind: skill
          ref: swift-scaffold-package
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/swift-test-harness/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/swift-test-harness/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all swift-test-harness runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  swift-contributor-guide:
    version: "1.0.0"
    commands:
      - /swift-contributor-guide
    parameters:
      - name: project_name
        type: text
        required: true
        description: "Project name for scaffolding"
    requires:
      context: []
    composition:
      mode: prerequisites
      failure_policy: fail_fast
      steps:
        - id: swift-macos-app
          kind: skill
          ref: swift-macos-app
          role: prerequisite
          required: true
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/swift-contributor-guide/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/swift-contributor-guide/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all swift-contributor-guide runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  react:
    version: "1.0.0"
    commands: []
    parameters: []
    requires:
      context: []
    io:
      inputs: []
      outputs: []

  react-native:
    version: "1.0.0"
    commands: []
    parameters: []
    requires:
      context: []
    io:
      inputs: []
      outputs: []

  postgres:
    version: "1.0.0"
    commands: []
    parameters: []
    requires:
      context: []
    io:
      inputs: []
      outputs: []

  react-composition-patterns:
    version: "1.0.0"
    commands:
      - /react-composition-patterns
    parameters:
      - name: target
        type: folder
        required: false
        default: "."
        description: "Directory to review for composition patterns"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/react-composition-patterns/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this pattern review"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/react-composition-patterns/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  react-best-practices:
    version: "1.0.1"
    commands:
      - /react-best-practices
    parameters:
      - name: target
        type: folder
        required: false
        default: "."
        description: "Directory to review against React/Next.js best practices"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/react-best-practices/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this review"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/react-best-practices/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  react-native-best-practices:
    version: "1.0.0"
    commands:
      - /react-native-best-practices
    parameters:
      - name: target
        type: folder
        required: false
        default: "."
        description: "Directory to review against React Native/Expo best practices"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/react-native-best-practices/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this review"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/react-native-best-practices/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  vercel:
    version: "1.0.0"
    commands: []
    parameters: []
    requires:
      context: []
    io:
      inputs: []
      outputs: []

  vercel-deploy:
    version: "1.0.0"
    commands:
      - /vercel-deploy
    parameters:
      - name: environment
        type: text
        required: false
        default: "production"
        description: "Deployment target: production, preview"
      - name: project_dir
        type: folder
        required: false
        default: "."
        description: "Directory containing the project to deploy"
    requires:
      context:
        - type: command_available
          command: "vercel"
          description: "Requires Vercel CLI installed and authenticated"
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/vercel-deploy/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log capturing deployment URL and status"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/vercel-deploy/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all deploy runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  deploy-status:
    version: "1.0.1"
    commands:
      - /deploy-status
    parameters:
      - name: project
        type: text
        required: false
        description: "Vercel project name; defaults to linked project"
      - name: deployment
        type: text
        required: false
        description: "Deployment URL or deployment id to inspect"
      - name: environment
        type: text
        required: false
        default: "production"
        description: "Deployment target: production or preview"
      - name: check_url
        type: boolean
        required: false
        default: true
        description: "Run a URL reachability check when a deployment URL is found"
    requires:
      context:
        - type: command_available
          command: "vercel"
          description: "Requires Vercel CLI installed and authenticated"
    io:
      inputs: []
      outputs:
        - name: status_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-deploy-status-{{project}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Deployment status report with readiness assessment and follow-ups"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/deploy-status/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for deployment status checks"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/deploy-status/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of deploy-status runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  provider-vercel-delivery:
    version: "0.1.0"
    commands:
      - /provider-vercel-delivery
    parameters:
      - name: operation
        type: text
        required: false
        default: "preview"
        description: "Delivery operation mode (preview, promote, rollback)"
      - name: environment
        type: text
        required: false
        default: "production"
        description: "Target environment context"
      - name: deployment
        type: text
        required: false
        description: "Optional deployment URL or id"
    requires:
      context:
        - type: command_available
          command: "vercel"
          description: "Requires Vercel CLI installed and authenticated"
    composition:
      mode: sequential
      failure_policy: fail_fast
      steps:
        - id: vercel-deploy
          kind: skill
          ref: vercel-deploy
          role: invoke
          required: true
          bindings:
            environment: parameter.environment
        - id: deploy-status
          kind: skill
          ref: deploy-status
          role: invoke
          required: true
          bindings:
            deployment: parameter.deployment
            environment: parameter.environment
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/provider-vercel-delivery/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for provider-specific delivery actions"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/provider-vercel-delivery/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of provider-vercel-delivery runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  provider-github-gates:
    version: "0.1.0"
    commands:
      - /provider-github-gates
    parameters:
      - name: repository
        type: text
        required: false
        description: "Repository in owner/name format"
      - name: pr
        type: text
        required: false
        description: "Pull request number to inspect"
      - name: gate_scope
        type: text
        required: false
        default: "required"
        description: "Gate scope (required, all)"
    requires:
      context:
        - type: command_available
          command: "gh"
          description: "Requires GitHub CLI installed and authenticated"
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/provider-github-gates/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for provider-specific gate mapping"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/provider-github-gates/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of provider-github-gates runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  provider-nextjs-astro-runtime:
    version: "0.1.0"
    commands:
      - /provider-nextjs-astro-runtime
    parameters:
      - name: surface
        type: text
        required: false
        default: "nextjs"
        description: "Surface to analyze (nextjs, astro, mixed)"
      - name: mode
        type: text
        required: false
        default: "ssr"
        description: "Runtime mode (ssr, ssg, edge, serverless)"
      - name: focus
        type: text
        required: false
        default: "observability"
        description: "Focus area (observability, caching, headers)"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/provider-nextjs-astro-runtime/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for framework/runtime adapter analysis"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/provider-nextjs-astro-runtime/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of provider-nextjs-astro-runtime runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  provider-preview-smoke:
    version: "0.1.0"
    commands:
      - /provider-preview-smoke
    parameters:
      - name: url
        type: text
        required: true
        description: "Preview target URL"
      - name: profile
        type: text
        required: false
        default: "t2"
        description: "Smoke profile depth (t1, t2, t3)"
      - name: retries
        type: text
        required: false
        default: "2"
        description: "Retry count for transient failures"
    requires:
      context:
        - type: command_available
          command: "curl"
          description: "Requires curl for HTTP smoke checks"
    io:
      inputs: []
      outputs:
        - name: smoke_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-provider-preview-smoke-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Preview smoke summary with readiness classification"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/provider-preview-smoke/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for provider preview smoke checks"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/provider-preview-smoke/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of provider-preview-smoke runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  turborepo-taskgraph:
    version: "0.1.0"
    commands:
      - /turborepo-taskgraph
    parameters:
      - name: scope
        type: text
        required: false
        default: "repo"
        description: "Diagnostic scope (repo, workspace, pipeline)"
      - name: focus
        type: text
        required: false
        default: "graph"
        description: "Primary focus (graph, cache, scripts)"
      - name: task
        type: text
        required: false
        description: "Optional pipeline task to target"
    requires:
      context:
        - type: command_available
          command: "turbo"
          description: "Requires Turborepo CLI available in execution context"
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/turborepo-taskgraph/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for Turborepo taskgraph diagnostics"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/turborepo-taskgraph/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of turborepo-taskgraph runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  create-skill:
    version: "1.0.1"
    commands:
      - /create-skill
    parameters:
      - name: skill_name
        type: text
        required: true
        description: "Skill identifier (kebab-case, 1-64 characters)"
      - name: group
        type: text
        required: true
        description: "Skill group directory (audit, remediation, operations, refactor, synthesis, meta, foundations, platforms)"
      - name: description
        type: text
        required: false
        description: "Initial description for SKILL.md"
      - name: skill_sets
        type: text
        required: false
        default: ""
        description: "Comma-separated skill sets (executor, coordinator, delegator, collaborator, integrator, specialist, guardian)"
      - name: capabilities
        type: text
        required: false
        default: ""
        description: "Comma-separated additional capabilities beyond skill set bundles"
    requires:
      context:
        - type: directory_exists
          path: ".octon/framework/capabilities/runtime/skills/_scaffold/template/"
          description: "Requires skill template directory"
    io:
      inputs: []
      outputs:
        - name: skill_directory
          path: "{{group}}/{{skill_name}}/"
          kind: directory
          format: mixed
          determinism: stable
          description: "The created skill directory (deliverable)"
        - name: scaffold_artifacts
          path: "/.octon/state/control/skills/checkpoints/create-skill/{{run_id}}/"
          kind: directory
          format: yaml
          determinism: unique
          description: "Execution state (checkpoint and summary) for session recovery"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/create-skill/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this skill creation"
        - name: skill_log_index
          path: "/.octon/state/evidence/runs/skills/create-skill/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all create-skill runs with metadata"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  resolve-pr-comments:
    version: "1.0.1"
    commands:
      - /resolve-pr-comments
    parameters:
      - name: pr
        type: text
        required: true
        description: "PR number or full GitHub URL"
      - name: reviewer
        type: text
        required: false
        description: "Filter to specific reviewer's comments only"
      - name: types
        type: text
        required: false
        default: "all"
        description: "Comma-separated comment types to resolve: bug,design,style,nit,question"
    requires:
      context:
        - type: command_available
          command: "gh"
          description: "Requires GitHub CLI installed and authenticated"
    io:
      inputs: []
      outputs:
        - name: resolution_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-pr-comments-resolved.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Structured resolution report with per-comment status"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/resolve-pr-comments/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this resolution run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/resolve-pr-comments/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all resolve-pr-comments runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  triage-ci-failure:
    version: "1.0.1"
    commands:
      - /triage-ci-failure
    parameters:
      - name: pr
        type: text
        required: false
        description: "PR number or URL with failing CI"
      - name: branch
        type: text
        required: false
        description: "Branch name with failing CI"
      - name: job
        type: text
        required: false
        description: "Target a specific job name"
      - name: step
        type: text
        required: false
        description: "Target a specific step name"
    requires:
      context:
        - type: command_available
          command: "gh"
          description: "Requires GitHub CLI installed and authenticated"
    io:
      inputs: []
      outputs:
        - name: triage_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-ci-triage.md"
          kind: file
          format: markdown
          determinism: unique
          description: "CI triage report with diagnosis and fix details"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/triage-ci-failure/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this triage run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/triage-ci-failure/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all triage-ci-failure runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  ci-gate-triage:
    version: "0.1.0"
    commands:
      - /ci-gate-triage
    parameters:
      - name: repository
        type: text
        required: false
        description: "Repository in owner/name format"
      - name: pr
        type: text
        required: false
        description: "Pull request number or URL"
      - name: run
        type: text
        required: false
        description: "Optional CI run identifier"
      - name: gate_scope
        type: text
        required: false
        default: "required"
        description: "Gate scope (required, all, failing)"
    requires:
      context:
        - type: command_available
          command: "gh"
          description: "Requires GitHub CLI installed and authenticated"
    composition:
      mode: sequential
      failure_policy: fail_fast
      steps:
        - id: triage-ci-failure
          kind: skill
          ref: triage-ci-failure
          role: invoke
          required: true
          bindings:
            pr: parameter.pr
    io:
      inputs: []
      outputs:
        - name: triage_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-ci-gate-triage-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Structured gate triage report with prioritized remediation plan"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/ci-gate-triage/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for CI gate triage runs"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/ci-gate-triage/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of ci-gate-triage runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  incident-response:
    version: "0.1.0"
    commands:
      - /incident-response
    parameters:
      - name: severity
        type: text
        required: false
        default: "sev2"
        description: "Incident severity label (sev1, sev2, sev3)"
      - name: scope
        type: text
        required: false
        default: "runtime"
        description: "Incident scope surface"
      - name: mode
        type: text
        required: false
        default: "mitigate"
        description: "Response mode (mitigate, rollback, document)"
      - name: evidence_refs
        type: text
        required: false
        description: "Comma-separated evidence references (trace IDs, links, IDs)"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: incident_report
          path: "/.octon/state/evidence/validation/analysis/{{date}}-incident-response-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Incident response receipt with timeline, mitigation, and follow-ups"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/incident-response/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for incident-response runs"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/incident-response/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of incident-response runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  postgres-best-practices:
    version: "1.0.0"
    commands:
      - /postgres-best-practices
    parameters:
      - name: target
        type: folder
        required: false
        default: "."
        description: "Directory to review against PostgreSQL best practices"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/postgres-best-practices/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this review"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/postgres-best-practices/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  spec-to-implementation:
    version: "1.0.2"
    commands:
      - /spec-to-implementation
    parameters:
      - name: spec
        type: text
        required: true
        description: "Path to spec document or inline specification text"
      - name: scope
        type: text
        required: false
        default: "."
        description: "Directory to scan for existing code"
      - name: format
        type: text
        required: false
        default: "markdown"
        description: "Output format: markdown or yaml"
      - name: change_profile
        type: text
        required: false
        default: "atomic"
        description: "Governance change profile: atomic or transitional"
      - name: release_state
        type: text
        required: false
        default: "auto"
        description: "Release state override: auto, pre-1.0, or stable"
      - name: transitional_exception_note
        type: text
        required: false
        description: "Required in pre-1.0 when change_profile is transitional (rationale, risks, owner, target_removal_date)"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: implementation_plan
          path: "/.octon/inputs/exploratory/plans/{{date}}-implementation-plan-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Staged implementation plan with profile selection receipt, impact map, compliance receipt, and escalation sections"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/spec-to-implementation/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this planning run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/spec-to-implementation/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all spec-to-implementation runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  plan-charter-alignment:
    version: "1.0.0"
    commands:
      - /plan-charter-alignment
    parameters:
      - name: charter_path
        type: file
        required: true
        description: "Path to the charter file to plan against"
      - name: findings_source
        type: file
        required: true
        description: "Path to the charter audit report or findings package"
      - name: target_score
        type: text
        required: false
        default: "95"
        description: "Desired minimum post-remediation score target"
      - name: change_profile
        type: text
        required: false
        default: "auto"
        description: "Governance change profile override: auto, atomic, or transitional"
      - name: release_state
        type: text
        required: false
        default: "auto"
        description: "Release state override: auto, pre-1.0, or stable"
      - name: scope
        type: text
        required: false
        default: "charter-only"
        description: "Planning scope for remediation"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: charter_alignment_plan
          path: "/.octon/inputs/exploratory/plans/{{date}}-charter-alignment-plan-{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Profile-governed charter alignment plan with change bundles and validation scenarios"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/plan-charter-alignment/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this charter alignment planning run"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/plan-charter-alignment/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all plan-charter-alignment runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"

  build-mcp-server:
    version: "1.0.1"
    commands:
      - /build-mcp-server
    parameters:
      - name: name
        type: text
        required: true
        description: "Server name (kebab-case, used for directory and package.json)"
      - name: tools
        type: text
        required: false
        description: "Comma-separated list of tool names to scaffold"
      - name: service
        type: text
        required: false
        description: "Description of the service to expose (alternative to tools)"
      - name: language
        type: text
        required: false
        default: "typescript"
        description: "Implementation language: typescript or python"
      - name: output_dir
        type: folder
        required: false
        default: "."
        description: "Directory where the MCP server project will be created"
    requires:
      context: []
    io:
      inputs: []
      outputs:
        - name: mcp_server_project
          path: "{{output_dir}}/{{name}}/"
          kind: directory
          format: mixed
          determinism: stable
          description: "The created MCP server project directory"
        - name: run_log
          path: "/.octon/state/evidence/runs/skills/build-mcp-server/{{run_id}}.md"
          kind: file
          format: markdown
          determinism: unique
          description: "Execution log for this build"
        - name: log_index
          path: "/.octon/state/evidence/runs/skills/build-mcp-server/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Index of all build-mcp-server runs"
        - name: top_log_index
          path: "/.octon/state/evidence/runs/skills/index.yml"
          kind: file
          format: yaml
          determinism: variable
          description: "Top-level log index (cross-skill)"
