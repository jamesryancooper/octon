# /proposal-program-clean-delivery — Disabled Compatibility Surface

This command is not registered for current use during RP-00. Any direct or
compatibility invocation fails before route-graph execution or lifecycle
dispatch with:

```text
RP00_CONTAINMENT_PUBLICATION_DISABLED
```

The denial preserves exact program/child refs, branches, worktrees, rollback
handles, retained evidence, and unrelated work. It never supplies
`--execute-routes`, requests `target_outcome=cleaned`, or delegates landing,
sync, cleanup, publication, branch deletion, Git/GitHub, provider, or archive
effects. Historical command and receipt text is interpretation-only.
