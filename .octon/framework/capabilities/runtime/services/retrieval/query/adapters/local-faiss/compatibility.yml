compatibility:
  interop_contract: "1.x"
  adapter_schema: "1.x"
  runtime_requirements:
    minimum_runtime: "2026.1"
  required_tools: [read, glob, grep, bash]
  optional_tools: [jq]
  conformance:
    capability_matrix_validation: true
    fallback_behavior_validation: true
