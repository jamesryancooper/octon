# Runtime

Runtime execution-role artifacts for the execution-role subsystem.

The final runtime layout is:

- `orchestrator/` for the single accountable default execution role
- `specialists/` for bounded stateless helpers
- `verifiers/` for optional independent verification roles
- `composition-profiles/` for reusable non-executing routing profiles
