# Negative control 2: a bank-known suite method (`tradeoff-review-method`) is
# declared in suite_methods but has no method_profiles entry. The lens-reference
# validator MUST fail closed (non-zero exit) on this bank.
schema_version: "architectural-review-lens-bank-v1"
mechanism_family_slug: "architectural-review"
lens_tiers:
  - "core"
  - "extended"
suite_methods:
  - slug: "balanced-architecture-review-method"
    slug_status: "canonical"
  - slug: "tradeoff-review-method"
    slug_status: "provisional"
lenses:
  - id: "system-job-framing"
    tier: "core"
  - id: "current-reality-map"
    tier: "core"
  - id: "tradeoff-adr"
    tier: "core"
method_profiles:
  balanced-architecture-review-method:
    required:
      - "system-job-framing"
      - "current-reality-map"
      - "tradeoff-adr"
