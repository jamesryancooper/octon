# Fixture: NC-B — routing selects a method not in the naming catalog.
# `nonexistent-review-method` appears in allowed_methods_by_route but is absent
# from this fixture's naming.yml methods.catalog, so the routing validator's
# unknown-method check fails closed. All other selection fields are valid.
schema_version: "architectural-review-routing-v2"
method_selection:
  default_method: "balanced-architecture-review-method"
  allowed_methods_by_route:
    pre-integration-architecture-review:
      - "balanced-architecture-review-method"
      - "nonexistent-review-method"
  escalation_map:
    balanced-architecture-review-method:
      - trigger: "two-or-more-viable-target-designs"
        to: "tradeoff-review-method"
  constitutional_conflict_routes_to: "constitutional-challenge"
fail_closed_conditions:
  - "unknown_method"
  - "missing_method_record"
