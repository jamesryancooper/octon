# Target Architecture

Work Package control truth lives at
`state/control/engagements/<engagement-id>/work-package.yml`.

Objective Brief is engagement control/candidate state, not workspace charter
authority.

The generated operator view is a non-authority read model only.
