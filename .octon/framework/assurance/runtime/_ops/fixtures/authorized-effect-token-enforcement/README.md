# Authorized Effect Token Enforcement Fixtures

This fixture family is non-authoritative. It maps token verifier acceptance and
denial cases to backing Rust tests so validation can prove the material
side-effect authorization boundary without treating fixtures as runtime policy.
