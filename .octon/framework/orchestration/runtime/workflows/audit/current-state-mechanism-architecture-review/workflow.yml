---
schema_version: workflow-contract-v2
name: current-state-mechanism-architecture-review
description: Review the current architecture of one governed cross-surface mechanism using native architectural review doctrine.
version: 1.0.0
entry_mode: human
execution_profile: core
side_effect_class: read_only
execution_controls:
  cancel_safe: true
coordination_key_strategy:
  kind: workflow-target
  source_fields:
    - workflow_group
    - workflow_id
  format: workflow:{workflow_group}/{workflow_id}
executor_interface_version: workflow-executor-v1
inputs:
  - name: mechanism_id
    type: text
    required: true
    description: Governed cross-surface mechanism id to review
stages:
  - id: configure
    asset: stages/01-configure.md
    kind: analysis
    consumes: []
    produces:
      - stage:configure
    mutation_scope: []
    authorization:
      action_type: execute_stage
      requested_capabilities:
        - workflow.stage.execute
        - evidence.write
      side_effects:
        write_repo: false
        write_evidence: true
        shell: true
        network: false
        model_invoke: true
        state_mutation: false
        publication: false
        branch_mutation: false
      risk_tier: low
      scope:
        read:
          - workflow-scope
        write:
          - workflow-evidence
      review_requirements:
        human_approval: false
        quorum: false
        rollback_metadata: false
      allowed_executor_profiles:
        - read_only_analysis
  - id: mechanism-review
    asset: stages/02-mechanism-review.md
    kind: verification
    consumes:
      - stage:configure
    produces:
      - stage:mechanism-review
    mutation_scope: []
    authorization:
      action_type: execute_stage
      requested_capabilities:
        - workflow.stage.execute
        - evidence.write
      side_effects:
        write_repo: false
        write_evidence: true
        shell: true
        network: false
        model_invoke: true
        state_mutation: false
        publication: false
        branch_mutation: false
      risk_tier: low
      scope:
        read:
          - workflow-scope
        write:
          - workflow-evidence
      review_requirements:
        human_approval: false
        quorum: false
        rollback_metadata: false
      allowed_executor_profiles:
        - read_only_analysis
  - id: receipt
    asset: stages/03-receipt.md
    kind: verification
    consumes:
      - stage:mechanism-review
    produces:
      - stage:receipt
    mutation_scope: []
    authorization:
      action_type: execute_stage
      requested_capabilities:
        - workflow.stage.execute
        - evidence.write
      side_effects:
        write_repo: false
        write_evidence: true
        shell: true
        network: false
        model_invoke: false
        state_mutation: false
        publication: false
        branch_mutation: false
      risk_tier: low
      scope:
        read:
          - workflow-scope
        write:
          - workflow-evidence
      review_requirements:
        human_approval: false
        quorum: false
        rollback_metadata: false
      allowed_executor_profiles:
        - read_only_analysis
artifacts:
  - name: current_state_mechanism_architecture_review_receipt
    path: "/.octon/state/evidence/runs/workflows/{{run_id}}/architectural-review/current-state-mechanism-architecture-review/support-receipt.yml"
    kind: file
    format: yaml
    determinism: unique
    description: Evidence-only current-state mechanism architecture review receipt
  - name: current_state_mechanism_architecture_review_method_selection_record
    path: "/.octon/state/evidence/runs/workflows/{{run_id}}/architectural-review/current-state-mechanism-architecture-review/report.yml"
    kind: file
    format: yaml
    determinism: unique
    description: Records the selected review method id (field method bound to naming.yml methods.catalog) and the applied lens profile (field lenses_applied bound to lens-bank.yml) through the architectural-review-report-v2 artifact in the existing architectural-review run-evidence root; descriptive run evidence only, granting the review output no lifecycle, acceptance, promotion, or closeout authority while the v1 support receipt stays method-free.
done_gate:
  checks:
    - "Mechanism index entry is present and validates"
    - "Receipt validates under architectural-review-support-receipt-v1"
    - "Current receipts classify external-tool boundaries and record external_tool_integrity coverage"
constraints:
  fail_closed: true
  require_relative_local_assets: true
