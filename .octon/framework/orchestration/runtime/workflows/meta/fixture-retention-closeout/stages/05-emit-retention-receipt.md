# Emit Retention Receipt

Write `retention-receipt.yml` under the workflow evidence bundle and validate it with `validate-fixture-retention-closeout-receipt.sh`.

The receipt explicitly states that it is nonblocking only for unrelated packet terminal readiness under exact receipt checks and that it does not authorize archive-ready, cleaned, archive relocation, proposal status mutation, generated publication edits, Git mutation, repo-hygiene deletion, or target packet evidence claims.
