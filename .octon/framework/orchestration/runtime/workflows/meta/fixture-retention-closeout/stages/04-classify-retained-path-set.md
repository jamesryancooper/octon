# Classify Retained Path Set

Run `git status --porcelain=v1 --untracked-files=all` over the derived fixture scope roots. The retained status entries must exactly match the receipt path set and status digest.

Terminal/worktree hygiene may consume the receipt only when exact path-set match, current digest match, schema/route version match, purpose and owner scope match, and freshness checks pass. Unrelated residue coverage is forbidden.
