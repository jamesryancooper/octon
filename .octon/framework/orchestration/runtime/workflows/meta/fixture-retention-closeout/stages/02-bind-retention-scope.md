# Bind Retention Scope

Derive all retained fixture scope roots from the manifest and workflow inputs:

- `fixture_path`
- `.octon/generated/proposals/artifacts/{proposal_kind}/{proposal_id}`
- `.octon/state/evidence/validation/proposals/{proposal_id}`

No archive relocation, proposal status mutation, generated publication edit, Git mutation, residue deletion, or repo-hygiene deletion authority is granted.
