---
title: Report Validation
description: Summarize the proposal validation outcome and persist the workflow bundle receipts.
---

# Step 2: Report Validation

## Purpose

Make the validation result explicit and reviewable.

## Actions

1. Write a top-level summary with proposal identity, kind, validator result, and bundle path.
2. Persist `summary.md`, `commands.md`, `inventory.md`, `validation.md`, and `bundle.yml`.
3. Emit an explicit final verdict.
