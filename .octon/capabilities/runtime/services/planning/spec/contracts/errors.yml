version: "1.0.0"
errors:
  - code: InputValidationError
    category: structural
    exit_code: 5
    retryable: false
    operator_action: "Fix payload to match schema/input.schema.json."
  - code: SpecExecutionError
    category: runtime
    exit_code: 4
    retryable: false
    operator_action: "Review run result and service diagnostics."
