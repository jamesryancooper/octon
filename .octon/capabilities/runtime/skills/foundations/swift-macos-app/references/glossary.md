# Glossary Reference

**Required when capability:** `domain-specialized`

## Core Terms

| Term | Definition | Example |
|------|------------|---------|
| SPM | Swift Package Manager build and dependency tool | `Package.swift` target graph |
| actor | Swift concurrency isolation primitive | Single-writer data/service actors |
| LaunchAgent | macOS background service registration | Daemon startup via plist |
| GRDB | SQLite toolkit used in Swift apps | Record types and migrations |
| XCTest | Native Swift test framework | Unit and integration test suites |
