[package]
name = "octon-kv-service"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.26"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[package.metadata.component]
package = "octon:runtime"
world = "octon-service"
