name: Skills Validation

on:
  push:
    paths:
      - '.harmony/capabilities/runtime/skills/**'
  pull_request:
    paths:
      - '.harmony/capabilities/runtime/skills/**'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python (for tiktoken)
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install tiktoken for accurate token validation
        run: pip install tiktoken

      - name: Validate skills
        run: |
          chmod +x .harmony/capabilities/runtime/skills/_ops/scripts/validate-skills.sh
          .harmony/capabilities/runtime/skills/_ops/scripts/validate-skills.sh --strict

      - name: Report validation results
        if: failure()
        run: |
          echo "::error::Skills validation failed. Run '.harmony/capabilities/runtime/skills/_ops/scripts/validate-skills.sh' locally to see details."
