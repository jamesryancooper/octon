name: Skills Validation

on:
  push:
    paths:
      - '.harmony/capabilities/skills/**'
  pull_request:
    paths:
      - '.harmony/capabilities/skills/**'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python (for tiktoken)
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install tiktoken for accurate token validation
        run: pip install tiktoken

      - name: Validate skills
        run: |
          chmod +x .harmony/capabilities/skills/scripts/validate-skills.sh
          .harmony/capabilities/skills/scripts/validate-skills.sh --strict

      - name: Report validation results
        if: failure()
        run: |
          echo "::error::Skills validation failed. Run '.harmony/capabilities/skills/scripts/validate-skills.sh' locally to see details."
