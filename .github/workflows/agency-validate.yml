name: Agency Validation

on:
  push:
    branches:
      - main
    paths:
      - '.octon/agency/**'
      - '.octon/octon.yml'
      - '.octon/START.md'
      - '.octon/README.md'
      - '.octon/scope.md'
      - '.octon/scaffolding/runtime/templates/octon/**'
  pull_request:
    paths:
      - '.octon/agency/**'
      - '.octon/octon.yml'
      - '.octon/START.md'
      - '.octon/README.md'
      - '.octon/scope.md'
      - '.octon/scaffolding/runtime/templates/octon/**'

permissions:
  contents: read

concurrency:
  group: agency-validate-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate agency contracts
        run: |
          chmod +x .octon/agency/_ops/scripts/validate/validate-agency.sh
          .octon/agency/_ops/scripts/validate/validate-agency.sh

      - name: Validate autonomy label catalog
        run: |
          chmod +x .octon/agency/_ops/scripts/validate/validate-autonomy-labels.sh
          .octon/agency/_ops/scripts/validate/validate-autonomy-labels.sh

      - name: Report validation failure
        if: failure()
        run: |
          echo "::error::Agency validation failed. Run '.octon/agency/_ops/scripts/validate/validate-agency.sh' locally to inspect errors."
