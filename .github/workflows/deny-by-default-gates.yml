name: Deny-by-Default Gates

on:
  pull_request:
    paths:
      - '.harmony/capabilities/_ops/**'
      - '.harmony/capabilities/runtime/services/**'
      - '.harmony/capabilities/runtime/skills/**'
      - '.harmony/capabilities/governance/policy/**'
      - '.harmony/assurance/runtime/_ops/scripts/validate-capability-engine-consistency.sh'
      - '.harmony/engine/runtime/spec/policy-interface-v1.md'
      - '.harmony/engine/runtime/crates/policy_engine/**'
  push:
    paths:
      - '.harmony/capabilities/_ops/**'
      - '.harmony/capabilities/runtime/services/**'
      - '.harmony/capabilities/runtime/skills/**'
      - '.harmony/capabilities/governance/policy/**'
      - '.harmony/assurance/runtime/_ops/scripts/validate-capability-engine-consistency.sh'
      - '.harmony/engine/runtime/spec/policy-interface-v1.md'
      - '.harmony/engine/runtime/crates/policy_engine/**'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Validate deny-by-default contract and enforcement (strict)
        run: |
          chmod +x .harmony/capabilities/_ops/scripts/validate-deny-by-default.sh
          .harmony/capabilities/_ops/scripts/validate-deny-by-default.sh --all --profile strict

      - name: Validate engine capabilities boundary
        run: |
          chmod +x .harmony/assurance/runtime/_ops/scripts/validate-engine-capability-boundary.sh
          .harmony/assurance/runtime/_ops/scripts/validate-engine-capability-boundary.sh

      - name: Validate capability/engine consistency
        run: |
          chmod +x .harmony/assurance/runtime/_ops/scripts/validate-capability-engine-consistency.sh
          .harmony/assurance/runtime/_ops/scripts/validate-capability-engine-consistency.sh

      - name: Friction SLO report
        run: |
          chmod +x .harmony/capabilities/_ops/scripts/policy-rollout-mode.sh
          .harmony/capabilities/_ops/scripts/policy-rollout-mode.sh slo-report
