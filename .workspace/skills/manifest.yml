# Skills Manifest (Project-Specific)
# Extends .harmony/skills/manifest.yml with project-specific skills.
#
# Add project-specific skills here for Tier 1 discovery.
# I/O mappings and pipelines remain in registry.yml.
#
# Convention: display_name uses Title Case derived from id
#             (e.g., "analyze-codebase" → "Analyze Codebase")

schema_version: "1.2"
extends: "../../.harmony/skills/manifest.yml"
default: null

# Project-specific skills (add here when needed)
skills: []
