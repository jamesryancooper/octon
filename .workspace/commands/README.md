# Commands

Generic commands live in `.harmony/commands/`.

Project-specific commands can be added here.

## Inherited from `.harmony/`

- `recover.md` - Recovery procedures for agent failure modes
- `validate-frontmatter.md` - Validate YAML frontmatter in markdown files

## Local Commands

(None currently - add project-specific commands here)
