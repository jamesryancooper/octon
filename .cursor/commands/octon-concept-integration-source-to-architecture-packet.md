# Source To Architecture Packet

Run the `source-to-architecture-packet` bundle.

Use this when you want the default external-source flow:

- extract implementable concepts
- verify them against the live repo
- generate an architecture proposal packet
- optionally generate an implementation prompt
