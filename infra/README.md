# infra

Infrastructure-as-code and CI/CD configuration that supports the monorepo.

- `ci/` contains GitHub Actions workflow definitions (kept here for code review; copy to `.github/workflows/` to activate).
- `otel/` contains OpenTelemetry bootstrap and shared observability config.

