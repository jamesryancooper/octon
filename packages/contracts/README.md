# packages/contracts

Central place for API contracts: OpenAPI/JSON Schemas and (optional) Pact files.

- Files: `openapi.yaml`, `openapi-base.yaml` (for breaking-change checks).
- CI: `contracts:check` uses these for drift detection.

