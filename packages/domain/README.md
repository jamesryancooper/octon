# packages/domain

Pure business logic and domain types. No framework or IO code.

- Allowed deps: none (internal TS only).
- Disallowed: react, next, axios, pg, node-sdk clients.
- Expose ports (interfaces) for adapters to implement.

