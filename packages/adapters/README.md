# packages/adapters

Implements domain ports (DB, HTTP, cache, queue). May depend on infra libs.

- Allowed deps: `@domain`, `@contracts`, infra SDKs.
- Disallowed: importing app code from `apps/*`.

